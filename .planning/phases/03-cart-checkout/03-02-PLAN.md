---
phase: 03-cart-checkout
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [src/components/cart/CartDrawer.tsx, src/components/cart/CartItem.tsx, src/components/nav/Navbar.tsx]
autonomous: false
requirements: [CART-01, CART-02, CART-04, ANIM-04]
must_haves:
  truths:
    - "Cart drawer slides in from the left in Arabic and right in English"
    - "Subtotal is calculated correctly in SAR"
    - "Quantity controls update the store state immediately"
  artifacts:
    - path: "src/components/cart/CartDrawer.tsx"
      provides: "Main slide-out container"
    - path: "src/components/cart/CartItem.tsx"
      provides: "Individual item rendering with controls"
  key_links:
    - from: "src/components/cart/CartItem.tsx"
      to: "src/store/cartStore.ts"
      via: "updateQuantity call"
---

<objective>
Implement the interactive cart drawer with full line-item controls and direction-aware animations.

Purpose: To provide a high-end shopping experience that allows users to manage their selection without leaving the current page.
Output: Direction-aware CartDrawer component.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cart-checkout/03-CONTEXT.md
@.planning/phases/03-cart-checkout/03-UI-SPEC.md
@src/components/ui/sheet.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build CartDrawer Container</name>
  <files>src/components/cart/CartDrawer.tsx</files>
  <read_first>src/components/ui/sheet.tsx, .planning/phases/03-cart-checkout/03-UI-SPEC.md</read_first>
  <action>
    Create the CartDrawer using the `Sheet` component. 
    D-03/ANIM-04: Slide direction must be `right` for EN and `left` for AR.
    Apply styling from ORKI-UI-01: `bg-black/95`, `backdrop-blur-xl`, `border-white/10`.
    Implement the empty state (ORKI-UI-06) if `items.length === 0`.
    Add the Subtotal (CART-04) and "Checkout" CTA at the bottom.
  </action>
  <verify>
    <automated>grep -q "SheetContent" src/components/cart/CartDrawer.tsx</automated>
  </verify>
  <done>CartDrawer container exists with correct locale-aware slide direction and branding.</done>
</task>

<task type="auto">
  <name>Task 2: Implement CartItem Component</name>
  <files>src/components/cart/CartItem.tsx</files>
  <read_first>src/store/cartStore.ts, .planning/phases/03-cart-checkout/03-UI-SPEC.md</read_first>
  <action>
    Create the CartItem component per ORKI-UI-03.
    Display product image (1:1 aspect), name (uppercase), size, and price.
    Implement D-02: Inline +/- controls calling `updateQuantity` and a "Remove" button calling `removeItem`.
  </action>
  <verify>
    <automated>grep -q "updateQuantity" src/components/cart/CartItem.tsx</automated>
  </verify>
  <done>CartItem allows granular quantity management and removal within the drawer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Cart Drawer with line-item management and RTL/LTR animations.</what-built>
  <how-to-verify>
    1. Add an item from a PDP.
    2. Verify drawer slides open automatically (D-01).
    3. Switch to Arabic: verify drawer now slides from the left (D-03).
    4. Test +/- controls and remove action.
    5. Check subtotal calculation.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Manual verification of the slide direction and interaction flow is required.
</verification>

<success_criteria>
- Drawer is direction-aware.
- Subtotal and items update live.
- Visuals match the high-end editorial spec in ORKI-UI-01.
</success_criteria>

<output>
After completion, create `.planning/phases/03-cart-checkout/03-02-SUMMARY.md`
</output>
