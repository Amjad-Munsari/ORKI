---
phase: 03-cart-checkout
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified: [src/app/[locale]/checkout/page.tsx, src/components/checkout/ShippingForm.tsx, src/components/checkout/OrderSummary.tsx]
autonomous: false
requirements: [CHKOUT-01, CHKOUT-02]
must_haves:
  truths:
    - "Checkout page displays a shipping form and a sticky order summary"
    - "Shipping form includes Saudi-specific fields (District, City)"
    - "Order summary displays all items from the cart store"
  artifacts:
    - path: "src/app/[locale]/checkout/page.tsx"
      provides: "Checkout page layout"
    - path: "src/components/checkout/ShippingForm.tsx"
      provides: "Localized address input"
  key_links:
    - from: "src/app/[locale]/checkout/page.tsx"
      to: "src/store/cartStore.ts"
      via: "useCartStore items read"
---

<objective>
Build the single-page checkout flow with a localized shipping form and a persistent order summary.

Purpose: To provide a streamlined, low-friction path to purchase.
Output: Functional checkout page.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cart-checkout/03-CONTEXT.md
@.planning/phases/03-cart-checkout/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ShippingForm</name>
  <files>src/components/checkout/ShippingForm.tsx</files>
  <read_first>.planning/phases/03-cart-checkout/03-UI-SPEC.md</read_first>
  <action>
    Create the ShippingForm per ORKI-UI-04.
    Use underline-only inputs (`border-b border-white/20`) with floating labels.
    Include Saudi-specific fields: First Name, Last Name, Email, Phone (+966), City, District, Street Address, Apartment/Villa (Optional).
    Add basic Zod validation for required fields.
  </action>
  <verify>
    <automated>grep -q "District" src/components/checkout/ShippingForm.tsx</automated>
  </verify>
  <done>ShippingForm exists with correct localized fields and high-end visual style.</done>
</task>

<task type="auto">
  <name>Task 2: Build OrderSummary Component</name>
  <files>src/components/checkout/OrderSummary.tsx</files>
  <read_first>src/store/cartStore.ts, .planning/phases/03-cart-checkout/03-CONTEXT.md</read_first>
  <action>
    Implement the OrderSummary component (CHKOUT-02).
    D-05: Ensure it is sticky on desktop using `sticky top-24`.
    Display line items (image, name, price) and the total in SAR.
    Prepare a slot for "Error Alerts" per D-07.
  </action>
  <verify>
    <automated>grep -q "sticky" src/components/checkout/OrderSummary.tsx</automated>
  </verify>
  <done>OrderSummary displays cart contents and maintains visibility during form completion.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Checkout page layout with shipping form and sticky summary.</what-built>
  <how-to-verify>
    1. Navigate to `/en/checkout` with items in cart.
    2. Verify single-page layout (D-04).
    3. Verify form field styling (ORKI-UI-04).
    4. Scroll down: verify summary stays visible on desktop (D-05).
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Check visual alignment and responsiveness of the checkout page.
</verification>

<success_criteria>
- Shipping form contains all required Saudi fields.
- Layout is high-end and follows the single-page decision.
- Summary accurately reflects cart contents.
</success_criteria>

<output>
After completion, create `.planning/phases/03-cart-checkout/03-03-SUMMARY.md`
</output>
