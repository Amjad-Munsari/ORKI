---
phase: 03-cart-checkout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/store/cartStore.ts, src/components/nav/CartBadge.tsx, src/components/pdp/AddToCartButton.tsx]
autonomous: true
requirements: [NAV-03, CART-03]
must_haves:
  truths:
    - "Cart badge updates live when items are added"
    - "Cart state supports quantity updates and removal"
    - "Cart badge shows zero when empty"
  artifacts:
    - path: "src/store/cartStore.ts"
      provides: "updateQuantity action"
    - path: "src/components/nav/CartBadge.tsx"
      provides: "Visual count badge"
  key_links:
    - from: "src/components/pdp/AddToCartButton.tsx"
      to: "src/store/cartStore.ts"
      via: "addItem call"
---

<objective>
Update the cart store with granular controls and implement the interactive cart badge in the navigation.

Purpose: To provide the reactive state foundation for the cart drawer and ensure the user sees immediate feedback on product pages.
Output: Extended cart store and a functional navbar badge.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cart-checkout/03-CONTEXT.md
@.planning/phases/03-cart-checkout/03-UI-SPEC.md
@src/store/cartStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Cart Store</name>
  <files>src/store/cartStore.ts</files>
  <read_first>src/store/cartStore.ts, src/types/domain.ts</read_first>
  <action>
    Add `updateQuantity(productId: string, selectedSize: string, delta: number)` to the `CartState` and implementation. 
    Ensure it clamps quantity to minimum 1. 
    Reference D-02 for inline control support.
  </action>
  <verify>
    <automated>grep -q "updateQuantity" src/store/cartStore.ts</automated>
  </verify>
  <done>Store supports granular quantity updates without full removal.</done>
</task>

<task type="auto">
  <name>Task 2: Implement CartBadge UI</name>
  <files>src/components/nav/CartBadge.tsx</files>
  <read_first>src/components/nav/CartBadge.tsx, .planning/phases/03-cart-checkout/03-UI-SPEC.md</read_first>
  <action>
    Implement the badge UI per ORKI-UI-02. 
    Use `bg-white`, `text-black`, `font-sans text-[10px] font-bold`.
    Add a Framer Motion `AnimatePresence` wrapper for the "pop" effect on count change.
    Handle empty state: do not render badge if count is 0.
  </action>
  <verify>
    <automated>grep -q "AnimatePresence" src/components/nav/CartBadge.tsx</automated>
  </verify>
  <done>CartBadge displays live count with the specified "pop" animation.</done>
</task>

<task type="auto">
  <name>Task 3: Trigger Drawer on Add</name>
  <files>src/components/pdp/AddToCartButton.tsx</files>
  <read_first>src/components/pdp/AddToCartButton.tsx, .planning/phases/03-cart-checkout/03-CONTEXT.md</read_first>
  <action>
    Update AddToCartButton to trigger the cart drawer open state after a successful `addItem`.
    This requires a new `isDrawerOpen` state in the `cartStore` or a dedicated `useUIStore`.
    Decision D-01: Auto-open on add.
  </action>
  <verify>
    <automated>grep -q "addItem" src/components/pdp/AddToCartButton.tsx</automated>
  </verify>
  <done>Add to Cart action now initiates the drawer open flow.</done>
</task>

</tasks>

<verification>
Check that adding an item to cart increments the badge and triggers the intended (yet to be built) drawer state.
</verification>

<success_criteria>
- Cart badge is reactive to store changes.
- Store allows +/- quantity adjustments.
- Adding items triggers the auto-open intent.
</success_criteria>

<output>
After completion, create `.planning/phases/03-cart-checkout/03-01-SUMMARY.md`
</output>
