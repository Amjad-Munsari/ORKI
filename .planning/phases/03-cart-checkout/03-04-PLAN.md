---
phase: 03-cart-checkout
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified: [src/components/checkout/PaymentGrid.tsx, src/app/api/checkout/mock/route.ts, src/app/[locale]/checkout/confirmation/page.tsx]
autonomous: true
requirements: [CHKOUT-03, CHKOUT-04, CHKOUT-05]
must_haves:
  truths:
    - "Payment grid displays branded buttons for Mada, STC Pay, etc."
    - "Mock API handles order submission and returns a mock order ID"
    - "Confirmation page displays the mock order details"
  artifacts:
    - path: "src/components/checkout/PaymentGrid.tsx"
      provides: "Branded quick-button grid"
    - path: "src/app/api/checkout/mock/route.ts"
      provides: "Submission endpoint"
  key_links:
    - from: "src/app/[locale]/checkout/page.tsx"
      to: "/api/checkout/mock"
      via: "POST submission"
---

<objective>
Implement the payment method selector and the end-to-end mock checkout completion flow.

Purpose: To finalize the purchase path and provide a realistic "Thank You" experience for the user.
Output: Completed mock checkout flow.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cart-checkout/03-CONTEXT.md
@.planning/phases/03-cart-checkout/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build PaymentGrid</name>
  <files>src/components/checkout/PaymentGrid.tsx</files>
  <read_first>.planning/phases/03-cart-checkout/03-UI-SPEC.md</read_first>
  <action>
    Create the PaymentGrid component per ORKI-UI-05.
    Implement a 2-column grid of buttons for: Credit/Debit Card, Mada, STC Pay, Apple Pay, Cash on Delivery.
    Use Decision D-06: Branded logos in monochrome, inverting on selection.
  </action>
  <verify>
    <automated>grep -q "Apple Pay" src/components/checkout/PaymentGrid.tsx</automated>
  </verify>
  <done>Payment selector is visually high-end and localized for Saudi payment methods.</done>
</task>

<task type="auto">
  <name>Task 2: Setup Mock Checkout API</name>
  <files>src/app/api/checkout/mock/route.ts</files>
  <action>
    Create a mock POST endpoint that accepts the shipping form and payment data.
    Implement a simple logic to return a random order ID (e.g., ORK-XXXXXX) after a 1.5s artificial delay.
    Handle mock error states (e.g., if a specific flag is passed in the request, return 400 for OOS).
  </action>
  <verify>
    <automated>test -f src/app/api/checkout/mock/route.ts</automated>
  </verify>
  <done>Mock API provides a realistic completion/error delay for the UI.</done>
</task>

<task type="auto">
  <name>Task 3: Create Confirmation Page</name>
  <files>src/app/[locale]/checkout/confirmation/page.tsx</files>
  <read_first>.planning/phases/03-cart-checkout/03-UI-SPEC.md</read_first>
  <action>
    Implement the confirmation page (CHKOUT-05).
    Display "THANK YOU FOR YOUR ORDER" in large uppercase editorial typography.
    Show the mock Order ID, a summary of items purchased, and a "Continue Shopping" button.
    Clear the `cartStore` on mount if a successful order ID is present in the URL/state.
  </action>
  <verify>
    <automated>grep -q "THANK YOU" src/app/[locale]/checkout/confirmation/page.tsx</automated>
  </verify>
  <done>Order confirmation page successfully closes the purchase loop and clears the cart.</done>
</task>

</tasks>

<verification>
Perform a full end-to-end checkout flow from Shop -> PDP -> Cart -> Checkout -> Confirmation.
</verification>

<success_criteria>
- Branded payment grid is functional and visual.
- API submission flow shows loading states.
- Confirmation page displays the mock order ID and resets the store.
</success_criteria>

<output>
After completion, create `.planning/phases/03-cart-checkout/03-04-SUMMARY.md`
</output>
