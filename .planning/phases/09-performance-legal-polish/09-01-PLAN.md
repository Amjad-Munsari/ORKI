---
phase: 09-performance-legal-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/seo.ts
  - messages/en.json
  - messages/ar.json
  - src/components/footer/Footer.tsx
  - src/components/checkout/TrustSignals.tsx
  - src/components/pdp/PDPInfoPanel.tsx
  - src/app/[locale]/legal/layout.tsx
  - src/app/[locale]/shop/[category]/[slug]/page.tsx
  - .planning/phases/09-performance-legal-polish/notes/wave-0-checks.md
autonomous: true
requirements:
  - SEO-03
  - LGL-01
  - LGL-02
  - LGL-03
user_setup: []
must_haves:
  truths:
    - "buildMetadata helper exists and composes title/description/canonical/hreflang/OG"
    - "Legal i18n namespace skeleton present in EN and AR with all 17 privacy / 17 terms / 8 cookies section keys"
    - "Returns-policy duration is 14 days everywhere it appears in customer-facing copy"
    - "Footer legal-link href targets /legal/privacy, /legal/terms, /legal/cookies"
    - "Legal route folder layout exists and renders children unmodified"
    - "Title-template double-application sanity check is documented"
    - "Phase 9 wave manifest is captured in notes/wave-0-checks.md"
  artifacts:
    - path: "src/lib/seo.ts"
      provides: "buildMetadata({path, locale, titleKey, descriptionKey, ogImage?}) helper"
      contains: "alternates"
    - path: "src/app/[locale]/legal/layout.tsx"
      provides: "Legal segment layout"
    - path: "messages/en.json"
      provides: "Legal.*, Errors.*, Meta.* namespaces (skeleton with all section keys)"
    - path: "messages/ar.json"
      provides: "Mirror of EN Legal/Errors/Meta namespaces in formal AR legal register"
    - path: ".planning/phases/09-performance-legal-polish/notes/wave-0-checks.md"
      provides: "Title-template / PDP double-suffix check + returns-policy reconciliation note + Phase 9 wave manifest"
  key_links:
    - from: "src/lib/seo.ts"
      to: "messages/{en,ar}.json"
      via: "getTranslations({locale, namespace}) call"
      pattern: "getTranslations\\("
    - from: "src/components/footer/Footer.tsx"
      to: "src/app/[locale]/legal/{privacy,terms,cookies}"
      via: "next-intl <Link href='/legal/...'>"
      pattern: "/legal/(privacy|terms|cookies)"
---

<objective>
Wave 0 foundation: every downstream Phase 9 plan depends on the artifacts produced here.

This plan delivers (1) the `src/lib/seo.ts buildMetadata` helper used by every page-level `generateMetadata` in Wave 1; (2) the EN+AR i18n namespace skeletons (`Legal`, `Errors`, `Meta`, `Footer.legal`, `CookieBanner`) — full key set in place so Wave 1 plans only fill in copy; (3) the `src/app/[locale]/legal/layout.tsx` segment layout so Wave 1 legal pages have a route folder; (4) the footer href migration to the new `/legal/*` shape with the new Cookie Policy link and i18n labels; (5) the returns-policy duration reconciliation across Footer + Shop + TrustSignals + PDP to a single value (14 days, per RESEARCH.md recommendation — more conservative); (6) a Wave 0 sanity check on the `title.template` cascade vs PDP `generateMetadata` to confirm the suffix is applied exactly once.

Purpose: unblock all Wave 1 parallel work without requiring downstream tasks to scaffold infrastructure.
Output: 1 new helper, 1 new layout, 1 new note file, 5 modified files (footer, two trust-signal surfaces, PDP page, two i18n bundles).
</objective>

<execution_context>
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-performance-legal-polish/09-CONTEXT.md
@.planning/phases/09-performance-legal-polish/09-RESEARCH.md
@.planning/phases/09-performance-legal-polish/09-PATTERNS.md
@.planning/phases/09-performance-legal-polish/09-UI-SPEC.md
@.planning/phases/09-performance-legal-polish/09-VALIDATION.md
@CLAUDE.md
@messages/en.json
@messages/ar.json
@src/i18n/routing.ts
@src/components/footer/Footer.tsx
@src/components/checkout/TrustSignals.tsx
@src/components/pdp/PDPInfoPanel.tsx
@src/app/[locale]/shop/[category]/[slug]/page.tsx

<interfaces>
<!-- Existing patterns the executor MUST mirror, not invent. -->

next-intl routing config (`src/i18n/routing.ts`):
```ts
export const routing = defineRouting({
  locales: ['en', 'ar'],
  defaultLocale: 'en',
  localePrefix: 'always',
});
```

Existing PDP `generateMetadata` shape to migrate (`src/app/[locale]/shop/[category]/[slug]/page.tsx`):
```tsx
export async function generateMetadata({ params }: Props): Promise<Metadata> {
  const { slug, locale } = await params
  const product = await getProductBySlug(slug)
  if (!product) return {}
  const title = `${product.name[locale]} | ORKI`        // ← will double-suffix once parent sets template; FIX in this plan
  const description = product.description[locale]
  const image = product.images[0] || '/images/og-default.png'
  return { title, description, openGraph: {...}, twitter: {...} }
}
```

Footer current legal-link group (`src/components/footer/Footer.tsx:56-63`):
```tsx
<div className="space-y-6">
  <span className="text-xs font-semibold text-white/40 block">(Legal)</span>
  <ul className="space-y-4">
    <li><Link href="/privacy" ...>Privacy Policy</Link></li>
    <li><Link href="/terms" ...>Terms of Service</Link></li>
  </ul>
</div>
```

Existing returns-policy strings (audit results):
- `messages/en.json:43` `"returnPolicy": "Free returns within 14 days. No questions asked."`  ← Shop  (14)
- `messages/en.json:76` `"returnsPolicy": "30-Day Returns"`  ← Checkout TrustSignals  (30)
- `messages/ar.json:76` `"returnsPolicy": "إرجاع خلال 30 يوم"`  (30)
- `src/components/footer/Footer.tsx:17` literal `Easy returns within 30 days`  (30)
- `src/components/pdp/PDPInfoPanel.tsx:78` `Free returns within 14 days. No questions asked.`  (14)

Locked reconciliation: ALL surfaces → **14 days** (per RESEARCH.md Pitfall 8 / UI-SPEC §"[FLAG] Section 8" — more conservative, KSA-typical).
</interfaces>
</context>

<wave_manifest>
**Phase 9 wave structure (post-revision, locked):**
- **Wave 1:** 09-01 (this plan — Wave 0 foundation)
- **Wave 2:** 09-02 (legal pages), 09-03 (analytics + title.template), 09-06 (cookie scaffold) — all depend only on 09-01; parallel
- **Wave 3:** 09-04 (sitemap + robots + OG + per-page metadata wiring) — depends on 09-01 AND 09-03 (consumes title.template set by 09-03)
- **Wave 4:** 09-05 (error boundaries + reliability hygiene) — depends on 09-01 AND 09-04

Same-wave plans have zero `files_modified` overlap. Plan 09-04 was bumped from Wave 2 to Wave 3 to ensure Plan 09-03 has set `title.template` before 09-04 wires per-page `generateMetadata` (otherwise smoke tests could observe partial state). Plan 09-05 was bumped to Wave 4 to keep its `depends_on: ['09-01', '09-04']` graph valid.
</wave_manifest>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create src/lib/seo.ts buildMetadata helper</name>
  <files>src/lib/seo.ts</files>
  <read_first>
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-RESEARCH.md (Pattern 4 §"src/lib/seo.ts buildMetadata helper", lines 409-500)
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-PATTERNS.md (§"src/lib/seo.ts" lines 97-141 — `'server-only'` import + Metadata return shape)
    - C:/dev/Antigravity/ORKI/src/app/[locale]/shop/[category]/[slug]/page.tsx (existing inline `generateMetadata` reference shape)
    - C:/dev/Antigravity/ORKI/src/types/domain.ts (verify `Locale` type export)
  </read_first>
  <action>
    Create `src/lib/seo.ts` with EXACTLY this content (per RESEARCH.md Pattern 4, lines 414-489), implementing D-04 (sitemap + per-page metadata) and D-04 OG default rule:

    ```tsx
    import 'server-only';
    import type { Metadata } from 'next';
    import { getTranslations } from 'next-intl/server';
    import type { Locale } from '@/types/domain';

    const HOST = 'https://orki.sa';
    const DEFAULT_OG_IMAGE = `${HOST}/og-default.png`;

    interface BuildMetadataInput {
      /** Path relative to the locale prefix, e.g. '/about', '/legal/privacy', '/' */
      path: string;
      /** Required current locale */
      locale: Locale;
      /** next-intl namespace.key for the title (e.g. 'Meta.about.title') — returned as the BARE page name; root layout's title.template applies '%s | ORKI' */
      titleKey: string;
      /** next-intl namespace.key for the description */
      descriptionKey: string;
      /** Optional override for OG image (absolute URL). Defaults to /og-default.png. */
      ogImage?: string;
    }

    export async function buildMetadata({
      path,
      locale,
      titleKey,
      descriptionKey,
      ogImage,
    }: BuildMetadataInput): Promise<Metadata> {
      const [titleNs, ...titleRest] = titleKey.split('.');
      const titleSubKey = titleRest.join('.');
      const tTitle = await getTranslations({ locale, namespace: titleNs });

      const [descNs, ...descRest] = descriptionKey.split('.');
      const descSubKey = descRest.join('.');
      const tDesc = await getTranslations({ locale, namespace: descNs });

      const title = tTitle(titleSubKey);
      const description = tDesc(descSubKey);
      const image = ogImage ?? DEFAULT_OG_IMAGE;

      const suffix = path === '/' ? '' : path;
      const canonical = `${HOST}/${locale}${suffix}`;

      return {
        title, // bare; root layout sets title.template = '%s | ORKI'
        description,
        alternates: {
          canonical,
          languages: {
            en: `${HOST}/en${suffix}`,
            ar: `${HOST}/ar${suffix}`,
            'x-default': `${HOST}/en${suffix}`,
          },
        },
        openGraph: {
          title,
          description,
          url: canonical,
          siteName: 'ORKI',
          locale: locale === 'ar' ? 'ar_SA' : 'en_US',
          type: 'website',
          images: [{ url: image, width: 1200, height: 630, alt: 'ORKI' }],
        },
        twitter: {
          card: 'summary_large_image',
          title,
          description,
          images: [image],
        },
      };
    }
    ```

    Notes:
    - `'server-only'` import (mirrors `src/lib/products.ts:17` — PATTERNS §"`server-only` import convention").
    - `Locale` type imported from `@/types/domain` (existing project type).
    - Title is returned BARE (no ` | ORKI` suffix) so the parent layout's `title.template` applies it exactly once. Plan 09-04 sets `title.template` in `[locale]/layout.tsx`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('src/lib/seo.ts','utf8'); process.exit(/'server-only'/.test(c) && /export async function buildMetadata/.test(c) && /alternates/.test(c) && /'x-default'/.test(c) && /og-default\.png/.test(c) ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/seo.ts` exists.
    - `import 'server-only'` is the first non-comment line.
    - Default export or named export `buildMetadata` is an async function returning `Promise<Metadata>`.
    - Returns object includes `alternates.canonical`, `alternates.languages.en`, `alternates.languages.ar`, `alternates.languages['x-default']`.
    - Default OG image path resolves to `https://orki.sa/og-default.png` when `ogImage` is omitted.
    - Type-check passes: `npx tsc --noEmit` exits 0 against this file (verify in Task 6 batch type-check).
  </acceptance_criteria>
  <done>
    Helper compiles, exports `buildMetadata`, contains the exact `alternates.languages` shape, and uses `'server-only'`. Confirmed by grep of the four key strings.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add Legal / Errors / Meta / Footer.legal / CookieBanner i18n namespace skeleton (EN + AR)</name>
  <files>messages/en.json, messages/ar.json</files>
  <read_first>
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-RESEARCH.md (§"Code Examples — messages/en.json skeleton additions", lines 937-986; §"Section-level skeleton: legal documents", lines 872-936)
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-PATTERNS.md (§"i18n string conventions" lines 491-557 — top-level namespace = page or feature, camelCase, AR mirrors EN 1:1)
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-UI-SPEC.md (§"Copywriting Contract" lines 105-179 — locked EN/AR strings for chrome, errors, 404, footer, banner)
    - C:/dev/Antigravity/ORKI/messages/en.json (full file — to confirm existing namespace shape and where to merge)
    - C:/dev/Antigravity/ORKI/messages/ar.json (full file — to confirm 1:1 mirror shape)
  </read_first>
  <action>
    Add the following NEW namespaces (or merge if any subset exists) to BOTH `messages/en.json` and `messages/ar.json`. Strings come VERBATIM from UI-SPEC §"Copywriting Contract" (chrome + error/404 + footer + banner) and from RESEARCH.md §"Section-level skeleton" (legal section structure). For section bodies, scaffold with `"body": "[DRAFT — populate in Plan 09-02]"` placeholder strings; do NOT draft body copy here — Plan 09-02 owns that. **AR formal legal register** required (per Pitfall 6); flag any section body whose AR draft is uncertain with `<!-- [AR-LEGAL-REVIEW] -->` (kept inline in the JSON value as a literal substring — JSON has no comment syntax, so use `"[AR-LEGAL-REVIEW] {text}"` prefix on the value).

    NEW namespaces to add to `messages/en.json` (mirror in `messages/ar.json`):

    ```jsonc
    {
      "Legal": {
        "lastUpdatedLabel": "Last updated:",
        "templateDisclaimer": "This document is a template draft pending legal review. Contact us with questions.",
        "privacy": {
          "metaTitle": "Privacy Policy",
          "metaDescription": "How ORKI collects, stores, and uses your personal data. KSA PDPL and GDPR aligned.",
          "eyebrow": "(Legal) — 01",
          "heading": "Privacy Policy",
          "section1":  { "heading": "Who we are", "body": "[DRAFT]" },
          "section2":  { "heading": "What personal data we collect", "body": "[DRAFT]" },
          "section3":  { "heading": "Why we collect it", "body": "[DRAFT]" },
          "section4":  { "heading": "How we collect it", "body": "[DRAFT]" },
          "section5":  { "heading": "How we store it", "body": "[DRAFT]" },
          "section6":  { "heading": "How we process and use it", "body": "[DRAFT]" },
          "section7":  { "heading": "Who we share it with", "body": "[DRAFT]" },
          "section8":  { "heading": "International transfers", "body": "[DRAFT]" },
          "section9":  { "heading": "How long we keep it", "body": "[DRAFT]" },
          "section10": { "heading": "How we destroy it", "body": "[DRAFT]" },
          "section11": { "heading": "Your rights", "body": "[DRAFT]" },
          "section12": { "heading": "How to exercise your rights", "body": "[DRAFT — must include controller email]" },
          "section13": { "heading": "Cookies", "body": "[DRAFT]" },
          "section14": { "heading": "Children's privacy", "body": "[DRAFT]" },
          "section15": { "heading": "Changes to this policy", "body": "[DRAFT]" },
          "section16": { "heading": "Contact us", "body": "[DRAFT — must include controller contact email]" }
        },
        "terms": {
          "metaTitle": "Terms & Conditions",
          "metaDescription": "ORKI's terms of sale: pricing, payment, shipping, returns, and dispute resolution.",
          "eyebrow": "(Legal) — 02",
          "heading": "Terms & Conditions",
          "section1":  { "heading": "Acceptance of terms", "body": "[DRAFT]" },
          "section2":  { "heading": "About ORKI", "body": "[DRAFT]" },
          "section3":  { "heading": "Eligibility", "body": "[DRAFT]" },
          "section4":  { "heading": "Account and orders", "body": "[DRAFT]" },
          "section5":  { "heading": "Pricing and currency", "body": "[DRAFT]" },
          "section6":  { "heading": "Payment", "body": "[DRAFT]" },
          "section7":  { "heading": "Shipping", "body": "[DRAFT]" },
          "section8":  { "heading": "Returns and refunds", "body": "[DRAFT — must state 14 days]" },
          "section9":  { "heading": "Order cancellation", "body": "[DRAFT]" },
          "section10": { "heading": "Liability", "body": "[DRAFT]" },
          "section11": { "heading": "Intellectual property", "body": "[DRAFT]" },
          "section12": { "heading": "Prohibited uses", "body": "[DRAFT]" },
          "section13": { "heading": "Privacy", "body": "[DRAFT]" },
          "section14": { "heading": "Governing law and dispute resolution", "body": "[DRAFT]" },
          "section15": { "heading": "Changes to these terms", "body": "[DRAFT]" },
          "section16": { "heading": "Contact us", "body": "[DRAFT]" }
        },
        "cookies": {
          "metaTitle": "Cookie Policy",
          "metaDescription": "Cookies ORKI uses to run the cart and checkout. No analytics or marketing cookies are set.",
          "eyebrow": "(Legal) — 03",
          "heading": "Cookie Policy",
          "section1": { "heading": "What are cookies", "body": "[DRAFT]" },
          "section2": { "heading": "Cookies we use", "body": "[DRAFT — renders <CookieTable> in page]" },
          "section3": { "heading": "We do not use", "body": "[DRAFT — explicitly: no analytics cookies, no marketing cookies, no third-party trackers]" },
          "section4": { "heading": "Vercel Analytics and Speed Insights", "body": "[DRAFT — explain cookieless beacons]" },
          "section5": { "heading": "Your choices", "body": "[DRAFT]" },
          "section6": { "heading": "Changes to this policy", "body": "[DRAFT]" },
          "section7": { "heading": "Contact us", "body": "[DRAFT]" },
          "tableHeaders": {
            "name": "Name",
            "purpose": "Purpose",
            "duration": "Duration",
            "type": "Type"
          },
          "tableRows": {
            "orkiSidName":     "orki_sid",
            "orkiSidPurpose":  "Cart persistence",
            "orkiSidDuration": "30 days",
            "orkiSidType":     "Strictly necessary",
            "sbAuthName":      "sb-*",
            "sbAuthPurpose":   "Admin authentication",
            "sbAuthDuration":  "Session",
            "sbAuthType":      "Strictly necessary"
          }
        }
      },
      "Errors": {
        "heading": "Something broke.",
        "body":    "We're working on it. Try again, or head back home.",
        "retry":   "Try again",
        "home":    "Return home",
        "notFound": {
          "heading":    "404 — Lost in the noise.",
          "body":       "This page doesn't exist or was moved. Try the shop instead.",
          "browseShop": "Browse shop",
          "returnHome": "Return home"
        },
        "section": {
          "shopLoadFailed":      "We're having trouble loading the catalog right now. Try again in a moment.",
          "orderLoadFailed":     "We can't pull up your order right now — your payment is confirmed. Email us if you need help.",
          "adminOrdersFailed":   "Order list temporarily unavailable."
        }
      },
      "Meta": {
        "siteTitle":   "ORKI — Saudi Streetwear",
        "home":        { "title": "ORKI — Saudi Streetwear",   "description": "Saudi streetwear. Dark. Underground." },
        "about":       { "title": "About",                      "description": "ORKI's story: Saudi streetwear, dark and underground." },
        "contact":     { "title": "Contact",                    "description": "Get in touch with ORKI for orders, support, or partnerships." },
        "shop":        { "title": "Shop",                       "description": "Shop ORKI streetwear: tops and bottoms. New drops first." },
        "shopTops":    { "title": "Tops",                       "description": "Hoodies, tees, and outerwear from ORKI." },
        "shopBottoms": { "title": "Bottoms",                    "description": "Pants, shorts, and joggers from ORKI." }
      },
      "Shop": {
        "categories": {
          "tops":    { "title": "Tops",    "description": "Hoodies, tees, and outerwear from ORKI." },
          "bottoms": { "title": "Bottoms", "description": "Pants, shorts, and joggers from ORKI." }
        }
      },
      "Footer": {
        "legal": {
          "groupLabel": "(Legal)",
          "privacy":    "Privacy Policy",
          "terms":      "Terms & Conditions",
          "cookies":    "Cookie Policy"
        }
      },
      "CookieBanner": {
        "body":          "We use strictly-necessary cookies to run the cart and checkout. No analytics or marketing cookies are set.",
        "primaryCta":    "Got it",
        "secondaryLink": "Read our Cookie Policy"
      }
    }
    ```

    AR mirror — formal legal register. Use UI-SPEC §"Copywriting Contract" verbatim for chrome strings (Privacy `سياسة الخصوصية`, etc., Last updated `آخر تحديث:`, disclaimer `هذه الوثيقة مسودة قابلة للمراجعة القانونية. تواصل معنا لأي استفسار.`). For section headings, use the AR strings from RESEARCH.md §"Section-level skeleton" tables (lines 880-934). For the Errors namespace, use UI-SPEC §"Error pages" / "404 pages" / "Section-degradation copy" tables verbatim (`حدث خطأ ما.`, `إعادة المحاولة`, `العودة إلى الرئيسية`, `404 — ضاع في الضجيج.`, `تصفح المتجر`, `نواجه مشكلة في تحميل المتجر الآن. حاول مرة أخرى بعد قليل.`, etc.). For `adminOrdersFailed` AR is intentionally identical to EN (admin pages stay EN-only per UI-SPEC line 146).

    **AR mirror for the new `Shop.categories.{tops,bottoms}` keys** (Plan 09-04 Task 4 will consume these for the category page `generateMetadata`):
    ```jsonc
    "Shop": {
      "categories": {
        "tops":    { "title": "القمم",    "description": "هوديز وتيشيرتات وملابس خارجية من أوركي." },
        "bottoms": { "title": "السراويل", "description": "بناطيل وشورتات وجوغرز من أوركي." }
      }
    }
    ```
    Note: `Shop` namespace already exists in `messages/{en,ar}.json` (with `returnPolicy` etc.); MERGE the new `categories` sub-key into it — do NOT replace.

    Important also: in `messages/en.json` and `messages/ar.json` UPDATE the existing TrustSignals returns key (it currently reads `"returnsPolicy": "30-Day Returns"` / `"إرجاع خلال 30 يوم"`) to **`"returnsPolicy": "14-Day Returns"`** / **`"returnsPolicy": "إرجاع خلال 14 يوم"`** to reconcile the duration (per RESEARCH.md Pitfall 8 / planner reconciliation #2). Existing key path is `Checkout.trust.returnsPolicy`.

    Do NOT touch the existing `Shop.returnPolicy` ("Free returns within 14 days. No questions asked.") — already 14 days, already correct.

    Preserve all existing namespaces (`Nav`, `Footer.{shipping,returns,contact,copyright}`, `Shop`, `Checkout`, `Order`, etc.) untouched. Add new keys via merge — keep the file valid JSON.
  </action>
  <verify>
    <automated>node -e "const en=require('./messages/en.json'); const ar=require('./messages/ar.json'); const ok = en.Legal && en.Legal.privacy && en.Legal.privacy.section16 && en.Legal.terms && en.Legal.terms.section16 && en.Legal.cookies && en.Legal.cookies.section7 && en.Legal.cookies.tableHeaders && en.Errors && en.Errors.notFound && en.Meta && en.Meta.about && en.Footer.legal && en.CookieBanner && ar.Legal && ar.Legal.privacy && ar.Legal.privacy.section16 && ar.Errors && ar.Footer.legal && en.Checkout.trust.returnsPolicy.includes('14') && ar.Checkout.trust.returnsPolicy.includes('14') && en.Shop.categories && en.Shop.categories.tops && en.Shop.categories.tops.title && en.Shop.categories.bottoms && ar.Shop.categories && ar.Shop.categories.tops && ar.Shop.categories.bottoms; process.exit(ok ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - VALIDATION §SEO-03 uniqueness: `node -e "const en=require('./messages/en.json'); const titles=[en.Meta.about.title, en.Meta.contact.title, en.Meta.shop.title, en.Legal.privacy.metaTitle, en.Legal.terms.metaTitle, en.Legal.cookies.metaTitle, en.Shop.categories.tops.title, en.Shop.categories.bottoms.title]; const dupes=titles.filter((t,i)=>titles.indexOf(t)!==i); process.exit(dupes.length===0?0:1)"` exits 0.
    - `messages/en.json` parses as JSON (`node -e "require('./messages/en.json')"` exits 0).
    - `messages/ar.json` parses as JSON.
    - VALIDATION row LGL-01: privacy section1..section16 all present in `Legal.privacy` (16 sections — section17 disclaimer is the shared `Legal.templateDisclaimer` key).
    - VALIDATION row LGL-04: `Legal.privacy.section11` (rights) and `Legal.privacy.section12` / `section16` (contact mechanism) keys exist.
    - VALIDATION row LGL-02 cookies: `Legal.cookies.section3.body` placeholder mentions "no analytics cookies" intent (verified during Plan 09-02 body-copy task; here the heading and skeleton suffice).
    - VALIDATION row SEO-03 returns key `Checkout.trust.returnsPolicy` value contains `14` in both EN and AR.
    - **Issue #3 fix:** `Shop.categories.tops.{title,description}` and `Shop.categories.bottoms.{title,description}` keys exist in BOTH `messages/en.json` and `messages/ar.json` (consumed by Plan 09-04 Task 4 for `/shop/[category]` `generateMetadata`).
    - Every metaTitle is ≤ 60 chars (manual code-review: `Privacy Policy` = 14, `Terms & Conditions` = 18, `Cookie Policy` = 13, `About` = 5, `Contact` = 7, `Shop` = 4, `Tops` = 4, `Bottoms` = 7 — all well under).
    - Every metaDescription is ≤ 160 chars (manual code-review).
  </acceptance_criteria>
  <done>
    Both message files contain the full Legal / Errors / Meta / Footer.legal / CookieBanner namespaces with all section keys, EN and AR are 1:1 in shape, JSON parses, the Checkout.trust.returnsPolicy value reconciliation to "14" is complete, and `Shop.categories.{tops,bottoms}` are present in both locales.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Migrate Footer legal links + add Cookie link + i18n labels</name>
  <files>src/components/footer/Footer.tsx</files>
  <read_first>
    - C:/dev/Antigravity/ORKI/src/components/footer/Footer.tsx (full file — current legal-link group + the literal "Easy returns within 30 days" line at :17)
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-PATTERNS.md (§"src/components/footer/Footer.tsx (EDIT)" lines 440-461)
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-UI-SPEC.md (§"Footer legal-link additions" lines 148-162)
    - C:/dev/Antigravity/ORKI/src/i18n/navigation.ts (`Link` from `@/i18n/navigation` is auto-locale-prefixed)
  </read_first>
  <action>
    Edit `src/components/footer/Footer.tsx`:

    1. **Add `getTranslations` import** at the top (Footer is a Server Component per UI-SPEC Open Issue #3; use `getTranslations` from `next-intl/server`, NOT `useTranslations`):
       ```tsx
       import { getTranslations } from 'next-intl/server';
       ```
       If Footer's signature is currently sync, change to `export default async function Footer()` and `await getTranslations('Footer.legal')` inside.

    2. **Reconcile the returns-policy line at `Footer.tsx:17`** — replace the literal `Easy returns within 30 days` with `Easy returns within 14 days` (per Wave 0 reconciliation #2 — single 14-day value sitewide). DO NOT i18n-key this string in this plan; it stays as inline EN. (AR users see the locale via separate component rendering; if AR users need this string, that is a follow-up. Current Footer hard-codes EN inline — this matches the existing convention.)

       **TODO(i18n) comment requirement (Issue #1 fix):** Immediately above the benefit-bar block in `Footer.tsx` (i.e. the `{/* Benefit Bar */}` div at ~line 9), add a comment flagging the four EN-only benefit-bar lines as deferred i18n work:
       ```tsx
       {/* TODO(i18n) benefit-bar: the four lines below (shipping / returns / payments / support)
           are EN-only by current Phase-9 contract. Phase 9 reconciles only the returns-policy
           duration (30 days → 14 days). Full i18n keying of the benefit bar is a future-phase
           concern. */}
       ```
       This makes the EN-only contract explicit and greppable so future planners do not silently inherit the gap. The literal substring `TODO(i18n)` paired with `benefit` is what the acceptance grep will look for.

    3. **Replace the legal-link group block** (currently around `Footer.tsx:56-63`):
       FROM:
       ```tsx
       <div className="space-y-6">
         <span className="text-xs font-semibold text-white/40 block">(Legal)</span>
         <ul className="space-y-4">
           <li><Link href="/privacy" className="...">Privacy Policy</Link></li>
           <li><Link href="/terms" className="...">Terms of Service</Link></li>
         </ul>
       </div>
       ```
       TO (preserve the existing className strings on `<Link>` — do NOT shorten them; copy the original verbatim):
       ```tsx
       {/* Group 2: Legal */}
       <div className="space-y-6">
         <span className="text-xs font-semibold text-white/40 block">{tLegal('groupLabel')}</span>
         <ul className="space-y-4">
           <li><Link href="/legal/privacy" className="<existing className>">{tLegal('privacy')}</Link></li>
           <li><Link href="/legal/terms"   className="<existing className>">{tLegal('terms')}</Link></li>
           <li><Link href="/legal/cookies" className="<existing className>">{tLegal('cookies')}</Link></li>
         </ul>
       </div>
       ```
       where `tLegal = await getTranslations('Footer.legal')`.

    4. Keep `Link` import from `@/i18n/navigation` (already present); locale prefix is automatic.
    5. Keep all other footer groups (shipping/returns/contact/copyright) untouched.

    Do NOT add a `'use client'` directive — Footer must remain a Server Component (UI-SPEC Open Issue #3).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('src/components/footer/Footer.tsx','utf8'); const ok = /\/legal\/privacy/.test(c) && /\/legal\/terms/.test(c) && /\/legal\/cookies/.test(c) && !/href=\"\/privacy\"/.test(c) && !/href=\"\/terms\"/.test(c) && /Easy returns within 14 days/.test(c) && !/Easy returns within 30 days/.test(c) && /getTranslations/.test(c) && /TODO\(i18n\).*benefit/.test(c); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "href=\"/legal/(privacy|terms|cookies)\"" src/components/footer/Footer.tsx` returns ≥ 3 matches.
    - `grep -E "href=\"/(privacy|terms)\"" src/components/footer/Footer.tsx` returns 0 matches (old hrefs gone).
    - `grep "Easy returns within 14 days" src/components/footer/Footer.tsx` returns 1 match.
    - `grep "Easy returns within 30 days" src/components/footer/Footer.tsx` returns 0 matches.
    - `grep -c "getTranslations" src/components/footer/Footer.tsx` ≥ 1.
    - `grep -c "'use client'" src/components/footer/Footer.tsx` = 0 (Footer stays RSC).
    - **Issue #1 fix:** `grep -c "TODO(i18n).*benefit" src/components/footer/Footer.tsx` ≥ 1 (literal substring `TODO(i18n)` paired with `benefit` present in the comment above the benefit-bar block).
  </acceptance_criteria>
  <done>
    Footer renders 3 legal links to `/legal/{privacy,terms,cookies}`, labels come from `Footer.legal.*`, the inline returns-policy line is reconciled to "14 days", and the benefit-bar i18n deferral is explicitly flagged with a `TODO(i18n) benefit-bar` comment.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Reconcile remaining returns-policy mentions (TrustSignals + PDPInfoPanel sanity)</name>
  <files>src/components/checkout/TrustSignals.tsx, src/components/pdp/PDPInfoPanel.tsx</files>
  <read_first>
    - C:/dev/Antigravity/ORKI/src/components/checkout/TrustSignals.tsx (full file — uses `t('returnsPolicy')` from `Checkout.trust` namespace)
    - C:/dev/Antigravity/ORKI/src/components/pdp/PDPInfoPanel.tsx (lines 70-90 — currently reads "Free returns within 14 days. No questions asked.")
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-RESEARCH.md (§"Common Pitfalls" — Pitfall 8 returns-policy reconciliation)
  </read_first>
  <action>
    1. **TrustSignals.tsx** — no code change needed. The string is sourced from `Checkout.trust.returnsPolicy` which Task 2 already updated to "14-Day Returns" / "إرجاع خلال 14 يوم". Just verify file still imports `t('returnsPolicy')` and uses it via icon row at line ~10. If file structure has changed unexpectedly, leave it — the i18n key change in Task 2 is sufficient.

    2. **PDPInfoPanel.tsx (line 78)** — currently reads `: 'Free returns within 14 days. No questions asked.'`. ALREADY 14 days. No change required. Confirm the line reads exactly `Free returns within 14 days. No questions asked.` — if anything else, update to that string.

    Net effect: this task is a verification gate, not a code change. The reconciliation work was done in Tasks 2+3.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const ts=fs.readFileSync('src/components/checkout/TrustSignals.tsx','utf8'); const pdp=fs.readFileSync('src/components/pdp/PDPInfoPanel.tsx','utf8'); const ok = /returnsPolicy/.test(ts) && /Free returns within 14 days/.test(pdp) && !/Free returns within 30 days/.test(pdp) && !/30-Day Returns/.test(ts); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - Sitewide grep audit: `grep -rn "30 days\|30-Day\|30 Day\|إرجاع خلال 30" src/components/ src/app/ messages/` returns 0 returns-policy-related matches (cart cookie comment in `src/lib/cart/session.ts:17` `30 days (CONTEXT.md)` is OUT of scope — that is the cart cookie max-age, NOT the returns policy. Allowlist that one line.)
    - The single allowlisted match is `src/lib/cart/session.ts:17` (cart-cookie max-age constant).
    - `grep -rn "14 days\|14-Day\|14 Day\|إرجاع خلال 14" src/components/footer/Footer.tsx src/components/pdp/PDPInfoPanel.tsx messages/en.json messages/ar.json` returns ≥ 4 matches.
  </acceptance_criteria>
  <done>
    All customer-facing returns-policy mentions read 14 days. The cart-cookie max-age comment is correctly excluded from the audit (different concern).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Create src/app/[locale]/legal/layout.tsx route segment</name>
  <files>src/app/[locale]/legal/layout.tsx</files>
  <read_first>
    - C:/dev/Antigravity/ORKI/src/app/[locale]/layout.tsx (full file — analog for `params: Promise<{locale}>` shape)
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-PATTERNS.md (§"src/app/[locale]/legal/layout.tsx" lines 76-94)
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-UI-SPEC.md (§"Component Inventory" line 189-191 — "just layout segment, no `<html>`/`<body>`/Navbar/Footer")
  </read_first>
  <action>
    Create `src/app/[locale]/legal/layout.tsx` as a thin pass-through wrapper. Per UI-SPEC and PATTERNS, this segment layout MUST NOT render `<html>`, `<body>`, Navbar, or Footer (those belong to the parent locale layout).

    Exact content:
    ```tsx
    import type { ReactNode } from 'react';

    type Props = {
      children: ReactNode;
      params: Promise<{ locale: string }>;
    };

    export default async function LegalLayout({ children }: Props) {
      // Pass-through. The LegalArticle wrapper inside each /legal/{privacy,terms,cookies}/page.tsx
      // owns the visual chrome (max-w-[768px], min-h-screen, eyebrow + h1 + last-updated + body slot).
      return <>{children}</>;
    }
    ```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('src/app/[locale]/legal/layout.tsx','utf8'); const ok=/export default async function LegalLayout/.test(c) && !/<html/.test(c) && !/<body/.test(c) && !/Navbar/.test(c) && !/Footer/.test(c); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/[locale]/legal/layout.tsx` exists.
    - Default export `LegalLayout` is an async function.
    - Renders `{children}` only (no extra DOM).
    - Does NOT contain `<html`, `<body`, `Navbar`, or `Footer` strings.
  </acceptance_criteria>
  <done>
    Legal route folder layout is in place; Wave 1 Plan 09-02 can create `privacy/`, `terms/`, `cookies/` pages without scaffolding the segment first.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Wave 0 sanity checks — title.template double-suffix + vitest config + write notes</name>
  <files>.planning/phases/09-performance-legal-polish/notes/wave-0-checks.md, src/app/[locale]/shop/[category]/[slug]/page.tsx</files>
  <read_first>
    - C:/dev/Antigravity/ORKI/src/app/[locale]/shop/[category]/[slug]/page.tsx (lines 14-40 — current `generateMetadata` builds `${product.name[locale]} | ORKI`)
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-RESEARCH.md (Pattern 4 §"Note on title.template" lines 491-500 — root layout sets template, helper returns BARE title)
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-VALIDATION.md (§"Wave 0 Requirements" — vitest config check)
    - C:/dev/Antigravity/ORKI/vitest.config.ts (already exists per audit — verify presence)
  </read_first>
  <action>
    1. **vitest config check** — `vitest.config.ts` already exists at repo root (verified during planning). No action needed; document this in the notes file (item 1 below).

    2. **PDP `generateMetadata` double-suffix fix** — In Plan 09-03 (Wave 2), `[locale]/layout.tsx` will set `metadata.title = { default, template: '%s | ORKI' }`. The existing PDP at `shop/[category]/[slug]/page.tsx:14-40` builds `const title = ${product.name[locale]} | ORKI` — once the parent template is set, this would render as `Black Hoodie | ORKI | ORKI` (double-suffix). FIX HERE so Plan 09-03 can flip the template safely.

       Edit the file: change the `title` line FROM:
       ```tsx
       const title = `${product.name[locale]} | ORKI`
       ```
       TO:
       ```tsx
       const title = product.name[locale]
       ```
       Inside the returned `Metadata` object, the `title` field becomes the bare product name; root `title.template` (set by Plan 09-03) will append `| ORKI`. Also update the `openGraph.title` and `twitter.title` to use the same bare value (or compose `${title} | ORKI` explicitly for OG/Twitter — those are NOT subject to title.template). Per Next.js 15 behavior: `title.template` ONLY affects the `<title>` element / `metadata.title`; `openGraph.title` is independent. Use:
       ```tsx
       const title = product.name[locale]
       const fullTitle = `${title} | ORKI`
       // ...
       return {
         title,                                // bare → root template appends '| ORKI'
         description,
         openGraph: { title: fullTitle, ... }, // explicit full title for OG
         twitter:   { title: fullTitle, ... },
         // ALSO add alternates block (currently missing — RESEARCH.md Pattern 4 + PATTERNS.md):
         alternates: {
           canonical: `https://orki.sa/${locale}/shop/${product.category}/${product.slug}`,
           languages: {
             en:          `https://orki.sa/en/shop/${product.category}/${product.slug}`,
             ar:          `https://orki.sa/ar/shop/${product.category}/${product.slug}`,
             'x-default': `https://orki.sa/en/shop/${product.category}/${product.slug}`,
           },
         },
       }
       ```
       Keep the existing dynamic-OG image (product hero) — do NOT route PDP through `buildMetadata`.

    3. **Write `.planning/phases/09-performance-legal-polish/notes/wave-0-checks.md`** documenting:
       - vitest.config.ts exists (verified).
       - PDP `generateMetadata` updated to return bare title; OG/Twitter use explicit `${title} | ORKI`. PDP now has `alternates.languages`.
       - Returns-policy reconciliation: locked at 14 days. Cart-cookie max-age (30 days) explicitly NOT a returns-policy concern — kept as is.
       - Any [AR-LEGAL-REVIEW] flags so far in `messages/ar.json` (legal section bodies will receive these in Plan 09-02).
       - The reconciliation between RESEARCH-recommended `app/global-error.tsx` and UI-SPEC's `app/error.tsx`: planner LOCKED `app/global-error.tsx` (per RESEARCH Pattern 6). Plan 09-05 implements that file.
       - **Phase 9 wave manifest (post-revision):** Wave 1 = 09-01; Wave 2 = 09-02 + 09-03 + 09-06 (parallel); Wave 3 = 09-04 (depends on 09-01 + 09-03); Wave 4 = 09-05 (depends on 09-01 + 09-04).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const pdp=fs.readFileSync('src/app/[locale]/shop/[category]/[slug]/page.tsx','utf8'); const note=fs.readFileSync('.planning/phases/09-performance-legal-polish/notes/wave-0-checks.md','utf8'); const ok = /alternates:/.test(pdp) && /'x-default'/.test(pdp) && /const title = product\.name\[locale\]/.test(pdp) && !/const title = `\$\{product\.name\[locale\]\} \| ORKI`/.test(pdp) && /global-error/.test(note) && /14 days/.test(note) && /Wave 4/.test(note); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` exists at repo root: `test -f vitest.config.ts && echo OK` returns OK.
    - PDP `generateMetadata` no longer hand-builds `${product.name[locale]} | ORKI` — it returns bare title.
    - PDP `generateMetadata` includes `alternates.languages` with `en`, `ar`, `x-default` keys.
    - PDP `openGraph.title` and `twitter.title` use the explicit `${name} | ORKI` form (these aren't subject to title.template).
    - Notes file exists at `.planning/phases/09-performance-legal-polish/notes/wave-0-checks.md` and contains the four bullet points above PLUS the post-revision wave manifest (Wave 1 / Wave 2 / Wave 3 / Wave 4 listed explicitly).
  </acceptance_criteria>
  <done>
    PDP is migrated to bare-title pattern, has hreflang, sanity-check note documents the four Wave 0 reconciliations and the post-revision Phase 9 wave manifest.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /legal/* | Public, unauthenticated reads of localized legal content; no PII intake on these surfaces |
| Browser → Footer href | Internal links; locale prefix is automatic via `@/i18n/navigation` |
| File system → messages/*.json | Static, build-time only; no user input flows here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Information Disclosure | `messages/en.json` / `ar.json` legal section bodies | mitigate | All bodies are scaffolded with `[DRAFT]` placeholders in this plan; no real PII / contact emails / addresses are baked in until Plan 09-02 fills copy. Plan 09-02 must use a generic controller email (e.g. `privacy@orki.sa`) — never developer personal addresses. |
| T-09-01-02 | Tampering | i18n JSON bundle | accept | Static at build time; Vercel CDN serves immutable assets; no runtime mutation path. Low severity. |
| T-09-01-03 | Stored XSS | Legal copy strings | accept (LOW) | Strings are static JSON loaded at build time; next-intl renders them through React's text-node escaping. No user input merges into these strings. To mitigate aggressively, Plan 09-02 must NOT use `t.rich()` with user-supplied components — only static React fragments for inline links. |
| T-09-01-04 | Spoofing | Footer Cookie/Privacy/Terms hrefs | mitigate | All three hrefs use `@/i18n/navigation` `<Link>` (auto-locale-prefixed) — prevents accidental `/privacy` (legacy) leak. Verify via grep: 0 occurrences of bare `href="/privacy"` or `href="/terms"`. |
| T-09-01-05 | Information Disclosure | PDP `generateMetadata` `title` change | accept | Bare-title return is required for `title.template` correctness; bare title is product name (`product.name[locale]`) which is already public. No new disclosure surface. |

**HIGH severity threats:** None blocking. T-09-01-01 mitigated by enforcement in Plan 09-02 acceptance criteria.
</threat_model>

<verification>
After all tasks:
1. `npm test` passes (zero unit tests yet — passes via `--passWithNoTests`).
2. `npx tsc --noEmit` exits 0 (no new TS errors from added files).
3. `node -e "require('./messages/en.json'); require('./messages/ar.json')"` exits 0 (both bundles parse).
4. `npm run dev` boots; manual visit `/en` and `/ar` shows the footer with three Legal links resolving to `/[locale]/legal/{privacy,terms,cookies}` (which 404 in this plan — Plan 09-02 implements the pages).
5. `notes/wave-0-checks.md` exists and is committed.
</verification>

<success_criteria>
- `src/lib/seo.ts` exports `buildMetadata` returning a Metadata object with `alternates.languages.{en,ar,'x-default'}`.
- `messages/en.json` and `messages/ar.json` contain Legal / Errors / Meta / Footer.legal / CookieBanner namespaces with all section keys; AR mirrors EN 1:1; both parse as valid JSON.
- `Shop.categories.{tops,bottoms}.{title,description}` keys exist in BOTH locales (Issue #3 fix; consumed by Plan 09-04 Task 4).
- Footer renders 3 legal links via `<Link href="/legal/...">` with i18n-keyed labels; old `/privacy` and `/terms` hrefs are gone.
- Footer benefit-bar block has an explicit `TODO(i18n) benefit-bar` comment flagging the four EN-only lines as deferred i18n work (Issue #1 fix).
- Returns-policy duration is "14 days" / "14-Day" / "إرجاع خلال 14 يوم" everywhere except the cart-cookie max-age constant.
- `src/app/[locale]/legal/layout.tsx` exists as a pass-through wrapper.
- PDP `generateMetadata` returns bare title and has `alternates.languages`; no double-suffix risk when Plan 09-03 sets `title.template`.
- Wave 0 reconciliation note documents the four locked decisions AND the post-revision Phase 9 wave manifest.
</success_criteria>

<output>
After completion, create `.planning/phases/09-performance-legal-polish/09-01-SUMMARY.md` summarizing files created/modified, reconciliations locked (returns-policy 14 days; `global-error.tsx` over `error.tsx`; PDP bare-title), benefit-bar i18n TODO marker, the new `Shop.categories` i18n keys, and any flagged AR copy.
</output>
