---
phase: 09-performance-legal-polish
plan: 08
type: execute
wave: 2
gap_closure: true
depends_on: []
files_modified:
  - src/components/footer/Footer.tsx
  - src/app/[locale]/checkout/confirmation/page.tsx
  - src/lib/seo.ts
  - src/lib/legal/last-updated.ts
  - src/app/[locale]/legal/privacy/page.tsx
  - src/app/[locale]/legal/terms/page.tsx
  - src/app/[locale]/legal/cookies/page.tsx
  - src/app/[locale]/legal/layout.tsx
  - .planning/phases/09-performance-legal-polish/notes/wave-0-checks.md
  - scripts/check-legal-placeholders.cjs
  - scripts/smoke-routes.cjs
  - package.json
  - .planning/phases/09-performance-legal-polish/launch-checklist.md
  - .planning/phases/09-performance-legal-polish/deferred-items.md
  - .planning/phases/09-performance-legal-polish/09-HUMAN-UAT.md
autonomous: true
requirements:
  - LGL-01
  - LGL-02
  - LGL-03
  - LGL-04
  - PERF-04
  - PERF-05
  - PERF-06
  - SEO-02
  - SEO-03
user_setup: []
must_haves:
  truths:
    - "Footer benefit-bar shipping line reads `Free shipping from 300 SAR` in EN (matches Phase 7 spec; `$149` retired)"
    - "Confirmation page no longer carries the redundant `lookupFailed` boolean — infra failure renders fallback inline; `notFound()` afterward"
    - "`buildMetadata` uses a single `getTranslations({ locale })` call (no namespace split + no double `as never` casts)"
    - "Legal `lastUpdated` date is centralized in `src/lib/legal/last-updated.ts` and formatted per-locale (Western numerals via `'ar-SA-u-nu-latn'`); the three legal pages call the shared formatter"
    - "`src/app/[locale]/legal/layout.tsx` is deleted; legal pages still render unchanged"
    - "`scripts/check-legal-placeholders.cjs` exits 1 when any `[AR-LEGAL-REVIEW]` or `[USER-CONFIRM` substring exists in `messages/{en,ar}.json`; wired as `npm run check:legal-placeholders`"
    - "`scripts/smoke-routes.cjs` boots `next dev` with stub env, smoke-curls 5 routes, asserts 200 + `<title>… | ORKI</title>` cascade, exits 0; wired as `npm run smoke:routes`"
    - "Pre-launch placeholder cleanup is tracked in `launch-checklist.md` as an explicit owned item"
    - "`09-HUMAN-UAT.md` items 3, 4, 5 are annotated `now runnable locally via npm run smoke:routes`"
    - "`notes/wave-0-checks.md` records 'IN-05 deletion approved by gap-closure plan 09-08' so the 09-01 must_have #5 audit trail stays consistent"
  artifacts:
    - path: "src/components/footer/Footer.tsx"
      provides: "Footer with corrected SAR shipping threshold and TODO note for full benefit-bar i18n migration"
      contains: "Free shipping from 300 SAR"
    - path: "src/app/[locale]/checkout/confirmation/page.tsx"
      provides: "Confirmation page without dead lookupFailed flag"
    - path: "src/lib/seo.ts"
      provides: "buildMetadata with single getTranslations call; same public signature"
    - path: "src/lib/legal/last-updated.ts"
      provides: "LEGAL_LAST_UPDATED constant + formatLegalLastUpdated(locale) helper"
      contains: "new Date(2026, 4, 10)"
    - path: "scripts/check-legal-placeholders.cjs"
      provides: "CI guard that fails on [AR-LEGAL-REVIEW] / [USER-CONFIRM in messages/{en,ar}.json"
      contains: "AR-LEGAL-REVIEW"
    - path: "scripts/smoke-routes.cjs"
      provides: "Local smoke-runner that boots next dev with stub env and curls 5 routes"
      contains: "STORAGE_URL"
    - path: ".planning/phases/09-performance-legal-polish/launch-checklist.md"
      provides: "Pre-launch tracking for [AR-LEGAL-REVIEW] / [USER-CONFIRM] cleanup + smoke-routes UAT helper"
  key_links:
    - from: "src/app/[locale]/legal/{privacy,terms,cookies}/page.tsx"
      to: "src/lib/legal/last-updated.ts"
      via: "imported helper"
      pattern: "from '@/lib/legal/last-updated'"
    - from: "package.json"
      to: "scripts/check-legal-placeholders.cjs"
      via: "scripts.check:legal-placeholders"
      pattern: "check:legal-placeholders"
    - from: "package.json"
      to: "scripts/smoke-routes.cjs"
      via: "scripts.smoke:routes"
      pattern: "smoke:routes"
---

<objective>
Phase 9 gap-closure (advisory review triage) — wave 2: hygiene, copy, dev-loop polish.

Closes the remaining advisory findings: WR-04 (narrow — Footer SAR currency only; full i18n migration explicitly deferred), WR-05 (confirmation page redundant flag), WR-06 (`seo.ts` dotted-key split refactor), IN-01 + IN-02 + a CI guard wiring (placeholder tracking), IN-04 (`lastUpdated` centralization + per-locale formatting), IN-05 (`legal/layout.tsx` deletion + `wave-0-checks.md` audit-trail addendum), and a UAT smoke-routes helper that makes 09-HUMAN-UAT items 3, 4, 5 runnable locally without modifying the fragile `src/lib/env.ts` build-time validation.

This plan does NOT depend on Plan 09-07 — files_modified do not overlap. It is `wave: 2` only because Plan 09-07 is `wave: 1`; running them sequentially is operationally easier (no merge conflicts on `src/app/[locale]/shop/[category]/[slug]/page.tsx`'s history) but the dependency graph is empty.

Purpose: leave Phase 9 with zero advisory backlog and a clean local-UAT loop.
Output: 9 modified files; 4 new files; 2 new npm scripts.
</objective>

<execution_context>
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-performance-legal-polish/09-CONTEXT.md
@.planning/phases/09-performance-legal-polish/09-RESEARCH.md
@.planning/phases/09-performance-legal-polish/09-PATTERNS.md
@.planning/phases/09-performance-legal-polish/09-UI-SPEC.md
@.planning/phases/09-performance-legal-polish/09-VALIDATION.md
@.planning/phases/09-performance-legal-polish/09-VERIFICATION.md
@.planning/phases/09-performance-legal-polish/09-REVIEW.md
@.planning/phases/09-performance-legal-polish/09-HUMAN-UAT.md
@.planning/phases/09-performance-legal-polish/deferred-items.md
@CLAUDE.md

<interfaces>
<!-- Existing patterns the executor MUST mirror, not invent. -->

Existing scripts pattern (CommonJS .cjs and .ts both present in `scripts/`):
- `scripts/seed.ts`, `scripts/generate-og-default.ts` (TS via tsx)
- New scripts in this plan use **`.cjs`** because they invoke `child_process.spawn` against `next dev` and run BEFORE/around the dev-server boot — keeping them in plain Node CommonJS avoids the tsx-load step. Rationale: `npm run smoke:routes` must work in a fresh checkout without a tsx warm-up.

Existing build-time env validation (untouched by this plan, per scope guidance):
```ts
// src/lib/env.ts:11-28
const envSchema = z.object({
  STORAGE_URL: z.string().url().optional(),
  DATABASE_URL: z.string().url().optional(),
  NODE_ENV: z.enum(['development','production','test']).default('development'),
}).refine((data) => Boolean(data.DB_URL), { message: 'A PostgreSQL connection URL is required.' });
```
Stub strategy: `STORAGE_URL=stub://example` passes `z.string().url()` because `stub:` is a valid URL scheme. The zod parser accepts it; runtime DB calls would fail, but `sitemap.ts`/`robots.ts`/static pages do NOT hit the DB during a smoke (sitemap reads `getAllProducts()` which DOES hit Drizzle — so we restrict the smoke to static routes only that do NOT depend on Drizzle: `/sitemap.xml` requires the DB so it is OUT of the smoke set; `/robots.txt`, `/en/about`, `/en/contact`, `/en/shop/tops` are listed below — note `/en/shop/tops` IS DB-backed via `getAllProducts()` so it would also hit the DB).

**Revised smoke set (DB-free only):** `/robots.txt`, `/en/about`, `/en/contact`, `/en/legal/privacy`, `/en/legal/terms`. These five routes pull translations + buildMetadata only and do NOT call any Drizzle helper. Sitemap and shop pages REMAIN in 09-HUMAN-UAT.md as preview-deploy-only (annotated explicitly so the human-UAT file stays accurate).

Existing `seo.ts` shape (current — uses dotted-key split + `as never`):
```ts
const [titleNs, ...titleRest] = titleKey.split('.');
const titleSubKey = titleRest.join('.');
const tTitle = await getTranslations({ locale, namespace: titleNs as never });
// ...repeat for description...
const title = tTitle(titleSubKey as never);
```

Existing legal page lastUpdated literal (3x identical):
```tsx
// src/app/[locale]/legal/{privacy,terms,cookies}/page.tsx
<LegalArticle ... lastUpdated="10 May 2026" locale={locale}>
```

Existing legal layout (target of IN-05 deletion):
```tsx
// src/app/[locale]/legal/layout.tsx
export default async function LegalLayout({ children }: Props) {
  return <>{children}</>;
}
```

Existing confirmation page (target of WR-05 simplification):
```tsx
// src/app/[locale]/checkout/confirmation/page.tsx:28-49
let order: Order | null = null;
let lookupFailed = false;
try { order = await getOrderByReference(ref); }
catch (err) { console.error(...); lookupFailed = true; }
if (lookupFailed) { /* render fallback */ return ...; }
if (!order) notFound();
```

Existing Footer benefit-bar shipping line (target of WR-04 narrow fix):
```tsx
// src/components/footer/Footer.tsx:19
<span ...>Free shipping from $149</span>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: WR-04 (narrow) — Footer benefit-bar SAR currency fix; defer full i18n migration</name>
  <files>src/components/footer/Footer.tsx, .planning/phases/09-performance-legal-polish/deferred-items.md</files>
  <read_first>
    - C:/dev/Antigravity/ORKI/src/components/footer/Footer.tsx (full file — line 19 shipping; lines 11-14 existing TODO(i18n) comment)
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-REVIEW.md (WR-04 lines 156-165)
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/deferred-items.md (existing deferred-items pattern)
  </read_first>
  <action>
    1. **Edit `src/components/footer/Footer.tsx` line 19.**

       FROM:
       ```tsx
       <span className="text-sm font-medium tracking-tight text-white/80">Free shipping from $149</span>
       ```
       TO:
       ```tsx
       <span className="text-sm font-medium tracking-tight text-white/80">Free shipping from 300 SAR</span>
       ```

       Per Phase 7 cart spec, the free-shipping threshold is 300 SAR; the existing `$149` was wrong-currency for KSA and a launch defect regardless of i18n status.

       Do NOT touch lines 23, 27, 31 (the other three benefit-bar lines). Do NOT migrate the Footer / About / ContactClient hardcoded EN/AR ternaries to next-intl in this plan — that is OUT OF SCOPE per gap-closure scope guidance. The existing TODO(i18n) comment at lines 11-14 already flags the deferral; do NOT modify that comment.

    2. **Append to `.planning/phases/09-performance-legal-polish/deferred-items.md`** the following section (preserve existing content above):

       ```markdown

       ## Full benefit-bar / About / Contact next-intl migration (deferred from gap closure)

       **Source:** 09-REVIEW.md WR-04
       **Surfaced:** 2026-05-10 (Plan 09-08)
       **Files affected:**
       - `src/components/footer/Footer.tsx:23,27,31,55-58,76-77` (benefit bar + nav links — three of four benefit-bar lines remain hardcoded EN; nav links remain hardcoded EN)
       - `src/app/[locale]/about/page.tsx:30-86` (entire page is `isRtl ? 'AR' : 'EN'` ternaries)
       - `src/app/[locale]/contact/ContactClient.tsx:30-95` (form labels, button copy, success state)

       **What 09-08 did:** Fixed only the SAR-vs-USD currency defect on the shipping line (`$149` → `300 SAR`). All other ternary strings remain EN/AR ternaries.

       **Recommended owner:** A standalone follow-up plan (post-Phase 9 polish or a Phase-10+ i18n hygiene pass) that:
       - Adds `Footer.benefitBar.{shipping,returns,payments,support}` keys to `messages/{en,ar}.json`.
       - Adds `Footer.nav.{home,tops,bottoms,ourStory}` keys.
       - Migrates `about/page.tsx` body copy to a `Pages.about.*` namespace.
       - Migrates `ContactClient.tsx` strings to a `Pages.contact.*` namespace.
       - Drops the `TODO(i18n) benefit-bar` comment at `Footer.tsx:11-14` once migration is complete.

       Out of scope for Phase 9; not gated by any Phase 9 must_have or roadmap success criterion.
       ```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const f=fs.readFileSync('src/components/footer/Footer.tsx','utf8'); const d=fs.readFileSync('.planning/phases/09-performance-legal-polish/deferred-items.md','utf8'); const ok=/Free shipping from 300 SAR/.test(f) && !/\\\$149/.test(f) && /Full benefit-bar.*next-intl migration/.test(d) && /WR-04/.test(d); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Free shipping from 300 SAR" src/components/footer/Footer.tsx` returns 1 line.
    - `grep -nE "\\\$149" src/components/footer/Footer.tsx` returns 0 lines.
    - `grep -n "WR-04" .planning/phases/09-performance-legal-polish/deferred-items.md` returns ≥ 1 line.
    - `grep -n "Full benefit-bar" .planning/phases/09-performance-legal-polish/deferred-items.md` returns 1 line.
    - The existing `TODO(i18n) benefit-bar` comment at `Footer.tsx:11-14` is UNCHANGED: `grep -c "TODO(i18n)" src/components/footer/Footer.tsx` ≥ 1.
  </acceptance_criteria>
  <done>
    Footer ships the correct KSA shipping threshold (300 SAR); the larger i18n migration is explicitly tracked in `deferred-items.md` with a recommended owner.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: WR-05 — Remove redundant `lookupFailed` flag from confirmation page</name>
  <files>src/app/[locale]/checkout/confirmation/page.tsx</files>
  <read_first>
    - C:/dev/Antigravity/ORKI/src/app/[locale]/checkout/confirmation/page.tsx (lines 28-51 — current pattern)
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-REVIEW.md (WR-05 lines 167-184)
    - C:/dev/Antigravity/ORKI/src/lib/orders/server.ts (verify: `getOrderByReference` returns `Order | null`, throws only on infra error)
  </read_first>
  <action>
    Edit `src/app/[locale]/checkout/confirmation/page.tsx`.

    Replace lines 25-51 (the `let order ... let lookupFailed ... try/catch ... if (lookupFailed) ... if (!order) notFound()` block) with the following — the catch handler renders the fallback INLINE and returns; `notFound()` runs only on the success-but-null branch.

    FROM (lines 25-51):
    ```tsx
      // PERF-05: separate "DB unavailable" (try/catch fallback) from "order
      // doesn't exist" (still notFound()). A Supabase blip should NOT 404 a user
      // whose payment we already accepted — render reassurance copy instead.
      let order: Order | null = null;
      let lookupFailed = false;
      try {
        order = await getOrderByReference(ref);
      } catch (err) {
        console.error('[checkout/confirmation] getOrderByReference failed', err);
        lookupFailed = true;
      }

      if (lookupFailed) {
        const tErr = await getTranslations('Errors.section');
        return (
          <div
            role="alert"
            className="min-h-[60vh] flex items-center justify-center px-6"
          >
            <p className="text-lg text-white/60 max-w-md text-center">
              {tErr('orderLoadFailed')}
            </p>
          </div>
        );
      }

      if (!order) notFound();
    ```
    TO:
    ```tsx
      // PERF-05: separate "DB unavailable" (try/catch fallback) from "order
      // doesn't exist" (still notFound()). A Supabase blip should NOT 404 a user
      // whose payment we already accepted — render reassurance copy instead.
      let order: Order | null;
      try {
        order = await getOrderByReference(ref);
      } catch (err) {
        console.error('[checkout/confirmation] getOrderByReference failed', err);
        const tErr = await getTranslations('Errors.section');
        return (
          <div
            role="alert"
            className="min-h-[60vh] flex items-center justify-center px-6"
          >
            <p className="text-lg text-white/60 max-w-md text-center">
              {tErr('orderLoadFailed')}
            </p>
          </div>
        );
      }

      if (!order) notFound();
    ```

    Behavior is identical for current `getOrderByReference` semantics (returns `Order | null`, throws on infra). The `lookupFailed` boolean and the post-catch `if (lookupFailed)` branch are gone; the inline render-and-return inside the catch handler is the only fallback path.

    Do NOT touch any other part of the page (imports, the `await params` block, the success render, the styling).
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('src/app/[locale]/checkout/confirmation/page.tsx','utf8'); const ok=!/lookupFailed/.test(c) && /catch \(err\)/.test(c) && /tErr\('orderLoadFailed'\)/.test(c) && /if \(!order\) notFound\(\)/.test(c); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "lookupFailed" src/app/[locale]/checkout/confirmation/page.tsx` returns 0 lines.
    - `grep -nE "catch \\(err\\)" "src/app/[locale]/checkout/confirmation/page.tsx"` returns 1 line.
    - `grep -n "tErr('orderLoadFailed')" src/app/[locale]/checkout/confirmation/page.tsx` returns 1 line (still inside the catch).
    - `grep -nE "if \\(!order\\) notFound\\(\\)" "src/app/[locale]/checkout/confirmation/page.tsx"` returns 1 line.
    - File type-checks (Task 9 batch).
  </acceptance_criteria>
  <done>
    Dead `lookupFailed` boolean removed; identical user-visible behavior; future `getOrderByReference` semantic shifts are isolated to the catch handler.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: WR-06 — Refactor `seo.ts` to single getTranslations call</name>
  <files>src/lib/seo.ts</files>
  <read_first>
    - C:/dev/Antigravity/ORKI/src/lib/seo.ts (full file — current dual-getTranslations pattern at lines 32-50)
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-REVIEW.md (WR-06 lines 186-196)
    - C:/dev/Antigravity/ORKI/src/app/[locale]/{about,contact}/page.tsx (callers — verify they pass `titleKey: 'Meta.about.title'` etc., i.e. dotted keys)
    - C:/dev/Antigravity/ORKI/src/app/[locale]/legal/{privacy,terms,cookies}/page.tsx (callers)
  </read_first>
  <action>
    Replace the body of `buildMetadata` in `src/lib/seo.ts`. **Preserve the public signature exactly** (`buildMetadata({ path, locale, titleKey, descriptionKey, ogImage? })`) so all 09-01..06 callers continue to compile.

    Replace lines 25-83 (the entire `export async function buildMetadata` definition) with:

    ```ts
    export async function buildMetadata({
      path,
      locale,
      titleKey,
      descriptionKey,
      ogImage,
    }: BuildMetadataInput): Promise<Metadata> {
      // Single translator scoped to the locale (no namespace) — `t('Meta.about.title')`
      // works because next-intl resolves dotted paths against the root messages tree.
      // Drops the dual getTranslations call + redundant per-namespace splits.
      const t = await getTranslations({ locale });
      const title = t(titleKey as never);
      const description = t(descriptionKey as never);
      const image = ogImage ?? DEFAULT_OG_IMAGE;

      const suffix = path === '/' ? '' : path;
      const canonical = `${HOST}/${locale}${suffix}`;

      return {
        title, // bare; root layout sets title.template = '%s | ORKI'
        description,
        alternates: {
          canonical,
          languages: {
            en: `${HOST}/en${suffix}`,
            ar: `${HOST}/ar${suffix}`,
            'x-default': `${HOST}/en${suffix}`,
          },
        },
        openGraph: {
          title,
          description,
          url: canonical,
          siteName: 'ORKI',
          locale: locale === 'ar' ? 'ar_SA' : 'en_US',
          type: 'website',
          images: [{ url: image, width: 1200, height: 630, alt: 'ORKI' }],
        },
        twitter: {
          card: 'summary_large_image',
          title,
          description,
          images: [image],
        },
      };
    }
    ```

    Keep:
    - The `'server-only'` import (line 1) UNCHANGED.
    - The `import type { Metadata }` (line 2) UNCHANGED.
    - The `import { getTranslations } from 'next-intl/server'` (line 3) UNCHANGED.
    - The `import type { Locale } from '@/types/domain'` (line 4) UNCHANGED.
    - The `HOST` and `DEFAULT_OG_IMAGE` constants (lines 6-7) UNCHANGED.
    - The `BuildMetadataInput` interface (lines 9-23) UNCHANGED.

    The single `as never` cast on each translator call is acceptable — it is the documented next-intl idiom for accepting dynamic dotted keys at runtime; the previous double-cast (`namespace: titleNs as never` AND `tTitle(titleSubKey as never)`) was the redundant pattern.

    All Phase 9 callers (`Meta.about.title`, `Legal.privacy.metaTitle`, `Shop.categories.tops.title`, etc.) pass dotted-key strings that resolve correctly when `t` is scoped to the root messages object — verified via the next-intl docs (Context7 if needed, but the documented behavior is stable across `next-intl@4.x`).
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('src/lib/seo.ts','utf8'); const getTransCount=(c.match(/await getTranslations/g)||[]).length; const ok=getTransCount===1 && /t\(titleKey as never\)/.test(c) && /t\(descriptionKey as never\)/.test(c) && !/titleNs/.test(c) && !/descNs/.test(c) && /'x-default'/.test(c) && /buildMetadata/.test(c); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "await getTranslations" src/lib/seo.ts` = 1 (single call).
    - `grep -n "titleNs" src/lib/seo.ts` returns 0 lines.
    - `grep -n "descNs" src/lib/seo.ts` returns 0 lines.
    - `grep -n "t(titleKey as never)" src/lib/seo.ts` returns 1 line.
    - `grep -n "t(descriptionKey as never)" src/lib/seo.ts` returns 1 line.
    - Public signature preserved: `grep -nE "export async function buildMetadata\\(\\{" src/lib/seo.ts` returns 1 line and the destructured fields are still `path, locale, titleKey, descriptionKey, ogImage`.
    - `npx tsc --noEmit` exits 0 (Task 9 batch — verifies all callers still compile).
    - `'x-default'` still present in alternates.languages.
  </acceptance_criteria>
  <done>
    `buildMetadata` runs one `getTranslations` per call instead of two; signature preserved; all Phase 9 callers continue to compile and resolve their dotted keys.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: IN-04 — Centralize legal `lastUpdated` constant + per-locale formatter</name>
  <files>src/lib/legal/last-updated.ts, src/app/[locale]/legal/privacy/page.tsx, src/app/[locale]/legal/terms/page.tsx, src/app/[locale]/legal/cookies/page.tsx</files>
  <read_first>
    - C:/dev/Antigravity/ORKI/src/app/[locale]/legal/privacy/page.tsx (line 46 — `lastUpdated="10 May 2026"`)
    - C:/dev/Antigravity/ORKI/src/app/[locale]/legal/terms/page.tsx (line 46 — same literal)
    - C:/dev/Antigravity/ORKI/src/app/[locale]/legal/cookies/page.tsx (line 39 — same literal)
    - C:/dev/Antigravity/ORKI/CLAUDE.md ("Currency Western numerals via `'ar-SA-u-nu-latn'`" — same locale-modifier convention applies to dates)
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-REVIEW.md (IN-04 lines 219-224)
  </read_first>
  <action>
    1. **Create `src/lib/legal/last-updated.ts`** with this exact content:

       ```ts
       import type { Locale } from '@/types/domain';

       /**
        * Single source of truth for the legal-policy "last updated" date.
        * Update this constant when any of /legal/{privacy,terms,cookies} body copy changes.
        * Per CLAUDE.md, AR locale uses Western numerals via the 'ar-SA-u-nu-latn' modifier
        * to keep the rendered date consistent with site-wide currency formatting.
        */
       export const LEGAL_LAST_UPDATED = new Date(2026, 4, 10); // 10 May 2026 (month is 0-indexed)

       const FORMATTERS: Record<Locale, Intl.DateTimeFormat> = {
         en: new Intl.DateTimeFormat('en-GB', { dateStyle: 'long' }),
         ar: new Intl.DateTimeFormat('ar-SA-u-nu-latn', { dateStyle: 'long' }),
       };

       export function formatLegalLastUpdated(locale: Locale): string {
         return FORMATTERS[locale].format(LEGAL_LAST_UPDATED);
       }
       ```

       Notes:
       - `en-GB` (`10 May 2026`) is closer to the existing literal than `en-US` (`May 10, 2026`); preserves the visual layout user has already approved.
       - `ar-SA-u-nu-latn` produces e.g. `10 مايو 2026` — Arabic month name + Western numerals (per CLAUDE.md numerals rule).
       - Month index is `4` (May) because JS Dates are 0-indexed.

    2. **Edit `src/app/[locale]/legal/privacy/page.tsx`**:
       - Add import after the existing imports (after line 5):
         ```tsx
         import { formatLegalLastUpdated } from '@/lib/legal/last-updated';
         ```
       - Inside `PrivacyPage`, before the `return (`, add:
         ```tsx
         const lastUpdated = formatLegalLastUpdated(locale);
         ```
       - Change the `LegalArticle` prop from `lastUpdated="10 May 2026"` to `lastUpdated={lastUpdated}`.

    3. **Edit `src/app/[locale]/legal/terms/page.tsx`** — apply the same three changes (import, in-function const, prop swap).

    4. **Edit `src/app/[locale]/legal/cookies/page.tsx`** — apply the same three changes.

    Do NOT change `LegalArticle` itself — the prop type stays `string`, the formatter just provides the value.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const lib=fs.readFileSync('src/lib/legal/last-updated.ts','utf8'); const p=fs.readFileSync('src/app/[locale]/legal/privacy/page.tsx','utf8'); const t=fs.readFileSync('src/app/[locale]/legal/terms/page.tsx','utf8'); const k=fs.readFileSync('src/app/[locale]/legal/cookies/page.tsx','utf8'); const ok=/LEGAL_LAST_UPDATED = new Date\(2026, 4, 10\)/.test(lib) && /ar-SA-u-nu-latn/.test(lib) && /en-GB/.test(lib) && /formatLegalLastUpdated/.test(lib) && /formatLegalLastUpdated/.test(p) && /formatLegalLastUpdated/.test(t) && /formatLegalLastUpdated/.test(k) && !/lastUpdated=\"10 May 2026\"/.test(p) && !/lastUpdated=\"10 May 2026\"/.test(t) && !/lastUpdated=\"10 May 2026\"/.test(k); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/legal/last-updated.ts` exists.
    - `grep -n "LEGAL_LAST_UPDATED = new Date(2026, 4, 10)" src/lib/legal/last-updated.ts` returns 1 line.
    - `grep -n "ar-SA-u-nu-latn" src/lib/legal/last-updated.ts` returns 1 line.
    - `grep -n "en-GB" src/lib/legal/last-updated.ts` returns 1 line.
    - `grep -nE "lastUpdated=\"10 May 2026\"" src/app/[locale]/legal` (recursive) returns 0 lines.
    - All three legal pages contain `formatLegalLastUpdated`: `grep -lc "formatLegalLastUpdated" src/app/[locale]/legal/privacy/page.tsx src/app/[locale]/legal/terms/page.tsx src/app/[locale]/legal/cookies/page.tsx` shows each file matched.
    - `npx tsc --noEmit` exits 0 (Task 9 batch).
    - Live render produces `10 May 2026` (EN) and `10 مايو 2026` (AR) — verifiable in the smoke run (Task 7) when those routes are added to the smoke set, otherwise verifiable via 09-HUMAN-UAT preview deploy.
  </acceptance_criteria>
  <done>
    Single source of truth for the date constant; per-locale formatting honors CLAUDE.md numerals rule; three legal pages no longer drift.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: IN-05 — Delete `legal/layout.tsx` + record audit-trail addendum</name>
  <files>src/app/[locale]/legal/layout.tsx, .planning/phases/09-performance-legal-polish/notes/wave-0-checks.md</files>
  <read_first>
    - C:/dev/Antigravity/ORKI/src/app/[locale]/legal/layout.tsx (full file — 12 lines, verifies it is truly a no-op pass-through)
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/notes/wave-0-checks.md (full file — establishes the existing audit-trail pattern; we are appending an addendum after section 6)
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-VERIFICATION.md (lines 67-69 — must_have #5 evidence references this file)
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-REVIEW.md (IN-05 lines 226-231)
  </read_first>
  <action>
    1. **Delete `src/app/[locale]/legal/layout.tsx`.** Use the platform-appropriate command:
       - Windows: `del "src\\app\\[locale]\\legal\\layout.tsx"` (PowerShell) or `rm "src/app/[locale]/legal/layout.tsx"` (Git Bash).
       - The file is 12 lines, exports a default async `LegalLayout` returning `<>{children}</>`. Next.js does not require a layout file per segment; deleting it is a no-op.

    2. **Append to `.planning/phases/09-performance-legal-polish/notes/wave-0-checks.md`** a new section after section 6 (the wave manifest). Add:

       ```markdown

       ---

       ## Addendum: 09-08 IN-05 deletion approval

       **Date:** {ISO date when 09-08 runs}
       **Plan:** 09-08 (gap-closure wave 2)
       **Source:** `09-REVIEW.md` IN-05 (`LegalLayout` is a no-op pass-through — could be deleted)

       Plan 09-01 must_have #5 originally read:
       > "Legal route folder layout exists and renders children unmodified"

       This was a Wave-0 unblocker for Plan 09-02 (legal pages). Once 09-02 shipped its
       three pages and the build proved Next.js does not require a per-segment layout,
       the file became dead code (verified by IN-05 review finding).

       **Approved deletion:** `src/app/[locale]/legal/layout.tsx`. Plan 09-02's three pages
       continue to render via the parent `[locale]/layout.tsx`. No regression to 09-VERIFICATION.md
       must_have #5 because the original gating concern (the legal route folder being
       routable) was a Wave-0 unblocker, not a runtime requirement once the pages exist.

       **Verification recorded by Plan 09-08:**
       - `test ! -f src/app/[locale]/legal/layout.tsx` exits 0.
       - `npm run smoke:routes` (Plan 09-08 Task 7) renders `/en/legal/privacy` with HTTP 200.
       ```

    Replace `{ISO date when 09-08 runs}` with the actual ISO date during execution.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const layoutGone=!fs.existsSync('src/app/[locale]/legal/layout.tsx'); const note=fs.readFileSync('.planning/phases/09-performance-legal-polish/notes/wave-0-checks.md','utf8'); const ok=layoutGone && /09-08 IN-05 deletion approval/.test(note) && /Plan 09-01 must_have #5/.test(note); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/app/[locale]/legal/layout.tsx` exits 0 (file gone).
    - `grep -n "09-08 IN-05 deletion approval" .planning/phases/09-performance-legal-polish/notes/wave-0-checks.md` returns 1 line.
    - `grep -n "must_have #5" .planning/phases/09-performance-legal-polish/notes/wave-0-checks.md` returns ≥ 1 line (audit-trail addendum present).
    - `npm run dev` (or any build smoke from Task 7) successfully renders `/en/legal/privacy` returning HTTP 200.
    - `npx tsc --noEmit` still exits 0 (no other file imported the deleted layout — verified).
  </acceptance_criteria>
  <done>
    Dead `legal/layout.tsx` removed; the 09-01 must_have #5 audit trail is updated in `wave-0-checks.md` with the approval rationale and the verification recorded.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: IN-01 + IN-02 — CI guard for legal placeholders + launch checklist</name>
  <files>scripts/check-legal-placeholders.cjs, package.json, .planning/phases/09-performance-legal-polish/launch-checklist.md</files>
  <read_first>
    - C:/dev/Antigravity/ORKI/package.json (current scripts block — pattern to mirror)
    - C:/dev/Antigravity/ORKI/messages/ar.json (39 occurrences of placeholder substrings — verifies the guard fires today)
    - C:/dev/Antigravity/ORKI/messages/en.json (7 occurrences of placeholder substrings)
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-REVIEW.md (IN-01 lines 200-205, IN-02 lines 207-212)
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/deferred-items.md (existing format)
  </read_first>
  <action>
    1. **Create `scripts/check-legal-placeholders.cjs`** with this exact content:

       ```js
       #!/usr/bin/env node
       /**
        * CI guard: fail the build if any [AR-LEGAL-REVIEW] or [USER-CONFIRM placeholder
        * remains in messages/{en,ar}.json. Wired as `npm run check:legal-placeholders`.
        * Pre-launch the team must strip all placeholders; the guard ensures none ship.
        */
       'use strict';
       const fs = require('node:fs');
       const path = require('node:path');

       const FILES = ['messages/en.json', 'messages/ar.json'];
       const PATTERN = /\[AR-LEGAL-REVIEW\]|\[USER-CONFIRM/;

       let bad = false;
       for (const rel of FILES) {
         const abs = path.resolve(process.cwd(), rel);
         const text = fs.readFileSync(abs, 'utf8');
         const lines = text.split('\n');
         for (let i = 0; i < lines.length; i++) {
           if (PATTERN.test(lines[i])) {
             console.error(`${rel}:${i + 1}: ${lines[i].trim()}`);
             bad = true;
           }
         }
       }

       if (bad) {
         console.error('\n[check:legal-placeholders] FAIL — strip [AR-LEGAL-REVIEW] / [USER-CONFIRM before launch.');
         process.exit(1);
       }
       console.log('[check:legal-placeholders] OK — no placeholders found.');
       ```

    2. **Edit `package.json`** to add the new npm script. Insert into the `"scripts"` object (preserve existing keys, valid JSON, two-space indent):

       Add this entry after the existing `"audit"` entry:
       ```json
           "check:legal-placeholders": "node scripts/check-legal-placeholders.cjs",
       ```

       The final scripts block should preserve every existing entry (`dev`, `build`, `vercel-build`, `start`, `lint`, `db:push`, `db:generate`, `db:migrate`, `db:studio`, `db:seed`, `audit`, `test`, `test:watch`) and add `check:legal-placeholders` plus (in Task 7) `smoke:routes`.

    3. **Create `.planning/phases/09-performance-legal-polish/launch-checklist.md`** with this content:

       ```markdown
       # Phase 9 Launch Checklist (pre-public-deploy)

       Owned blockers that MUST be resolved before flipping the public-deploy switch.
       Created by Plan 09-08 (gap closure) on 2026-05-10.

       ## Blocking — copy

       - [ ] **Strip `[AR-LEGAL-REVIEW]` placeholders** from `messages/ar.json` (30 occurrences as of 2026-05-10).
         - Verification: `npm run check:legal-placeholders` exits 0.
         - Owner: native AR legal reviewer + project lead.
         - Source: 09-REVIEW.md IN-01.

       - [ ] **Resolve `[USER-CONFIRM ...]` placeholders** in `messages/en.json` and `messages/ar.json` (collectively 16 occurrences as of 2026-05-10).
         - These mark missing real values: commercial registration number, controller postal address, retention periods, etc.
         - Verification: `npm run check:legal-placeholders` exits 0 (same script catches both pattern families).
         - Owner: project lead.
         - Source: 09-REVIEW.md IN-02.

       ## Blocking — UAT (post-deploy)

       The eight items in `09-HUMAN-UAT.md` remain pending. Five of them (items 3, 4, 5)
       are now runnable locally via `npm run smoke:routes` (added by Plan 09-08 Task 7).
       Items 1, 2, 6, 7, 8 still require a Vercel preview deploy.

       ## Suggested CI wiring (post-launch)

       Add `npm run check:legal-placeholders` to the `vercel-build` script (or a GitHub
       Actions pre-merge job) so the placeholder guard runs on every deploy. Not done in
       this plan because it would block the current preview deploy that still has
       placeholders by design — flip the switch only when the blocking items above are
       cleared.
       ```

    4. **Verify the guard fires correctly TODAY** (placeholders are still present; guard MUST exit 1):
       - During execution, run `npm run check:legal-placeholders` and confirm exit code 1 + at least 30 lines printed to stderr.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('scripts/check-legal-placeholders.cjs','utf8'); const pkg=JSON.parse(fs.readFileSync('package.json','utf8')); const cl=fs.readFileSync('.planning/phases/09-performance-legal-polish/launch-checklist.md','utf8'); const ok=/AR-LEGAL-REVIEW/.test(s) && /USER-CONFIRM/.test(s) && /process\.exit\(1\)/.test(s) && pkg.scripts['check:legal-placeholders']==='node scripts/check-legal-placeholders.cjs' && /Strip \\\[AR-LEGAL-REVIEW\\\]/.test(cl) && /USER-CONFIRM/.test(cl) && /09-HUMAN-UAT/.test(cl); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/check-legal-placeholders.cjs` exists and contains the regex `\[AR-LEGAL-REVIEW\]|\[USER-CONFIRM` literal.
    - `package.json` has `"check:legal-placeholders": "node scripts/check-legal-placeholders.cjs"` in `scripts`.
    - All previously existing `package.json` scripts are preserved: `node -e "const p=require('./package.json'); const required=['dev','build','vercel-build','start','lint','db:push','db:generate','db:migrate','db:studio','db:seed','audit','test','test:watch','check:legal-placeholders']; process.exit(required.every(k=>p.scripts[k])?0:1)"` exits 0.
    - Running `npm run check:legal-placeholders` against the CURRENT `messages/{en,ar}.json` exits 1 (placeholders are present and the guard fires correctly today).
    - File `.planning/phases/09-performance-legal-polish/launch-checklist.md` exists and contains both `[AR-LEGAL-REVIEW]` and `[USER-CONFIRM` references.
    - The launch checklist references `npm run check:legal-placeholders` AND `npm run smoke:routes` (added in Task 7).
  </acceptance_criteria>
  <done>
    CI guard exists, exits 1 today (proves it works), is wired into the npm script registry. Pre-launch placeholder cleanup is tracked as an explicit owned item with verification command and owner.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 7: UAT smoke-routes helper (DB-free routes only)</name>
  <files>scripts/smoke-routes.cjs, package.json, .planning/phases/09-performance-legal-polish/09-HUMAN-UAT.md</files>
  <read_first>
    - C:/dev/Antigravity/ORKI/src/lib/env.ts (full file — verify the zod schema accepts `stub://` URLs and required fields are STORAGE_URL or DATABASE_URL only)
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-HUMAN-UAT.md (full file — items 3, 4, 5 are the title-cascade / robots / sitemap-style smoke tests)
    - C:/dev/Antigravity/ORKI/src/app/[locale]/about/page.tsx (verify it does NOT import any Drizzle helper — confirms about/contact/legal pages are DB-free)
    - C:/dev/Antigravity/ORKI/src/app/[locale]/legal/privacy/page.tsx (verify it only imports `getTranslations` and `buildMetadata` — DB-free)
    - C:/dev/Antigravity/ORKI/src/app/robots.ts (verify it does not call any DB helper — confirmed in 09-VERIFICATION.md robots evidence row)
  </read_first>
  <action>
    1. **Create `scripts/smoke-routes.cjs`** with this exact content:

       ```js
       #!/usr/bin/env node
       /**
        * Local smoke-runner for Phase 9 routes that do NOT require database access.
        * Boots `next dev` with stub env so src/lib/env.ts validation passes (STORAGE_URL
        * is a real URL, satisfying z.string().url(); runtime DB calls are not made on
        * any of the smoked routes).
        *
        * Smokes 5 DB-free routes: /robots.txt, /en/about, /en/contact,
        * /en/legal/privacy, /en/legal/terms. Each must return HTTP 200; the legal/about/
        * contact pages must emit a `<title>... | ORKI</title>` cascade (or for /robots.txt,
        * return text/plain content with at least one Disallow line).
        *
        * Sitemap and shop routes are NOT smoked here because they hit Drizzle. They remain
        * in 09-HUMAN-UAT.md under preview-deploy-only verification.
        *
        * Wired as `npm run smoke:routes`. Out-of-scope to modify src/lib/env.ts.
        */
       'use strict';
       const { spawn } = require('node:child_process');
       const http = require('node:http');

       const PORT = 3030; // avoid colliding with 3000 if dev is already running
       const ROUTES = [
         { path: '/robots.txt',         expect: { status: 200, contains: 'Disallow:' } },
         { path: '/en/about',           expect: { status: 200, contains: '| ORKI</title>' } },
         { path: '/en/contact',         expect: { status: 200, contains: '| ORKI</title>' } },
         { path: '/en/legal/privacy',   expect: { status: 200, contains: '| ORKI</title>' } },
         { path: '/en/legal/terms',     expect: { status: 200, contains: '| ORKI</title>' } },
       ];

       function fetchOnce(path) {
         return new Promise((resolve, reject) => {
           const req = http.get(
             { host: '127.0.0.1', port: PORT, path, headers: { 'user-agent': 'smoke-routes/1.0' } },
             (res) => {
               let body = '';
               res.on('data', (c) => (body += c));
               res.on('end', () => resolve({ status: res.statusCode, body }));
             }
           );
           req.on('error', reject);
           req.setTimeout(15000, () => req.destroy(new Error('request timeout')));
         });
       }

       async function waitForReady(deadlineMs) {
         const start = Date.now();
         while (Date.now() - start < deadlineMs) {
           try {
             const r = await fetchOnce('/robots.txt');
             if (r.status === 200) return;
           } catch (_) { /* not up yet */ }
           await new Promise((r) => setTimeout(r, 500));
         }
         throw new Error(`next dev did not become ready on :${PORT} within ${deadlineMs}ms`);
       }

       async function main() {
         const child = spawn('npx', ['next', 'dev', '-p', String(PORT)], {
           stdio: ['ignore', 'inherit', 'inherit'],
           env: {
             ...process.env,
             STORAGE_URL: 'stub://smoke-runner.local/db',
             DATABASE_URL: 'stub://smoke-runner.local/db',
             NODE_ENV: 'development',
           },
           shell: process.platform === 'win32',
         });

         let exitCode = 0;
         try {
           await waitForReady(60000);
           for (const route of ROUTES) {
             const { status, body } = await fetchOnce(route.path);
             const ok = status === route.expect.status && body.includes(route.expect.contains);
             const tag = ok ? 'PASS' : 'FAIL';
             console.log(`[smoke:routes] ${tag} ${route.path} status=${status} contains=${route.expect.contains}`);
             if (!ok) exitCode = 1;
           }
         } catch (err) {
           console.error('[smoke:routes] FAIL', err && err.message);
           exitCode = 1;
         } finally {
           child.kill('SIGTERM');
           // give next dev up to 5s to drain
           await new Promise((r) => setTimeout(r, 1500));
         }
         process.exit(exitCode);
       }

       main();
       ```

    2. **Edit `package.json`** to add the npm script. Insert after `"check:legal-placeholders"` (added in Task 6):
       ```json
           "smoke:routes": "node scripts/smoke-routes.cjs",
       ```

    3. **Edit `.planning/phases/09-performance-legal-polish/09-HUMAN-UAT.md`** — annotate items 3, 4, 5. Replace the body of each item to keep the original `expected:` line and add a new `local:` line.

       Items currently look like:
       ```markdown
       ### 3. sitemap.xml returns 200 with ≥ 18 `<url>` entries (live)
       expected: `curl -s http://localhost:3000/sitemap.xml | grep -c '<url>'` ≥ 18
       result: [pending]
       ```

       Update items 3, 4, 5 to:

       ```markdown
       ### 3. sitemap.xml returns 200 with ≥ 18 `<url>` entries (live)
       expected: `curl -s http://localhost:3000/sitemap.xml | grep -c '<url>'` ≥ 18
       local: NOT covered by `npm run smoke:routes` — sitemap.ts hits Drizzle (`getAllProducts()`); requires Vercel preview deploy with real DB. Smoke runner uses stub:// DB URLs.
       result: [pending]

       ### 4. robots.txt returns 200 with locale-prefixed Disallow rules (live)
       expected: curl -s /robots.txt shows Disallow: /en/admin/, /ar/admin/, /en/checkout/, /ar/checkout/, /api/
       local: now runnable locally via `npm run smoke:routes` (covers /robots.txt = 200 + contains "Disallow:"). For full Disallow-line audit, run preview deploy.
       result: [pending]

       ### 5. Per-page `<title>` cascade renders correctly via title.template (live)
       expected: curl /en/about → `<title>About | ORKI</title>`; /en/contact → `<title>Contact | ORKI</title>`; /en/shop/tops → `<title>Tops | ORKI</title>`; PDP → `<product name> | ORKI` (single suffix only)
       local: now runnable locally via `npm run smoke:routes` for /en/about, /en/contact, /en/legal/privacy, /en/legal/terms (each must contain `| ORKI</title>`). Shop categories + PDP are DB-backed; preview deploy required for those.
       result: [pending]
       ```

       Do NOT change items 1, 2, 6, 7, 8. Do NOT change the frontmatter, summary, or gaps sections.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('scripts/smoke-routes.cjs','utf8'); const pkg=JSON.parse(fs.readFileSync('package.json','utf8')); const u=fs.readFileSync('.planning/phases/09-performance-legal-polish/09-HUMAN-UAT.md','utf8'); const ok=/STORAGE_URL/.test(s) && /next', 'dev/.test(s) && /\/robots\.txt/.test(s) && /\/en\/legal\/privacy/.test(s) && pkg.scripts['smoke:routes']==='node scripts/smoke-routes.cjs' && /now runnable locally via `npm run smoke:routes`/.test(u) && /Sitemap and shop routes are NOT smoked|sitemap\.ts hits Drizzle/.test(u); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/smoke-routes.cjs` exists and references all five smoke routes (`/robots.txt`, `/en/about`, `/en/contact`, `/en/legal/privacy`, `/en/legal/terms`).
    - File spawns `next dev` with `STORAGE_URL=stub://...` set in `env` (stub URL valid against `z.string().url()`).
    - File does NOT import or modify `src/lib/env.ts` — out of scope per gap-closure guidance.
    - `package.json` has `"smoke:routes": "node scripts/smoke-routes.cjs"`.
    - Running `npm run smoke:routes` exits 0 with all 5 routes returning 200 and the expected `contains` substring (executed during this task; capture exit code in Task 9 batch).
    - `09-HUMAN-UAT.md` items 3, 4, 5 each contain a `local:` line that mentions `npm run smoke:routes` (item 3 says NOT covered; items 4 and 5 say covered).
    - Items 1, 2, 6, 7, 8 in `09-HUMAN-UAT.md` are UNCHANGED: `grep -c "result: \[pending\]" .planning/phases/09-performance-legal-polish/09-HUMAN-UAT.md` ≥ 8.
    - Smoke set excludes sitemap and shop routes (DB-backed) — confirmed by absence of `/sitemap.xml`, `/en/shop/`, `/ar/shop/` in `scripts/smoke-routes.cjs`.
  </acceptance_criteria>
  <done>
    Local-UAT loop is runnable for the five DB-free smoke routes; 09-HUMAN-UAT.md is annotated to reflect the new local capability; src/lib/env.ts remains untouched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 8: Update `package.json` once and verify combined script set</name>
  <files>package.json</files>
  <read_first>
    - C:/dev/Antigravity/ORKI/package.json (current state — Tasks 6 and 7 each described inserting one entry; this task is the consolidation gate)
  </read_first>
  <action>
    Tasks 6 and 7 each described inserting a single entry into `package.json` `scripts`. If both edits were applied separately, this task is a no-op verification gate. If both edits collide on the same line, resolve here so the final `scripts` block reads (preserve every existing entry; the only ADDITIONS are the last two):

    ```json
      "scripts": {
        "dev": "next dev --turbopack",
        "build": "next build",
        "vercel-build": "next build",
        "start": "next start",
        "lint": "eslint",
        "db:push": "drizzle-kit push",
        "db:generate": "drizzle-kit generate",
        "db:migrate": "drizzle-kit migrate",
        "db:studio": "drizzle-kit studio",
        "db:seed": "tsx scripts/seed.ts",
        "audit": "npm audit --audit-level=moderate",
        "test": "vitest run --passWithNoTests",
        "test:watch": "vitest",
        "check:legal-placeholders": "node scripts/check-legal-placeholders.cjs",
        "smoke:routes": "node scripts/smoke-routes.cjs"
      },
    ```

    Do NOT touch any other section of `package.json` (`dependencies`, `devDependencies`, `name`, `version`, `private`).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const required=['dev','build','vercel-build','start','lint','db:push','db:generate','db:migrate','db:studio','db:seed','audit','test','test:watch','check:legal-placeholders','smoke:routes']; const present=required.every(k=>typeof p.scripts[k]==='string'); const exact=p.scripts['check:legal-placeholders']==='node scripts/check-legal-placeholders.cjs' && p.scripts['smoke:routes']==='node scripts/smoke-routes.cjs'; const noExtraKeys=Object.keys(p.scripts).length===15; process.exit(present && exact && noExtraKeys ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` parses as JSON.
    - `package.json` `scripts` has exactly 15 keys (13 original + 2 new): `dev, build, vercel-build, start, lint, db:push, db:generate, db:migrate, db:studio, db:seed, audit, test, test:watch, check:legal-placeholders, smoke:routes`.
    - The two new entries map to `node scripts/check-legal-placeholders.cjs` and `node scripts/smoke-routes.cjs` exactly.
    - No `dependencies` or `devDependencies` entries were added or removed.
  </acceptance_criteria>
  <done>
    `package.json` `scripts` block contains exactly the 15 expected entries; no collateral damage.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 9: Phase 9 gap-closure verification batch (09-08)</name>
  <files>.planning/phases/09-performance-legal-polish/notes/gap-closure-09-08-checks.md</files>
  <read_first>
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/notes/wave-0-checks.md (existing notes pattern)
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-VERIFICATION.md (28/28 must-haves baseline)
  </read_first>
  <action>
    Run the verification batch and write the result to `.planning/phases/09-performance-legal-polish/notes/gap-closure-09-08-checks.md`. The note must record:

    1. Per-task `<verify>` script exit codes (Tasks 1-8 above).
    2. Whole-plan checks:
       - `npx tsc --noEmit` exit code (must be 0; document any deferred Phase-8/test errors).
       - `node -e "require('./messages/en.json'); require('./messages/ar.json')"` exit code.
       - `npm test` exit code.
       - `npm run check:legal-placeholders` exit code (MUST be 1 today — proves the guard fires; this is a meta-test of the script itself).
       - `npm run smoke:routes` exit code (MUST be 0 — all 5 routes return 200 with title cascade or Disallow line).
    3. Reconciliation with 09-VERIFICATION.md:
       - 09-01 must_have #5 was originally "Legal route folder layout exists and renders children unmodified" — Plan 09-08 deletes this layout. Document why this is a deliberate authorized regression (audit-trail addendum in `wave-0-checks.md` per Task 5).
       - 09-01 must_have #1 (`buildMetadata`) — UNCHANGED public signature; refactor is internal-only.
       - 09-04 must_have (legal-page lastUpdated rendering) — display string remains `10 May 2026` (EN) or `10 مايو 2026` (AR) — visually equivalent to the literal that was hard-coded.
    4. Six closed findings: WR-04 (narrow), WR-05, WR-06, IN-01+IN-02, IN-04, IN-05, plus the UAT smoke-routes helper that supports HUMAN-UAT items 3, 4, 5.

    Exact file template (fill `[result]` placeholders during execution):
    ```markdown
    # Phase 9 Gap Closure — Plan 09-08 verification batch

    **Date:** {ISO date}
    **Plan:** 09-08 (hygiene + copy + dev-loop polish)
    **Closes:** WR-04 (narrow), WR-05, WR-06, IN-01, IN-02, IN-04, IN-05; ships UAT smoke-routes helper

    ## Per-task automated verify

    | Task | Finding/Item | Verify exit | Notes |
    |---|---|---|---|
    | 1 | WR-04 narrow Footer SAR | [exit] | $149 → 300 SAR; remainder deferred in deferred-items.md |
    | 2 | WR-05 confirmation page | [exit] | lookupFailed flag removed; behavior identical |
    | 3 | WR-06 seo.ts refactor | [exit] | Single getTranslations; signature unchanged |
    | 4 | IN-04 lastUpdated centralized | [exit] | New lib file + 3 page edits + AR formatter |
    | 5 | IN-05 layout deletion | [exit] | File gone; wave-0-checks.md addendum present |
    | 6 | IN-01+IN-02 placeholder guard | [exit] | Script runs and exits 1 today (proof it works) |
    | 7 | UAT smoke-routes | [exit] | 5 DB-free routes; src/lib/env.ts untouched |
    | 8 | package.json scripts | [exit] | 15 keys total; 2 added |

    ## Whole-plan checks

    - `npx tsc --noEmit`: [exit code]
    - `node -e "require('./messages/ar.json'); require('./messages/en.json')"`: [exit code]
    - `npm test`: [exit code]
    - `npm run check:legal-placeholders`: [exit code MUST be 1] — proves guard works
    - `npm run smoke:routes`: [exit code MUST be 0] — local UAT loop functional

    ## Reconciliation with 09-VERIFICATION.md

    Two must-haves require explicit reconciliation:

    1. **09-01 must_have #5** ("Legal route folder layout exists and renders children unmodified")
       — INTENTIONALLY regressed by Plan 09-08 Task 5. The original gating concern was
       Wave-0 unblocker for Plan 09-02 (legal pages). Once those pages shipped and the
       build proved Next.js does not require a per-segment layout, the file became dead
       code (IN-05). Audit-trail addendum recorded in
       `.planning/phases/09-performance-legal-polish/notes/wave-0-checks.md` (section
       "Addendum: 09-08 IN-05 deletion approval"). The smoke-runner (Task 7) verifies
       `/en/legal/privacy` returns 200 without the layout.

    2. **09-01 must_have #1** (`buildMetadata` exists with alternates) — public signature
       UNCHANGED by Task 3. All Phase 9 callers compile against the same destructure shape
       (`{ path, locale, titleKey, descriptionKey, ogImage }`). The refactor is internal:
       single `getTranslations({ locale })` call instead of two namespace-scoped calls.

    All other 28 must-haves verified in 09-VERIFICATION.md remain unaffected.
    ```
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('.planning/phases/09-performance-legal-polish/notes/gap-closure-09-08-checks.md','utf8'); const ok=/WR-04/.test(c) && /WR-05/.test(c) && /WR-06/.test(c) && /IN-01/.test(c) && /IN-04/.test(c) && /IN-05/.test(c) && /must_have #5/.test(c) && /Addendum: 09-08 IN-05 deletion approval/.test(c) && /smoke:routes/.test(c) && /check:legal-placeholders/.test(c); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/09-performance-legal-polish/notes/gap-closure-09-08-checks.md` exists.
    - All seven closed findings (WR-04 narrow, WR-05, WR-06, IN-01, IN-02, IN-04, IN-05) appear in the per-task table.
    - Per-task table records 8 tasks (Tasks 1-8 from this plan).
    - Whole-plan checks section explicitly states `npm run check:legal-placeholders` MUST exit 1 (the guard catches placeholders that still exist) and `npm run smoke:routes` MUST exit 0.
    - Reconciliation section explicitly addresses 09-01 must_have #5 (IN-05 deletion) AND 09-01 must_have #1 (signature preservation).
    - The reconciliation cites the `wave-0-checks.md` addendum by name.
  </acceptance_criteria>
  <done>
    Audit-trail note committed; all seven advisory findings closed with verification commands recorded; UAT loop helper documented; the only authorized must_have regression (IN-05) is reconciled with explicit cross-references.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build pipeline → `npm run check:legal-placeholders` | Gates artifact promotion on placeholder absence; exit-1 fails the pipeline |
| Local dev → `npm run smoke:routes` | Boots `next dev` on port 3030 with stub DB URLs; bound to 127.0.0.1 |
| Browser → confirmation page | Behavior unchanged; only internal control flow simplified |
| Browser → legal page lastUpdated | Date string is build-time deterministic; no user input flows here |
| Browser → Footer benefit-bar shipping line | Static EN string; matches Phase 7 cart spec value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08-01 | Information Disclosure | `[USER-CONFIRM ...]` placeholders rendered to public | mitigate | Task 6 — `check:legal-placeholders` script + launch-checklist owner forces removal before public deploy. Pre-deploy guard, not runtime. |
| T-09-08-02 | Tampering | `LEGAL_LAST_UPDATED` constant | accept | Single source of truth at build time; updating it is a code change reviewed in PR. No attack surface. |
| T-09-08-03 | DoS | smoke-routes binds local port 3030 | accept | Loopback-only (127.0.0.1); not exposed; collisions are local annoyances, not attacks. |
| T-09-08-04 | Information Disclosure | Stub DB URL `stub://smoke-runner.local/db` | accept | Stub does not reach any real network endpoint; zod accepts the syntactically valid URL but Drizzle would fail to connect. Smoke set is DB-free; no DB query is issued. |
| T-09-08-05 | Spoofing | smoke-routes content assertions (`contains`) | mitigate | Title-cascade check (`| ORKI</title>`) is specific enough that an attacker would have to compromise the dev server itself; no realistic spoofing vector for a local smoke. |

**HIGH severity threats:** None. All findings are hygiene / correctness / dev-experience improvements.
</threat_model>

<verification>
After all tasks:
1. `npx tsc --noEmit` exits 0 (or only emits errors already enumerated in `deferred-items.md`).
2. `node -e "require('./messages/en.json'); require('./messages/ar.json')"` exits 0.
3. `npm test` exits 0.
4. `npm run check:legal-placeholders` exits 1 (guard correctly detects current placeholders — this is the proof-of-life test).
5. `npm run smoke:routes` exits 0 (all 5 DB-free routes return 200 with the expected substring).
6. `test ! -f src/app/[locale]/legal/layout.tsx` exits 0.
7. `grep -c "Free shipping from 300 SAR" src/components/footer/Footer.tsx` returns 1.
8. `grep -c "lookupFailed" src/app/[locale]/checkout/confirmation/page.tsx` returns 0.
9. `grep -c "await getTranslations" src/lib/seo.ts` returns 1.
10. `grep -nE "lastUpdated=\"10 May 2026\"" src/app/[locale]/legal` (recursive) returns 0 lines.
11. `package.json` `scripts` has exactly 15 keys.
12. `09-HUMAN-UAT.md` items 3, 4, 5 each have a `local:` annotation; items 1, 2, 6, 7, 8 unchanged.
</verification>

<reconciliation_with_existing_phase>
This plan deliberately regresses ONE 09-VERIFICATION.md must_have and preserves all others. Specific reconciliations:

1. **09-01 must_have #5** ("Legal route folder layout exists and renders children unmodified")
   — INTENTIONALLY regressed by Task 5 (IN-05 deletion). The original gating concern (Wave-0 unblocker for Plan 09-02) is no longer applicable: Plan 09-02 shipped its three pages; Next.js does not require a per-segment layout for routes to resolve; the file is dead code. The audit-trail addendum in `wave-0-checks.md` (added by Task 5) records the approval rationale. Verification: Task 7 smoke-runner confirms `/en/legal/privacy` still returns 200 with the title cascade.

2. **09-01 must_have #1** (`buildMetadata` exists and composes title/description/canonical/hreflang/OG) — PRESERVED. Task 3 refactors the implementation but keeps the public signature exactly: same destructured input fields, same `Promise<Metadata>` return shape, same `alternates.languages.{en,ar,'x-default'}` block, same OG/Twitter blocks. All 09-01..06 callers continue to compile.

3. **09-01 must_have #3** (Returns-policy duration is 14 days everywhere) — UNTOUCHED. Task 1 changes the SHIPPING threshold (300 SAR), not the returns policy.

4. **09-01 must_have #6** (Title-template double-application sanity check documented) — UNTOUCHED. Task 5 ADDS to `wave-0-checks.md`; it does not remove the existing sections 1-6.

5. **09-02 must_haves** (legal pages render with all sections) — UNTOUCHED structurally. Task 4 changes only how the `lastUpdated` prop value is computed (now `formatLegalLastUpdated(locale)` returning `'10 May 2026'` for EN and `'10 مايو 2026'` for AR — visually equivalent to the previous EN literal in EN, and a corrected AR-localized string in AR which the previous hardcoded `"10 May 2026"` did not provide).

6. **09-04 must_haves** (sitemap, robots, OG, per-page metadata wiring) — UNTOUCHED.

7. **09-05 must_haves** (error boundaries, reliability hygiene) — UNTOUCHED. Task 2 simplifies `confirmation/page.tsx` internal control flow; the user-facing fallback render is bit-identical (same role="alert" container, same `tErr('orderLoadFailed')` text, same notFound() on null-order branch).

8. **09-06 must_haves** (CookieBanner scaffold) — UNTOUCHED.

9. **09-VERIFICATION human_verification items** (8 items) — items 3, 4, 5 are now annotated as `now runnable locally via npm run smoke:routes` (Task 7); items 1, 2, 6, 7, 8 remain preview-deploy-only and untouched.

The single authorized regression (09-01 must_have #5) is documented with rationale, replacement verification, and a written addendum to the audit trail.

Phase requirements claimed in this plan's frontmatter (LGL-01..04, PERF-04..06, SEO-02, SEO-03) are claimed for additional verification depth on top of 09-01..06's existing claims; no NEW requirement coverage is introduced.
</reconciliation_with_existing_phase>

<success_criteria>
- All 7 advisory findings (WR-04 narrow, WR-05, WR-06, IN-01, IN-02, IN-04, IN-05) closed with grep-/exit-code-verifiable acceptance.
- `npm run check:legal-placeholders` is wired and exits 1 today (guard works).
- `npm run smoke:routes` is wired and exits 0 (5 DB-free routes return 200 with title cascade or Disallow line).
- `src/lib/env.ts` is UNCHANGED — confirmed by absence of any edit instruction touching that file.
- `09-HUMAN-UAT.md` items 3, 4, 5 are annotated; items 1, 2, 6, 7, 8 are untouched.
- `launch-checklist.md` exists and tracks the placeholder cleanup as an explicit owned item with verification command and owner.
- The single authorized regression (09-01 must_have #5 / IN-05 deletion) is reconciled with an audit-trail addendum in `wave-0-checks.md`.
- Full benefit-bar / About / Contact i18n migration is explicitly deferred in `deferred-items.md` with a recommended owner.
</success_criteria>

<output>
After completion, create `.planning/phases/09-performance-legal-polish/09-08-SUMMARY.md` summarizing each closed finding (one row per WR-/IN-), the new files (`scripts/check-legal-placeholders.cjs`, `scripts/smoke-routes.cjs`, `src/lib/legal/last-updated.ts`, `launch-checklist.md`), the deleted file (`src/app/[locale]/legal/layout.tsx`), and an explicit "single authorized must_have regression (09-01 #5)" reconciliation pointing back at `notes/wave-0-checks.md` (Addendum) and `notes/gap-closure-09-08-checks.md`.
</output>
