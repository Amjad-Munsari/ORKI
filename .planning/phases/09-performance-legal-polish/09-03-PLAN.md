---
phase: 09-performance-legal-polish
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - package.json
  - package-lock.json
  - src/app/[locale]/layout.tsx
  - src/components/shop/ProductGrid.tsx
  - src/components/pdp/PDPGallery.tsx
autonomous: true
requirements:
  - PERF-03
  - PERF-04
user_setup:
  - service: vercel
    why: "Surfacing real-user CWV (LCP/CLS/INP) and page-view data for the live site"
    env_vars: []
    dashboard_config:
      - task: "After deploy, visit Vercel project → Analytics tab → confirm page-view events arriving"
        location: "https://vercel.com/<team>/<project>/analytics"
      - task: "After 24h of traffic, visit Speed Insights tab → confirm p75 LCP < 2500ms"
        location: "https://vercel.com/<team>/<project>/speed-insights"
must_haves:
  truths:
    - "Vercel Analytics page-view events fire on navigation in production"
    - "Vercel Speed Insights collects LCP / CLS / INP from real users in production"
    - "First 3-4 catalog grid cards have priority loading; rest are lazy"
    - "PDP hero image has priority loading"
    - "title.template '%s | ORKI' is set in [locale]/layout.tsx so per-page generateMetadata returns bare titles"
  artifacts:
    - path: "package.json"
      provides: "@vercel/analytics + @vercel/speed-insights as runtime deps"
      contains: "@vercel/analytics"
    - path: "src/app/[locale]/layout.tsx"
      provides: "Mounted <Analytics /> + <SpeedInsights /> after children, inside <body>; title.template set"
      contains: "<Analytics"
  key_links:
    - from: "src/app/[locale]/layout.tsx"
      to: "Vercel edge analytics + speed-insights endpoints"
      via: "@vercel/analytics/next + @vercel/speed-insights/next"
      pattern: "@vercel/(analytics|speed-insights)/next"
    - from: "src/components/shop/ProductGrid.tsx"
      to: "next/image priority hint"
      via: "priority={i < 4}"
      pattern: "priority=\\{i\\s*<\\s*4\\}"
---

<objective>
Mount Vercel Analytics + Speed Insights in the locale layout (cookieless RUM, satisfies PERF-03 and PERF-04 measurement); set `title.template` on `[locale]/layout.tsx` (locking D-04); audit/confirm `next/image priority` strategy on shop grid (`i < 4`) and PDP hero — both already implemented, verify still in place after changes.

Purpose: PERF-03 (CWV measurement live), PERF-04 (page-load via lazy/priority strategy), D-04 (title.template).
Output: 2 npm dependencies installed, 1 file edited (locale layout — analytics mount + title.template), 2 verification grep checks on existing components (ProductGrid, PDPGallery).

**Wave note (Issue #8 fix):** This plan was previously Wave 2 alongside 09-04. To eliminate the race where Plan 09-04's per-page `generateMetadata` smoke tests could observe partial state if 09-03's title.template hadn't landed, Plan 09-04 has been bumped to Wave 3 and now `depends_on: ['09-01', '09-03']`. This plan stays in Wave 2 (parallel with 09-02 and 09-06).
</objective>

<execution_context>
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-performance-legal-polish/09-CONTEXT.md
@.planning/phases/09-performance-legal-polish/09-RESEARCH.md
@.planning/phases/09-performance-legal-polish/09-PATTERNS.md
@.planning/phases/09-performance-legal-polish/09-VALIDATION.md
@CLAUDE.md
@src/app/[locale]/layout.tsx
@src/components/shop/ProductGrid.tsx
@src/components/pdp/PDPGallery.tsx

<interfaces>
<!-- @vercel/analytics/next + @vercel/speed-insights/next exports — copied from RESEARCH.md Pattern 1. -->

```ts
// from '@vercel/analytics/next'
export const Analytics: (props?: { debug?: boolean; mode?: 'auto'|'production'|'development' }) => JSX.Element;

// from '@vercel/speed-insights/next'
export const SpeedInsights: (props?: { debug?: boolean }) => JSX.Element;
```

Both internally apply `'use client'`. They are valid children of a Server Component layout. Mount AFTER `{children}`, INSIDE `<body>`. Do NOT add `'use client'` to the layout.

`src/app/[locale]/layout.tsx` current shape (PATTERNS.md §"Cross-cutting Edits — src/app/[locale]/layout.tsx"):
```tsx
return (
  <html lang={locale} dir={dir} className={`dark ${geist.variable} ${ibmPlexArabic.variable}`}>
    <body className="bg-black text-white antialiased min-h-screen flex flex-col">
      <NextIntlClientProvider locale={locale} messages={messages}>
        <StoreHydration />
        <Navbar />
        <CartDrawer locale={locale as Locale} />
        <PageTransition><main className="flex-1 pt-20">{children}</main></PageTransition>
        <Footer />
      </NextIntlClientProvider>
      {/* INSERT <Analytics /> + <SpeedInsights /> here, after </NextIntlClientProvider>, still inside <body> */}
    </body>
  </html>
);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install @vercel/analytics + @vercel/speed-insights and mount in locale layout (with title.template)</name>
  <files>package.json, package-lock.json, src/app/[locale]/layout.tsx</files>
  <read_first>
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-RESEARCH.md (§"Standard Stack" lines 122-158 — versions 2.0.1 + 2.0.0; §"Pattern 1: Vercel Analytics + Speed Insights mounting" lines 263-306)
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-PATTERNS.md (§"src/app/[locale]/layout.tsx (EDIT)" lines 388-417 — exact insertion points)
    - C:/dev/Antigravity/ORKI/src/app/[locale]/layout.tsx (full current file — to find the metadata export and the `</body>` close)
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-RESEARCH.md (§"Common Pitfalls — Pitfall 1" lines 738-743 — DO NOT also mount in `global-error.tsx`)
  </read_first>
  <action>
    1. Install the two cookieless RUM packages (versions per RESEARCH.md):
       ```bash
       npm install @vercel/analytics@latest @vercel/speed-insights@latest
       ```
       Both pin to their major v2 (2.0.x). Do not lock to a sub-version unless `npm view` reports breaking changes — RESEARCH.md verified them as stable 2026-05-10.

    2. Edit `src/app/[locale]/layout.tsx`:

       a. **Add imports** at the top (after existing imports, before `metadata` export — per PATTERNS.md insertion point):
       ```tsx
       import { Analytics } from '@vercel/analytics/next';
       import { SpeedInsights } from '@vercel/speed-insights/next';
       ```
       Use the `/next` subpath (App Router-aware). NOT `/react`. Per RESEARCH.md Pattern 1 line 299.

       b. **Update the `metadata` export** to use `title.template` so per-page `generateMetadata` calls return a bare title and the parent applies ` | ORKI` exactly once (per Plan 09-01 Task 6 sanity check). Replace the current `export const metadata` block with:
       ```tsx
       export const metadata: Metadata = {
         metadataBase: new URL('https://orki.sa'),
         title: {
           default:  'ORKI — Saudi Streetwear',
           template: '%s | ORKI',
         },
         description: 'Saudi streetwear. Dark. Underground.',
       };
       ```
       This locks D-04 (title template + meta description per CONTEXT §4). Plan 09-01 Task 6 already migrated PDP `generateMetadata` to return a bare title, so the cascade is consistent — Black Hoodie page will render `<title>Black Hoodie | ORKI</title>`, NOT `Black Hoodie | ORKI | ORKI`.

       c. **Mount the components** inside `<body>`, after `</NextIntlClientProvider>`, before `</body>`:
       ```tsx
              </NextIntlClientProvider>
              <Analytics />
              <SpeedInsights />
            </body>
       ```
       Do NOT add `debug` prop in production. Conditional debug is acceptable: `<Analytics debug={process.env.NODE_ENV !== 'production'} />` if dev noise becomes an issue, but default is fine.

       d. **DO NOT** add `'use client'` to the layout. The Vercel components apply it internally.
       e. **DO NOT** mount these inside `global-error.tsx` (Plan 09-05 will create that file — Pitfall 1 would cause duplicate page-view events).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const pkg=require('./package.json'); const c=fs.readFileSync('src/app/[locale]/layout.tsx','utf8'); const ok = !!pkg.dependencies['@vercel/analytics'] && !!pkg.dependencies['@vercel/speed-insights'] && /from '@vercel\/analytics\/next'/.test(c) && /from '@vercel\/speed-insights\/next'/.test(c) && /<Analytics\s*\/>/.test(c) && /<SpeedInsights\s*\/>/.test(c) && /template:\s*'%s \| ORKI'/.test(c) && !/'use client'/.test(c.split('\\n').slice(0,5).join('\\n')); process.exit(ok?0:1)"</automated>
    <automated>npm run build 2>&1 | tail -20 ; if [ "${PIPESTATUS[0]:-0}" -ne 0 ]; then echo "BUILD FAILED — peer-dep break or type error from new Vercel packages"; exit 1; fi</automated>
    <!-- PowerShell equivalent for Windows: -->
    <!-- npm run build 2>&1 | Select-Object -Last 20 ; if ($LASTEXITCODE -ne 0) { Write-Error "BUILD FAILED"; exit 1 } -->
  </verify>
  <acceptance_criteria>
    - VALIDATION row PERF-03: `grep -c '<SpeedInsights' src/app/\[locale\]/layout.tsx` = 1.
    - VALIDATION row PERF-04: `grep -c '<Analytics' src/app/\[locale\]/layout.tsx` = 1.
    - `package.json` lists `@vercel/analytics` and `@vercel/speed-insights` under dependencies.
    - `package-lock.json` is updated (commit it).
    - `src/app/[locale]/layout.tsx` contains `template: '%s | ORKI'` in the metadata export.
    - Layout file does NOT have `'use client'` at top.
    - **Issue #4 fix:** `npm run build` exits 0 (catches Next.js 15 peer-dep breaks introduced by the new `@vercel/*` packages in-task, not at the end of the wave). Required because Vercel SDKs occasionally lag a Next major release; running build in-task catches this immediately.
  </acceptance_criteria>
  <done>
    Both packages installed, locale layout mounts them once after children inside body, title.template is set, `npm run build` succeeds (peer-dep clean), type-checker is happy.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Audit + confirm next/image priority on ProductGrid + PDPGallery</name>
  <files>src/components/shop/ProductGrid.tsx, src/components/pdp/PDPGallery.tsx</files>
  <read_first>
    - C:/dev/Antigravity/ORKI/src/components/shop/ProductGrid.tsx (full file — RESEARCH.md confirms `priority={i < 4}` already at line 33)
    - C:/dev/Antigravity/ORKI/src/components/pdp/PDPGallery.tsx (full file — should have priority on first slide)
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-RESEARCH.md (§"Pattern 7: next/image priority strategy" lines 674-685; §"Pitfall 7" lines 781-787 — aspect-ratio enforcement is critical for CLS during locale switch)
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-VALIDATION.md (rows PERF-04 image-priority + PERF-04 PDP-hero — exact greps)
  </read_first>
  <action>
    Verify (and fix only if needed):

    1. **ProductGrid:** Confirm `src/components/shop/ProductGrid.tsx` contains a JSX expression `priority={i < 4}` on the `<Image>` component. The line was added in Phase 7 per RESEARCH.md line 676. If it's missing or has been refactored away, restore it. Pattern: when iterating products with `.map((product, i) => ...)`, add `priority={i < 4}` to the `<Image>` element inside the card. Do NOT add `priority` to ALL cards (RESEARCH.md anti-pattern: balloons LCP). Do NOT use viewport detection.

    2. **PDPGallery:** Confirm the first / hero image (typically `images[0]` or `currentIndex === 0`) sets `priority` (or `priority={true}` / `priority={index === 0}`). If absent, add it. The implementation detail varies based on the gallery's pagination model — open the file, find the `<Image>` element rendering the active/first slide, ensure exactly one image element has `priority` truthy on initial render.

    3. **Aspect-ratio sanity check:** Per RESEARCH.md Pitfall 7, the codebase already locks `aspectRatio: '3/4'` style on placeholder/grid images (`PlaceholderImage.tsx:43`). Do NOT remove that lock; if you see code that would override the aspect ratio (e.g. inline `height: 'auto'` without `aspect-ratio`), leave it alone — out of scope for this plan.

    4. **No new image-loading code paths.** This task is verification-and-restore-if-broken; if both files already match the spec, no edits are needed.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const grid=fs.readFileSync('src/components/shop/ProductGrid.tsx','utf8'); const gallery=fs.readFileSync('src/components/pdp/PDPGallery.tsx','utf8'); const gridOk = /priority=\{i\s*<\s*4\}/.test(grid); const galleryOk = /priority/.test(gallery); if(!gridOk) console.error('ProductGrid missing priority={i < 4}'); if(!galleryOk) console.error('PDPGallery missing priority'); process.exit((gridOk && galleryOk)?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - VALIDATION row PERF-04 image priority: `grep -E 'priority=\{i\s*<\s*4\}' src/components/shop/ProductGrid.tsx` returns ≥ 1 match.
    - VALIDATION row PERF-04 PDP hero priority: `grep -E 'priority' src/components/pdp/PDPGallery.tsx` returns ≥ 1 match.
    - Neither file has had `priority` removed nor blanket-added to ALL cards.
    - `aspectRatio` styling on grid card images remains in place (per Pitfall 7) — not removed.
  </acceptance_criteria>
  <done>
    Verified that the priority strategy from Phase 7 is intact; if anything was inadvertently removed, restored to spec.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Vercel edge analytics endpoint | Cookieless beacon (anonymous fingerprinting); no PII transit |
| Browser → Vercel edge speed-insights endpoint | Cookieless web-vitals beacon |
| Browser → next/image CDN | Public asset fetch; no auth/cookies attached |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Information Disclosure | Vercel Analytics fingerprint beacon | accept | RESEARCH.md confirms `@vercel/analytics` is cookieless and PDPL-strictly-necessary-exempt (line 301-302). Privacy Policy section 4 in Plan 09-02 explicitly documents this. |
| T-09-03-02 | Information Disclosure | Speed Insights CWV beacon | accept | Same justification — cookieless, anonymous; no PII collected. Documented in Cookie Policy section 4. |
| T-09-03-03 | Tampering | npm install of `@vercel/analytics` 2.0.1 + `@vercel/speed-insights` 2.0.0 | mitigate | Versions match RESEARCH.md verified state (2026-05-10); package-lock.json pins exact resolved versions. Run `npm audit` after install — flag any moderate+ findings to user before commit. The in-task `npm run build` (Issue #4 fix) catches peer-dep breaks against Next.js 15 immediately. |
| T-09-03-04 | Denial of Service | Analytics beacon failures cascading to page render | accept | Both Vercel components are silent-fail by design (RESEARCH.md "Email already a no-op" pattern, Pitfall 1 area). A network hiccup to vercel-edge does NOT block render. |
| T-09-03-05 | Information Disclosure | `Analytics debug={true}` left on in production | mitigate | Plan locks the prop OFF (no `debug` prop set). If a dev adds it later, code review catches it; alternatively pin to `debug={process.env.NODE_ENV !== 'production'}` if noise becomes a problem. |
| T-09-03-06 | Spoofing | Wrong title template double-application | mitigate | Plan 09-01 Task 6 migrated PDP to bare title; this plan sets template; explicit acceptance criterion verifies cascade. |

**HIGH severity threats:** None blocking.
</threat_model>

<verification>
After completion:
1. `npm run build` exits 0.
2. `npm test` exits 0 (passWithNoTests).
3. `npm audit --audit-level=moderate` exits 0 OR any new findings are documented in 09-03-SUMMARY.md.
4. `grep -c '<Analytics' src/app/[locale]/layout.tsx` = 1; `grep -c '<SpeedInsights' src/app/[locale]/layout.tsx` = 1.
5. `grep -E 'priority=\{i\s*<\s*4\}' src/components/shop/ProductGrid.tsx` matches.
6. **Manual (24h post-deploy):** Visit Vercel project → Analytics → confirm page-view events; → Speed Insights → confirm p75 LCP <2500ms after a day of traffic. Documented in user_setup frontmatter.
</verification>

<success_criteria>
- Both packages installed and locked in package-lock.json.
- Locale layout mounts `<Analytics />` and `<SpeedInsights />` exactly once each, after `</NextIntlClientProvider>`, inside `</body>`.
- Title template is `'%s | ORKI'` in the locale layout.
- ProductGrid retains `priority={i < 4}` on the first 3-4 cards.
- PDPGallery retains `priority` on the hero image.
- No `'use client'` added to the layout.
- `global-error.tsx` (created in Plan 09-05) does NOT mount these components (Pitfall 1).
- `npm run build` succeeded in-task (Issue #4 — peer-dep break would have been caught here, not at wave end).
</success_criteria>

<output>
After completion, create `.planning/phases/09-performance-legal-polish/09-03-SUMMARY.md` summarizing dependencies added, RUM mounting, the title.template lock, the in-task build verification result, and the Vercel dashboard verification steps the user must perform 24h post-deploy.
</output>
