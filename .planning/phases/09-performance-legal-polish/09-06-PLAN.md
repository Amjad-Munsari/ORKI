---
phase: 09-performance-legal-polish
plan: 06
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/lib/cookie-consent.ts
  - src/components/CookieBanner.tsx
autonomous: true
requirements:
  - LGL-02
user_setup: []
must_haves:
  truths:
    - "CookieBanner component exists, compiles, and uses next-intl 'CookieBanner' namespace"
    - "Cookie-consent helper module exposes typed read/write/has API surface"
    - "Neither file is imported into any layout — banner is built but NOT mounted (per CONTEXT §2)"
  artifacts:
    - path: "src/lib/cookie-consent.ts"
      provides: "readCookieConsent / writeCookieConsent / hasCookieConsent helpers + ConsentValue type"
      contains: "cookie_consent"
    - path: "src/components/CookieBanner.tsx"
      provides: "Future-proofing scaffold; <CookieBanner locale={...} /> compiles and would render banner if mounted"
      contains: "fixed bottom-0"
  key_links:
    - from: "src/components/CookieBanner.tsx"
      to: "src/lib/cookie-consent.ts"
      via: "import { readCookieConsent, writeCookieConsent }"
      pattern: "from '@/lib/cookie-consent'"
    - from: "src/components/CookieBanner.tsx"
      to: "messages/{en,ar}.json CookieBanner namespace"
      via: "useTranslations('CookieBanner')"
      pattern: "useTranslations\\('CookieBanner'\\)"
---

<objective>
Build the future-proofing cookie-consent scaffold per CONTEXT §2 — the banner component AND the helper module are written, type-checked, and compile, but are NEVER imported by any layout this phase. When (and if) GA4 / Meta Pixel / retargeting cookies are added in a future phase, mounting becomes a one-line `<CookieBanner locale={locale} />` insert in the locale layout.

Purpose: LGL-02 future-proofing (no banner needed today; scaffold ready).
Output: 2 new files; zero modifications to existing layouts.
</objective>

<execution_context>
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-performance-legal-polish/09-CONTEXT.md
@.planning/phases/09-performance-legal-polish/09-RESEARCH.md
@.planning/phases/09-performance-legal-polish/09-PATTERNS.md
@.planning/phases/09-performance-legal-polish/09-UI-SPEC.md
@.planning/phases/09-performance-legal-polish/09-VALIDATION.md
@CLAUDE.md
@src/lib/cart/session.ts
@src/components/cart/CartDrawer.tsx
@messages/en.json
@messages/ar.json

<interfaces>
<!-- Plan 09-01 already added the i18n namespace -->
```jsonc
"CookieBanner": {
  "body":          "We use strictly-necessary cookies ...",
  "primaryCta":    "Got it",
  "secondaryLink": "Read our Cookie Policy"
}
```

<!-- RESEARCH.md (lines 988-1047) provides the full code for both files; copy verbatim. -->
<!-- PATTERNS.md §"src/components/CookieBanner.tsx" lines 306-334; §"src/lib/cookie-consent.ts" lines 338-345. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create src/lib/cookie-consent.ts helper</name>
  <files>src/lib/cookie-consent.ts</files>
  <read_first>
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-RESEARCH.md (§"Code Examples — Cookie scaffold helper signature" lines 988-1013 — exact code)
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-PATTERNS.md (§"src/lib/cookie-consent.ts" lines 338-345)
    - C:/dev/Antigravity/ORKI/src/lib/cart/session.ts (analog cookie-helper conventions in this codebase)
  </read_first>
  <action>
    Create `src/lib/cookie-consent.ts` with EXACTLY this content (per RESEARCH.md lines 990-1013, with the `hasCookieConsent` predicate added per PATTERNS.md "API surface should match what a future <CookieBanner> will call: getConsent(), setConsent('accepted' | 'rejected'), hasConsent()"):

    ```ts
    // Future-proofing scaffold for Phase 9. Built but unmounted (CONTEXT §2).
    // When marketing/analytics cookies are added in a future phase, the banner mounts and
    // these helpers become live. Today: no analytics cookies → banner not rendered.

    const COOKIE_NAME = 'cookie_consent';

    export type ConsentValue = 'accepted' | 'rejected' | 'pending';

    /** Reads the cookie. Returns 'pending' if not set. Client-side only. */
    export function readCookieConsent(): ConsentValue {
      if (typeof document === 'undefined') return 'pending';
      const match = document.cookie.match(new RegExp(`(^|;\\s*)${COOKIE_NAME}=([^;]+)`));
      if (!match) return 'pending';
      return (match[2] === 'accepted' || match[2] === 'rejected') ? match[2] : 'pending';
    }

    /** Writes the cookie with 365-day expiry, SameSite=Lax. Client-side only. */
    export function writeCookieConsent(value: 'accepted' | 'rejected'): void {
      if (typeof document === 'undefined') return;
      const expires = new Date(Date.now() + 365 * 24 * 60 * 60 * 1000).toUTCString();
      document.cookie = `${COOKIE_NAME}=${value}; expires=${expires}; path=/; SameSite=Lax`;
    }

    /** Convenience predicate. */
    export function hasCookieConsent(): boolean {
      const v = readCookieConsent();
      return v === 'accepted' || v === 'rejected';
    }
    ```

    Note: this module is client-safe (no `'server-only'` import) since it reads/writes `document.cookie`. It is also import-safe in server contexts (functions guard with `typeof document === 'undefined'`).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('src/lib/cookie-consent.ts','utf8'); const ok = /COOKIE_NAME = 'cookie_consent'/.test(c) && /export type ConsentValue/.test(c) && /export function readCookieConsent/.test(c) && /export function writeCookieConsent/.test(c) && /SameSite=Lax/.test(c) && /365/.test(c); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/cookie-consent.ts` exists.
    - Exports `ConsentValue` type with `'accepted' | 'rejected' | 'pending'` union.
    - Exports `readCookieConsent`, `writeCookieConsent`, `hasCookieConsent` named functions.
    - `writeCookieConsent` sets `SameSite=Lax` and `path=/`.
    - Cookie name is the literal `cookie_consent`.
    - Both functions guard SSR with `typeof document === 'undefined'`.
    - `npx tsc --noEmit` passes against this file.
  </acceptance_criteria>
  <done>
    Helper module compiles, exposes the locked API surface, is SSR-safe.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create src/components/CookieBanner.tsx (built, NOT mounted)</name>
  <files>src/components/CookieBanner.tsx</files>
  <read_first>
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-RESEARCH.md (§"Code Examples — Cookie scaffold helper signature", lines 1015-1047 — exact code for the banner)
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-PATTERNS.md (§"src/components/CookieBanner.tsx" lines 306-334 — `'use client'`, fixed positioning with `inline-end-0` for RTL)
    - C:/dev/Antigravity/ORKI/.planning/phases/09-performance-legal-polish/09-UI-SPEC.md (§"CookieBanner scaffold" lines 163-172 — locked EN/AR copy; §"Animation" line 294 — slide-up from bottom-inline-end)
    - C:/dev/Antigravity/ORKI/CLAUDE.md (RTL — logical CSS only)
    - C:/dev/Antigravity/ORKI/src/components/cart/CartDrawer.tsx (closest analog for `'use client'` + `Link` + locale prop)
  </read_first>
  <action>
    Create `src/components/CookieBanner.tsx` with the exact pattern from RESEARCH.md lines 1015-1047, with minor tightening to match UI-SPEC §"Component Inventory" (use shadcn-styled button instead of native — match CartDrawer's button styling):

    ```tsx
    'use client';

    import { useEffect, useState } from 'react';
    import { useTranslations } from 'next-intl';
    import { Link } from '@/i18n/navigation';
    import { readCookieConsent, writeCookieConsent } from '@/lib/cookie-consent';

    interface Props {
      // Reserved for future use; banner is locale-agnostic in copy via next-intl.
      // Kept on the prop signature so the future flip is a one-line render change.
      locale?: 'en' | 'ar';
    }

    /**
     * CookieBanner — future-proofing scaffold built in Phase 9 (CONTEXT §2).
     * NOT MOUNTED in any layout this phase. When marketing/analytics cookies are added,
     * mount in src/app/[locale]/layout.tsx as <CookieBanner locale={locale} />.
     */
    export function CookieBanner(_props: Props) {
      const [show, setShow] = useState(false);
      const t = useTranslations('CookieBanner');

      useEffect(() => {
        setShow(readCookieConsent() === 'pending');
      }, []);

      if (!show) return null;

      return (
        <div
          className="fixed bottom-0 inline-end-0 z-50 max-w-md p-6 m-6 bg-[#111111] border border-white/10 space-y-4"
          role="region"
          aria-label="Cookie consent"
        >
          <p className="text-sm text-white/80 leading-relaxed">{t('body')}</p>
          <Link
            href="/legal/cookies"
            className="text-xs text-white/60 underline inline-block"
          >
            {t('secondaryLink')}
          </Link>
          <div>
            <button
              onClick={() => {
                writeCookieConsent('accepted');
                setShow(false);
              }}
              className="px-4 py-2 bg-white text-black text-sm font-semibold rounded-none"
            >
              {t('primaryCta')}
            </button>
          </div>
        </div>
      );
    }
    ```

    Notes:
    - `'use client'` — required for `useState`/`useEffect` and for reading `document.cookie`.
    - Positioning uses `inline-end-0` (logical) — RTL flips automatically per CLAUDE.md.
    - Padding/margin uses symmetric `p-6 m-6` — no `pl-`/`pr-` directional issues.
    - `<Link>` from `@/i18n/navigation` — locale prefix automatic.
    - `role="region"` + `aria-label` for screen-reader landmark.
    - **CRITICAL — DO NOT add this component to any layout.** VALIDATION row LGL-02 (no banner) requires that `grep -r 'import.*CookieBanner' src/app/` returns 0 lines.
    - Component compiles independently; type-check passes; future flip is one line in `src/app/[locale]/layout.tsx`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('src/components/CookieBanner.tsx','utf8'); const ok = /'use client'/.test(c) && /useTranslations\('CookieBanner'\)/.test(c) && /readCookieConsent/.test(c) && /writeCookieConsent/.test(c) && /inline-end-0/.test(c) && !/\bml-\d/.test(c) && !/\bmr-\d/.test(c) && !/\bpl-\d/.test(c) && !/\bpr-\d/.test(c); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/CookieBanner.tsx` exists.
    - Has `'use client'` directive at top.
    - Calls `useTranslations('CookieBanner')` (matches namespace from Plan 09-01).
    - Imports `readCookieConsent` and `writeCookieConsent` from `@/lib/cookie-consent`.
    - Uses `inline-end-0` (logical CSS), no `ml-`/`mr-`/`pl-`/`pr-` directional classes (CLAUDE.md rule).
    - Has `role="region"` + `aria-label` on the outer container.
    - **CRITICAL VALIDATION row LGL-02 (no banner verification):** `grep -r 'import.*CookieBanner' src/app/` returns 0 matches (component is built but NOT imported anywhere).
    - `npm run build` exits 0 — banner type-checks without being mounted.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>
    Banner component compiles, type-checks, and lives in the codebase as a scaffold. No layout imports it. The future flip to enable it is a single-line change.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → document.cookie (CookieBanner) | Reads/writes `cookie_consent` cookie client-side |
| Component instance → no layout import | The banner is NOT in any rendered tree this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06-01 | Tampering | `cookie_consent` cookie value tampered by user | accept (LOW) | Banner is unmounted — value is currently irrelevant to any code path. When mounted in a future phase, the helper's `readCookieConsent` constrains `match[2]` to literal `'accepted'` or `'rejected'`; any other value falls back to `'pending'`. Tampering can only force the banner re-show, which is benign. |
| T-09-06-02 | Information Disclosure | Banner shipped to client bundle even when unmounted | accept | Tree-shaking removes unimported components from the production bundle. Verify with `npm run build` — bundle analysis SHOULD show CookieBanner code is NOT in the chunks served to users. If a future phase adds an import, this threat reactivates. |
| T-09-06-03 | Spoofing | Cookie set with overly-permissive `SameSite=None` | accept | Helper hard-codes `SameSite=Lax` (RESEARCH.md Pattern). `Secure` flag is NOT set (acceptable: cookie carries no secret; `Lax` is sufficient cross-site protection for a yes/no consent state). |
| T-09-06-04 | Stored XSS | Banner copy interpolated via `t()` from i18n bundle | accept (LOW) | All copy is static JSON; React text-node escaping prevents injection. No `t.rich()` with HTML or user-supplied components. |
| T-09-06-05 | Tampering | Future flip mistakenly mounts banner before policy update | mitigate | This plan's success criterion explicitly REQUIRES `grep -r 'import.*CookieBanner' src/app/ returns 0`. Future phase that mounts MUST update Cookie Policy section 3 ("We do not use") before flipping. Encode this in the future-phase plan, not here. |

**HIGH severity threats:** None.
</threat_model>

<verification>
After all tasks:
1. `test -f src/lib/cookie-consent.ts && echo OK` returns OK.
2. `test -f src/components/CookieBanner.tsx && echo OK` returns OK.
3. **CRITICAL** — `grep -r 'import.*CookieBanner' src/app/` returns 0 lines (VALIDATION row LGL-02 no-banner check).
4. `grep -r 'import.*CookieBanner' src/components/` may return 0 or more — irrelevant; what matters is that no layout under `src/app/` imports it.
5. `npm run build` exits 0.
6. `npx tsc --noEmit` exits 0.
7. Bundle audit (informational): `npm run build` output should NOT show CookieBanner in any user-facing route's chunk listing. (Tree-shaking handles this; verify only if a regression is suspected.)
</verification>

<success_criteria>
- `src/lib/cookie-consent.ts` exports the typed API surface (`ConsentValue`, `readCookieConsent`, `writeCookieConsent`, `hasCookieConsent`).
- `src/components/CookieBanner.tsx` exists, has `'use client'`, uses logical CSS only.
- Banner is NOT imported anywhere in `src/app/` — a key VALIDATION assertion (LGL-02 "no banner this phase").
- Both files type-check and the production build succeeds without them being in the rendered tree.
- Future-flip-readiness: the only change required to enable the banner in a future phase is one `<CookieBanner locale={locale} />` line inside `src/app/[locale]/layout.tsx`.
</success_criteria>

<output>
After completion, create `.planning/phases/09-performance-legal-polish/09-06-SUMMARY.md` summarizing the two files, the API surface, and explicitly noting that no layout imports the banner.
</output>
