---
phase: 02-core-shopping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/products.ts
  - src/lib/products.ts
  - src/lib/animation-presets.ts
  - messages/en.json
  - messages/ar.json
autonomous: true
requirements:
  - SHOP-01
  - SHOP-02
  - SHOP-03
  - SHOP-04
  - PDP-03
  - PDP-08
  - PDP-09
  - ANIM-03

must_haves:
  truths:
    - "getAllProducts() returns 6 products"
    - "getProductsByCategory('tops') returns exactly 3 products"
    - "getProductsByCategory('bottoms') returns exactly 3 products"
    - "getStockState returns 'partial' for a product with mixed inStock sizes"
    - "getStockState returns 'out-of-stock' for a product with all sizes inStock=false"
    - "getRelatedProducts excludes the current product id and limits to 4"
    - "animationPresets includes cardHover, badgePop, dropdownOpen, successState keys"
    - "messages/en.json has a Shop namespace with all Phase 2 keys"
    - "messages/ar.json has a Shop namespace with all Phase 2 Arabic translations"
  artifacts:
    - path: "src/data/products.ts"
      provides: "6 ORKI-style products covering all stock states"
      contains: "orki-washed-tee-ecru"
    - path: "src/lib/products.ts"
      provides: "getStockState and getRelatedProducts functions"
      contains: "export function getStockState"
    - path: "src/lib/animation-presets.ts"
      provides: "Phase 2 animation presets"
      contains: "cardHover"
    - path: "messages/en.json"
      provides: "English Shop namespace translations"
      contains: "\"Shop\""
    - path: "messages/ar.json"
      provides: "Arabic Shop namespace translations"
      contains: "\"Shop\""
  key_links:
    - from: "src/lib/products.ts"
      to: "src/data/products.ts"
      via: "import { products } from '@/data/products'"
      pattern: "from '@/data/products'"
    - from: "src/lib/animation-presets.ts"
      to: "components using Motion"
      via: "animationPresets.cardHover / badgePop / dropdownOpen / successState"
      pattern: "cardHover|badgePop|dropdownOpen|successState"
---

<objective>
Establish the data foundation that all Phase 2 components depend on: product data (6 ORKI products covering all stock states), two new lib/products.ts helper functions (getStockState, getRelatedProducts), four new animation preset keys, and all Shop namespace translation keys in both locales.

Purpose: Every shop component in Waves 2–3 imports from lib/products.ts and animationPresets, and reads translation keys from the Shop namespace. This plan makes those contracts concrete before any component exists.

Output: Populated src/data/products.ts, extended src/lib/products.ts and src/lib/animation-presets.ts, extended messages/en.json and messages/ar.json.
</objective>

<execution_context>
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-shopping/02-RESEARCH.md
@.planning/phases/02-core-shopping/02-UI-SPEC.md
@.planning/phases/02-core-shopping/02-PATTERNS.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- From src/types/domain.ts — the shape every product must satisfy -->
```typescript
export type Locale = 'en' | 'ar';

export interface Size {
  label: string; // 'XS' | 'S' | 'M' | 'L' | 'XL'
  inStock: boolean;
}

export interface Product {
  id: string;
  slug: string;
  name: { en: string; ar: string };
  description: { en: string; ar: string };
  category: 'tops' | 'bottoms';
  price: number;
  currency: 'SAR';
  sizes: Size[];
  images: string[];
  inStock: boolean;
}
```

<!-- From src/lib/products.ts — existing functions, extend DO NOT rewrite -->
```typescript
import {Product} from '@/types/domain';
import {products} from '@/data/products';

export function getAllProducts(): Product[] { return products }
export function getProductBySlug(slug: string): Product | undefined {
  return products.find((p) => p.slug === slug);
}
export function getProductsByCategory(category: 'tops' | 'bottoms'): Product[] {
  return products.filter((p) => p.category === category);
}
```

<!-- From src/lib/animation-presets.ts — existing object, add inside it before closing `} as const` -->
```typescript
export const animationPresets = {
  navEnter: { duration: 0.2, ease: [0.23, 1, 0.32, 1] as const },
  navExit:  { duration: 0.16, ease: [0.32, 0.72, 0, 1] as const },
  fadeIn:   { duration: 0.2, ease: [0.23, 1, 0.32, 1] as const },
  fadeOut:  { duration: 0.15, ease: [0.32, 0.72, 0, 1] as const },
} as const;
export type AnimationPresetKey = keyof typeof animationPresets;
```

<!-- From messages/en.json — existing keys, add alongside them -->
```json
{
  "Nav": { "shop": "Shop", "tops": "Tops", "bottoms": "Bottoms", "about": "About", ... },
  "Footer": { ... }, "Placeholder": { ... }, "Meta": { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate product data and extend lib/products.ts</name>
  <files>src/data/products.ts, src/lib/products.ts</files>
  <read_first>
    - src/data/products.ts (current stub — currently empty array)
    - src/lib/products.ts (existing functions — must be preserved, not rewritten)
    - src/types/domain.ts (Product, Size, CartItem types — shapes are the backend contract)
  </read_first>
  <action>
**src/data/products.ts** — Replace the empty array with 6 ORKI products. Array order = newest first (default sort). Must cover all stock states: 2 fully-in-stock, 2 partial-OOS, 1 fully-OOS, 1 mixed. Use these exact products:

```typescript
import {Product} from '@/types/domain';

// Static product data — populated in Phase 2.
// Do NOT import this file directly from components.
// Always use /lib/products.ts as the data access layer.
export const products: Product[] = [
  {
    id: 'orki-heavy-tee-black',
    slug: 'orki-heavy-tee-black',
    name: { en: 'ORKI Heavy Tee — Black', ar: 'تيشيرت أوركي الثقيل — أسود' },
    description: {
      en: 'Heavyweight 320gsm cotton. Dropped shoulders. ORKI wordmark embroidered at chest.',
      ar: 'قطن ثقيل 320 جرام. أكتاف منخفضة. شعار أوركي مطرّز على الصدر.',
    },
    category: 'tops',
    price: 249,
    currency: 'SAR',
    sizes: [
      { label: 'XS', inStock: true },
      { label: 'S', inStock: true },
      { label: 'M', inStock: false },
      { label: 'L', inStock: true },
      { label: 'XL', inStock: true },
    ],
    images: [],
    inStock: true,
  },
  {
    id: 'orki-oversized-tee-white',
    slug: 'orki-oversized-tee-white',
    name: { en: 'ORKI Oversized Tee — White', ar: 'تيشيرت أوركي أوفرسايز — أبيض' },
    description: {
      en: 'Ultra-oversized silhouette. 300gsm cotton. Screen-printed graphic at back.',
      ar: 'سيلويت فضفاض للغاية. قطن 300 جرام. طباعة مطبوعة بالشاشة على الظهر.',
    },
    category: 'tops',
    price: 269,
    currency: 'SAR',
    sizes: [
      { label: 'XS', inStock: true },
      { label: 'S', inStock: true },
      { label: 'M', inStock: true },
      { label: 'L', inStock: true },
      { label: 'XL', inStock: true },
    ],
    images: [],
    inStock: true,
  },
  {
    id: 'orki-washed-tee-ecru',
    slug: 'orki-washed-tee-ecru',
    name: { en: 'ORKI Washed Tee — Ecru', ar: 'تيشيرت أوركي المغسول — إيكرو' },
    description: {
      en: 'Acid-washed 280gsm jersey. Relaxed fit. Chest pocket with woven label.',
      ar: 'جيرسي مغسول بالحامض 280 جرام. قصة مريحة. جيب صدر بعلامة منسوجة.',
    },
    category: 'tops',
    price: 219,
    currency: 'SAR',
    sizes: [
      { label: 'XS', inStock: false },
      { label: 'S', inStock: false },
      { label: 'M', inStock: false },
      { label: 'L', inStock: false },
      { label: 'XL', inStock: false },
    ],
    images: [],
    inStock: false,
  },
  {
    id: 'orki-utility-cargo-black',
    slug: 'orki-utility-cargo-black',
    name: { en: 'ORKI Utility Cargo — Black', ar: 'بنطلون أوركي الكارغو — أسود' },
    description: {
      en: 'Relaxed cargo silhouette. Six pockets. Adjustable ankle cuffs. Twill fabric.',
      ar: 'سيلويت كارغو مريح. ستة جيوب. أكمام قابلة للتعديل عند الكاحل. قماش تويل.',
    },
    category: 'bottoms',
    price: 349,
    currency: 'SAR',
    sizes: [
      { label: 'XS', inStock: true },
      { label: 'S', inStock: true },
      { label: 'M', inStock: true },
      { label: 'L', inStock: true },
      { label: 'XL', inStock: false },
    ],
    images: [],
    inStock: true,
  },
  {
    id: 'orki-wide-leg-trousers-black',
    slug: 'orki-wide-leg-trousers-black',
    name: { en: 'ORKI Wide Leg Trousers — Black', ar: 'بنطلون أوركي واسع الساق — أسود' },
    description: {
      en: 'High-rise wide leg cut. Pressed front crease. Concealed zip fly. Premium wool blend.',
      ar: 'قصة ساق واسعة بخصر مرتفع. كسرة أمامية مكوية. سحاب مخفي. خليط صوف فاخر.',
    },
    category: 'bottoms',
    price: 399,
    currency: 'SAR',
    sizes: [
      { label: 'XS', inStock: true },
      { label: 'S', inStock: true },
      { label: 'M', inStock: true },
      { label: 'L', inStock: true },
      { label: 'XL', inStock: true },
    ],
    images: [],
    inStock: true,
  },
  {
    id: 'orki-track-pants-black',
    slug: 'orki-track-pants-black',
    name: { en: 'ORKI Track Pants — Black', ar: 'بنطلون تراك أوركي — أسود' },
    description: {
      en: 'Technical fabric. Elastic waistband with drawcord. Tapered leg. Side zip pockets.',
      ar: 'قماش تقني. خصر مطاطي مع حبل ربط. ساق مخروطية. جيوب جانبية بسحاب.',
    },
    category: 'bottoms',
    price: 299,
    currency: 'SAR',
    sizes: [
      { label: 'XS', inStock: false },
      { label: 'S', inStock: true },
      { label: 'M', inStock: true },
      { label: 'L', inStock: false },
      { label: 'XL', inStock: true },
    ],
    images: [],
    inStock: true,
  },
]
```

**src/lib/products.ts** — APPEND below the existing 3 exports (do not touch or rewrite existing functions). Add:

```typescript
// Stock state helper — derives 3-way state from sizes array.
// DO NOT add a new field to Product — the domain type is the backend contract.
export type StockState = 'in-stock' | 'partial' | 'out-of-stock'

export function getStockState(product: Product): StockState {
  const hasAnyInStock = product.sizes.some(s => s.inStock)
  const hasAnyOOS = product.sizes.some(s => !s.inStock)
  if (!hasAnyInStock) return 'out-of-stock'
  if (hasAnyOOS) return 'partial'
  return 'in-stock'
}

export function getRelatedProducts(
  currentId: string,
  category: 'tops' | 'bottoms',
  limit = 4
): Product[] {
  return products
    .filter(p => p.category === category && p.id !== currentId)
    .slice(0, limit)
}
```
  </action>
  <verify>
    <automated>cd C:/dev/Antigravity/ORKI && node -e "const {getAllProducts,getProductsByCategory,getStockState,getRelatedProducts}=require('./src/lib/products.ts')" 2>&1 || npx tsc --noEmit --strict 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "orki-washed-tee-ecru" src/data/products.ts returns 1
    - grep -c "export function getStockState" src/lib/products.ts returns 1
    - grep -c "export function getRelatedProducts" src/lib/products.ts returns 1
    - grep -c "export type StockState" src/lib/products.ts returns 1
    - grep -c "inStock: false" src/data/products.ts returns at least 6 (multiple OOS sizes across products)
    - src/data/products.ts contains exactly 6 objects (6 product IDs)
    - src/lib/products.ts contains "from '@/data/products'" (existing import preserved)
  </acceptance_criteria>
  <done>src/data/products.ts has 6 ORKI products (3 tops, 3 bottoms) covering all stock states; src/lib/products.ts has getStockState and getRelatedProducts appended after existing exports.</done>
</task>

<task type="auto">
  <name>Task 2: Extend animation presets and add Shop i18n keys</name>
  <files>src/lib/animation-presets.ts, messages/en.json, messages/ar.json</files>
  <read_first>
    - src/lib/animation-presets.ts (existing presets — add inside the object, before closing `} as const`)
    - messages/en.json (existing Nav/Footer/Placeholder/Meta keys — add Shop and extend Nav)
    - messages/ar.json (existing Nav/Footer/Placeholder/Meta keys — add Shop and extend Nav)
  </read_first>
  <action>
**src/lib/animation-presets.ts** — Add 4 new preset keys inside the `animationPresets` object, before the closing `} as const`. Do not change existing keys. The final object must be:

```typescript
export const animationPresets = {
  navEnter:  { duration: 0.2,  ease: [0.23, 1, 0.32, 1] as const },
  navExit:   { duration: 0.16, ease: [0.32, 0.72, 0, 1] as const },
  fadeIn:    { duration: 0.2,  ease: [0.23, 1, 0.32, 1] as const },
  fadeOut:   { duration: 0.15, ease: [0.32, 0.72, 0, 1] as const },
  // Phase 2 additions
  cardHover: {
    duration: 0.3,
    ease: [0, 0, 0.2, 1] as const,   // CSS ease-out equivalent — smooth zoom for product card
  },
  badgePop: {
    duration: 0.15,
    ease: [0.34, 1.56, 0.64, 1] as const,  // slight overshoot for tactile badge pop
  },
  dropdownOpen: {
    duration: 0.15,
    ease: [0.23, 1, 0.32, 1] as const,     // reuse navEnter easing for category dropdown
  },
  successState: {
    duration: 0.15,
    ease: [0.23, 1, 0.32, 1] as const,     // AddToCartButton label swap fade
  },
} as const;

export type AnimationPresetKey = keyof typeof animationPresets;
```

**messages/en.json** — Extend Nav with `categories` and `cart` keys. Add entire `Shop` namespace. Keep all existing keys intact:

```json
{
  "Nav": {
    "shop": "Shop",
    "tops": "Tops",
    "bottoms": "Bottoms",
    "about": "About",
    "openMenu": "Open menu",
    "closeMenu": "Close menu",
    "switchToAr": "AR",
    "switchToEn": "EN",
    "categories": "Categories",
    "cart": "Cart"
  },
  "Footer": {
    "shipping": "Shipping",
    "returns": "Returns",
    "contact": "Contact",
    "copyright": "© 2026 ORKI. All rights reserved."
  },
  "Placeholder": {
    "imageAlt": "ORKI product placeholder"
  },
  "Meta": {
    "siteTitle": "ORKI — Underground Streetwear"
  },
  "Shop": {
    "tabAll": "All",
    "tabTops": "Tops",
    "tabBottoms": "Bottoms",
    "productCount": "{count} Products",
    "sortLabel": "Sort:",
    "sortNewest": "Newest",
    "sortPriceAsc": "Price: Low to High",
    "sortPriceDesc": "Price: High to Low",
    "emptyHeading": "Nothing here yet.",
    "emptyBody": "Browse all products or try a different category.",
    "sizeLabel": "Size",
    "sizeGuide": "Size Guide",
    "addToCart": "Add to Cart",
    "added": "Added",
    "outOfStock": "Out of Stock",
    "returnPolicy": "Free returns within 14 days. No questions asked.",
    "relatedHeading": "You Might Also Like",
    "someSizesSoldOut": "Some sizes sold out",
    "sizeGuideTitle": "Size Guide",
    "sizeGuideClose": "Close"
  }
}
```

**messages/ar.json** — Same structure with Arabic translations:

```json
{
  "Nav": {
    "shop": "المتجر",
    "tops": "الأعلى",
    "bottoms": "الأسفل",
    "about": "عن أوركي",
    "openMenu": "فتح القائمة",
    "closeMenu": "إغلاق القائمة",
    "switchToAr": "AR",
    "switchToEn": "EN",
    "categories": "الفئات",
    "cart": "السلة"
  },
  "Footer": {
    "shipping": "الشحن",
    "returns": "المرتجعات",
    "contact": "تواصل معنا",
    "copyright": "© 2026 ORKI. جميع الحقوق محفوظة."
  },
  "Placeholder": {
    "imageAlt": "صورة بديلة لمنتج أوركي"
  },
  "Meta": {
    "siteTitle": "أوركي — ملابس الشوارع"
  },
  "Shop": {
    "tabAll": "الكل",
    "tabTops": "التيشيرتات",
    "tabBottoms": "البناطيل",
    "productCount": "{count} منتجات",
    "sortLabel": "ترتيب:",
    "sortNewest": "الأحدث",
    "sortPriceAsc": "السعر: من الأقل للأعلى",
    "sortPriceDesc": "السعر: من الأعلى للأقل",
    "emptyHeading": "لا يوجد شيء هنا بعد.",
    "emptyBody": "تصفح جميع المنتجات أو جرّب فئة أخرى.",
    "sizeLabel": "المقاس",
    "sizeGuide": "دليل المقاسات",
    "addToCart": "أضف إلى السلة",
    "added": "تمت الإضافة",
    "outOfStock": "نفد المخزون",
    "returnPolicy": "إرجاع مجاني خلال 14 يوماً بدون أي أسئلة.",
    "relatedHeading": "قد يعجبك أيضاً",
    "someSizesSoldOut": "بعض المقاسات نفدت",
    "sizeGuideTitle": "دليل المقاسات",
    "sizeGuideClose": "إغلاق"
  }
}
```
  </action>
  <verify>
    <automated>cd C:/dev/Antigravity/ORKI && node -e "const p=require('./src/lib/animation-presets.ts'); if(!p.animationPresets.cardHover) throw new Error('cardHover missing')" 2>&1 || npx tsc --noEmit 2>&1 | grep "animation-presets" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "cardHover" src/lib/animation-presets.ts returns 1
    - grep -c "badgePop" src/lib/animation-presets.ts returns 1
    - grep -c "dropdownOpen" src/lib/animation-presets.ts returns 1
    - grep -c "successState" src/lib/animation-presets.ts returns 1
    - grep -c '"Shop"' messages/en.json returns 1
    - grep -c '"Shop"' messages/ar.json returns 1
    - grep -c '"categories": "Categories"' messages/en.json returns 1
    - grep -c '"cart": "Cart"' messages/en.json returns 1
    - grep -c '"addToCart"' messages/en.json returns 1
    - grep -c '"addToCart"' messages/ar.json returns 1
    - grep -c '"returnPolicy"' messages/en.json returns 1
    - grep -c '"returnPolicy"' messages/ar.json returns 1
    - node -e "JSON.parse(require('fs').readFileSync('messages/en.json','utf8'))" exits 0 (valid JSON)
    - node -e "JSON.parse(require('fs').readFileSync('messages/ar.json','utf8'))" exits 0 (valid JSON)
  </acceptance_criteria>
  <done>animation-presets.ts has 4 new Phase 2 presets; en.json and ar.json have complete Shop namespace and extended Nav keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Data file → lib → components | src/data/products.ts is static; no user input crosses this boundary in Phase 2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | src/data/products.ts | accept | Static file, no runtime modification; risk is zero until backend integration |
| T-02-02 | Information Disclosure | messages/en.json, messages/ar.json | accept | Translation files are public by design; no secrets present |
</threat_model>

<verification>
After both tasks complete:
- `grep -c "export function getStockState" src/lib/products.ts` → 1
- `grep -c "export function getRelatedProducts" src/lib/products.ts` → 1
- `grep -c "cardHover" src/lib/animation-presets.ts` → 1
- `node -e "JSON.parse(require('fs').readFileSync('messages/en.json','utf8'))"` → exits 0
- `npx tsc --noEmit` → exits 0 (no TypeScript errors)
</verification>

<success_criteria>
- src/data/products.ts contains 6 products: 3 tops (1 partial OOS, 1 fully OOS, 1 in-stock), 3 bottoms (1 partial OOS, 1 in-stock, 1 in-stock with partial OOS sizes)
- src/lib/products.ts exports getStockState returning 'in-stock' | 'partial' | 'out-of-stock'
- src/lib/products.ts exports getRelatedProducts filtering by category and excluding current product id
- src/lib/animation-presets.ts contains cardHover, badgePop, dropdownOpen, successState with exact values from UI-SPEC
- messages/en.json and messages/ar.json both contain Shop namespace with 16 keys each
- messages/en.json Nav namespace includes "categories" and "cart" keys
- TypeScript compilation passes with no errors on modified files
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-shopping/02-01-SUMMARY.md` following the summary template.
</output>
