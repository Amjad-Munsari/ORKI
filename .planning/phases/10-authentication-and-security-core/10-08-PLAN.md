---
phase: 10-authentication-and-security-core
plan: 08
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/app/api/auth/callback/route.ts
  - src/lib/cart/merge-on-signin.ts
  - src/components/nav/CartBadge.tsx
  - src/components/nav/MobileNavDrawer.tsx
  - src/components/auth/ForgotPasswordForm.tsx
  - tests/integration/cart-merge-union.test.ts
autonomous: true
requirements: [SEC-04, SEC-06, UX-09]
source: 10-UAT.md

must_haves:
  truths:
    - "Clicking the Supabase recovery email link opens the reset-password form (round-trip completes)"
    - "A guest's cart item is retained after sign-in even when the account already owns a cart"
    - "Auth/nav pages hydrate without a real (non-extension) Base UI useId mismatch under prefers-reduced-motion"
    - "Forgot-password success state shows exactly one 'back to sign in' affordance"
  artifacts:
    - path: "src/app/api/auth/callback/route.ts"
      provides: "token_hash + type=recovery verifyOtp branch (with ?code fallback)"
      contains: "verifyOtp"
    - path: "src/lib/cart/merge-on-signin.ts"
      provides: "Item-level cart_items union (onConflictDoUpdate summing quantity) before deleting the empty guest cart"
      contains: "onConflictDoUpdate"
    - path: "src/components/nav/CartBadge.tsx"
      provides: "Hydration-safe reduced-motion read"
      contains: "useReducedMotionSafe"
    - path: "src/components/nav/MobileNavDrawer.tsx"
      provides: "Hydration-safe reduced-motion read"
      contains: "useReducedMotionSafe"
    - path: "tests/integration/cart-merge-union.test.ts"
      provides: "Regression test proving guest item is unioned, not destroyed, when a user cart exists"
      contains: "mergeGuestCartIntoUserCart"
  key_links:
    - from: "src/app/api/auth/callback/route.ts"
      to: "supabase.auth.verifyOtp"
      via: "token_hash branch"
      pattern: "verifyOtp\\(\\{\\s*type"
    - from: "src/lib/cart/merge-on-signin.ts"
      to: "cart_items unique index (cartId, productId, sizeId)"
      via: "onConflictDoUpdate summing quantity"
      pattern: "onConflictDoUpdate"
    - from: "src/components/nav/CartBadge.tsx"
      to: "src/hooks/useReducedMotionSafe.ts"
      via: "import"
      pattern: "useReducedMotionSafe"
---

<objective>
Close five diagnosed Phase 10 UAT gaps (Tests 4, 6, 7, 10, 12). Four are code defects with file:line root causes; one is a non-defect verify-only item with optional defensive hardening.

Purpose: Phase 10 (Authentication & Security Core) UAT surfaced four genuine defects — a broken password-reset round-trip, a guest-cart merge that destroys items, a hydration mismatch on the nav, and a duplicate back-link — plus one environmental false positive. Each fix must be independently committable so the UAT can be re-run gap-by-gap.

Output:
- Recovery callback handles Supabase's default `token_hash`+`type=recovery` link shape.
- Guest cart merge unions items instead of cascade-deleting them.
- CartBadge + MobileNavDrawer use the hydration-safe reduced-motion hook.
- Forgot-password form drops its redundant success-state back-link.
- A regression test proving the cart-merge union.
- Re-verification notes for the environmental Test 4 symptom (optional hardening only).
</objective>

<execution_context>
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-authentication-and-security-core/10-UAT.md
@.planning/STATE.md

<interfaces>
<!-- Exact contracts the executor needs. Already extracted from the codebase — no exploration required. -->

From src/lib/supabase/server.ts — SSR client factory (RLS-bound, owns the auth cookie pair):
```ts
export async function createClient(): Promise<SupabaseClient>;
// supabase.auth.exchangeCodeForSession(code)  → PKCE ?code shape
// supabase.auth.verifyOtp({ type: 'recovery', token_hash }) → default email link shape
```

From src/lib/cart/server.ts:42-59 — the UPSERT pattern to MIRROR for the union:
```ts
await db
  .insert(cartItems)
  .values({ cartId, productId, sizeId, quantity })
  .onConflictDoUpdate({
    target: [cartItems.cartId, cartItems.productId, cartItems.sizeId],
    set: { quantity: sql`${cartItems.quantity} + ${quantity}` },
  });
```

From src/lib/db/schema.ts:125-141 — cart_items table + unique index:
```ts
export const cartItems = pgTable('cart_items', {
  id: uuid('id').primaryKey().defaultRandom(),
  cartId: uuid('cart_id').notNull().references(() => carts.id, { onDelete: 'cascade' }),
  productId: text('product_id').notNull()...,
  sizeId: uuid('size_id').notNull()...,
  quantity: integer('quantity').notNull().default(1),
  addedAt: timestamp('added_at').notNull().defaultNow(),
}, (table) => [
  index('cart_items_cart_id_idx').on(table.cartId),
  uniqueIndex('cart_items_cart_product_size_unq').on(table.cartId, table.productId, table.sizeId),
]);
// cart_items.cartId is ON DELETE CASCADE → deleting a cart row destroys its items.
```

From src/hooks/useReducedMotionSafe.ts — the existing hydration-safe wrapper to adopt:
```ts
export function useReducedMotionSafe(): boolean;
// Returns false on SSR + first client render, then the user's real preference after mount.
// Use ONLY where the value affects RENDERED OUTPUT (markup/inline style).
```

From src/lib/cart/merge-on-signin.ts — current signature (transaction body is what changes):
```ts
export async function mergeGuestCartIntoUserCart(
  userId: string,
  sessionId: string | null | undefined,
): Promise<void>;
// Race-safe: wrapped in db.transaction with SELECT ... FOR UPDATE on the user cart row.
// Idempotent. Never throws — all errors swallowed (auth flow must not break on merge failure).
```

From tests/helpers (used by the new regression test):
```ts
// tests/helpers/test-db.ts
export const hasDbUrl: boolean;
export async function cleanPhase8Tables(): Promise<void>;
export async function deleteTestProduct(productId: string): Promise<void>;
// tests/helpers/factories.ts
export async function seedProductWithSize(opts: { stock: number; sizeLabel: string }): Promise<{ productId: string; sizeId: string; ... }>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add token_hash recovery branch to the auth callback (Gap: Test 7, major)</name>
  <files>src/app/api/auth/callback/route.ts</files>
  <action>
Fix the password-reset round-trip (per SEC-04). Root cause: the callback only handles the PKCE `?code` shape via `exchangeCodeForSession`, but Supabase's default "Reset Password" email delivers a `token_hash` + `type=recovery` link. `searchParams.get('code')` is null on every real click, so control falls through to the invalid_link redirect.

Add a `token_hash` branch ABOVE the existing `?code` branch, keeping `?code` as a fallback so both shapes work. Do NOT customize the Supabase email template — `verifyOtp` matches Supabase's current default and avoids PKCE code_verifier-cookie fragility on cross-context email clicks (per UAT root_cause + debug session password-reset-invalid-link.md).

In `GET`, after reading `searchParams` and resolving `locale`/`next`:
1. Read `const token_hash = searchParams.get('token_hash');` and `const type = searchParams.get('type');`.
2. New branch FIRST: `if (token_hash && type === 'recovery') { try { const supabase = await createClient(); const { error } = await supabase.auth.verifyOtp({ type: 'recovery', token_hash }); if (!error) return NextResponse.redirect(\`${origin}${next}\`); } catch (err) { console.error('[auth/callback]', err); } }`.
3. Keep the existing `if (code) { ... exchangeCodeForSession ... }` block UNCHANGED as the fallback.
4. Keep the final invalid_link redirect unchanged.

Update the file's top docblock to mention both shapes (`token_hash`+`type=recovery` primary, `?code` fallback). Use the `verifyOtp` overload that takes `{ type, token_hash }` (the email-link variant) — not the `{ type, email, token }` variant.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | Select-String "callback/route" ; if ($LASTEXITCODE -ne 0) { } </automated>
    <automated>npx playwright test tests/e2e/password-reset.spec.ts -g "callback redirects on invalid code"</automated>
  </verify>
  <done>
The callback contains a `verifyOtp({ type: 'recovery', token_hash })` branch ahead of the `exchangeCodeForSession` fallback. The existing invalid-code e2e test still passes (an invalid `token_hash` AND an invalid `code` both fall through to `/forgot-password?error=invalid_link`). `tsc --noEmit` is clean. Full live round-trip (real email click → reset form) requires Supabase env + a real recovery email; document as the manual re-verification step for UAT Test 7.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Union guest cart items instead of cascade-deleting them (Gap: Test 10, major)</name>
  <files>src/lib/cart/merge-on-signin.ts, tests/integration/cart-merge-union.test.ts</files>
  <behavior>
Regression test (tests/integration/cart-merge-union.test.ts, mirror tests/integration/cart-merge.test.ts structure — `describe.skipIf(!hasDbUrl)`, `seedProductWithSize`, `cleanPhase8Tables`/`deleteTestProduct` lifecycle):
  - Setup: seed a product+size. Create a USER cart (userId = a fixed test uuid, sessionId = `user-sid-…`) holding the same product/size at qty 1. Create a GUEST cart (userId NULL, sessionId = `guest-sid-…`) holding that product/size at qty 2.
  - Act: call `mergeGuestCartIntoUserCart(testUserId, 'guest-sid-…')`.
  - Test A (union, not destroy): the user cart now has ONE cart_items row for that (productId, sizeId) with quantity === 3 (1 + 2). The guest item was NOT lost.
  - Test B (distinct item moved): when the guest cart holds a product/size the user cart does NOT have, after merge the user cart contains that item at its original quantity.
  - Test C (guest cart removed): the guest cart row (sessionId = `guest-sid-…`) no longer exists after merge.
  - Test D (claim branch unchanged): when NO user cart exists, the guest cart is claimed (its sessionId row now has userId === testUserId) and its items are intact.
  </behavior>
  <action>
Fix the guest-cart merge (per UX-09 cart retention). Root cause: when the user already owns a cart, the function takes the DELETE-guest-cart branch (merge-on-signin.ts:46-55); because `cart_items.cartId` is ON DELETE CASCADE (schema.ts:129), the guest's items are destroyed with no item-level union.

Replace the DELETE-only branch (lines 46-55) with a real item-level union, inside the existing transaction, preserving race-safety (`SELECT ... FOR UPDATE` on the user cart) and idempotency:
1. Capture the locked user cart id: `const userCartId = userCart[0].id;`.
2. Resolve the guest cart row by `sessionId` (still inside `tx`): `const guestCart = await tx.select().from(carts).where(and(eq(carts.sessionId, sessionId), isNull(carts.userId)));`. If absent (concurrent claim), `return;` (no-op, idempotent).
3. Load the guest cart's items: `const guestItems = await tx.select().from(cartItems).where(eq(cartItems.cartId, guestCart[0].id));`.
4. For each guest item, upsert into the user cart MIRRORING addItemToCart in server.ts:42-59 — insert `{ cartId: userCartId, productId, sizeId, quantity }` with `.onConflictDoUpdate({ target: [cartItems.cartId, cartItems.productId, cartItems.sizeId], set: { quantity: sql\`${cartItems.quantity} + ${item.quantity}\` } })`. (Batch the inserts in the loop; all within `tx`.)
5. Bump the user cart's `updatedAt: sql\`now()\``.
6. THEN delete the now-empty guest cart row by `(sessionId, isNull(userId))` — the CASCADE only removes the already-moved (empty) guest items.

Import `cartItems` from `@/lib/db/schema` (add to the existing import). Keep the no-user-cart claim branch (lines 58-66) UNCHANGED. Update the file docblock semantics block (lines 7-11) to describe item-level union instead of "silent DELETE / no item-level union", and remove the now-stale "T-10-05-07 accepted: no item-level union" line. Keep the outer try/catch swallow (never throw).
  </action>
  <verify>
    <automated>npx vitest run tests/integration/cart-merge-union.test.ts</automated>
    <automated>npx tsc --noEmit 2>&1 | Select-String "merge-on-signin"</automated>
  </verify>
  <done>
`mergeGuestCartIntoUserCart` unions items via `onConflictDoUpdate` summing quantity on the (cartId, productId, sizeId) unique index, then deletes the empty guest cart — all inside the existing FOR-UPDATE transaction. The new regression test passes Tests A–D (skipped automatically when no DATABASE_URL). `tsc --noEmit` clean. NOTE: this test needs `DATABASE_URL` (live DB) — `describe.skipIf(!hasDbUrl)` gates it like the existing cart-merge.test.ts.
  </done>
</task>

<task type="auto">
  <name>Task 3: Adopt hydration-safe reduced-motion hook in CartBadge + MobileNavDrawer (Gap: Test 12, major)</name>
  <files>src/components/nav/CartBadge.tsx, src/components/nav/MobileNavDrawer.tsx</files>
  <action>
Fix the Base UI `useId` hydration mismatch on the nav (per UX-09 / SEC anti-defect). Root cause: both components call the RAW `useReducedMotion()` from `motion/react` inside AnimatePresence subtrees. Raw `useReducedMotion` resolves `false` on the server but the user's real value on the client's first render — for a `prefers-reduced-motion` user the AnimatePresence subtrees render differently, consuming React `useId` slots from the same counter Base UI's Sheet later draws from, cascading into the SheetTrigger id mismatch. The project already ships `useReducedMotionSafe()` (src/hooks/useReducedMotionSafe.ts) for exactly this hazard; prior fix (commit 1e5c2f5) added it but left these two callers raw.

In BOTH files:
1. Remove `useReducedMotion` from the `motion/react` import (keep `motion`, `AnimatePresence`, and any others).
2. Add `import { useReducedMotionSafe } from '@/hooks/useReducedMotionSafe';` (match each file's existing import-quote style — CartBadge uses no semicolons / single quotes; MobileNavDrawer uses semicolons).
3. Replace `const shouldReduceMotion = useReducedMotion()` with `const shouldReduceMotion = useReducedMotionSafe()`.

Do NOT use `suppressHydrationWarning` anywhere — it only masks the symptom (per UAT `missing` directive).

Do NOT touch src/components/pdp/PDPGallery.tsx:26 — AUDITED: its `shouldReduceMotion` is used only in a `useEffect` (line 39), the effect dep array (line 45), and the scroll `behavior` event value (line 86). None of these affect RENDERED OUTPUT, so per the useReducedMotionSafe docblock the raw hook is correct there; migrating it would defer the parallax `matchMedia` setup by one tick for no hydration benefit. Record this audit result in the SUMMARY.
  </action>
  <verify>
    <automated>(Select-String -Path src/components/nav/CartBadge.tsx,src/components/nav/MobileNavDrawer.tsx -Pattern 'useReducedMotion\b' | Where-Object { $_.Line -notmatch 'useReducedMotionSafe' }).Count -eq 0</automated>
    <automated>npx playwright test tests/e2e/hydration-reduced-motion.spec.ts</automated>
  </verify>
  <done>
Neither CartBadge nor MobileNavDrawer references the raw `useReducedMotion` (only `useReducedMotionSafe`). The grep gate (raw-hook lines minus the Safe-wrapper) returns 0. `tests/e2e/hydration-reduced-motion.spec.ts` passes with `prefers-reduced-motion: reduce` emulated (no hydration error on the nav surfaces). NOTE: the e2e spec needs the dev server + Playwright browsers; if browsers aren't installed, the grep gate + a manual `/ar/login` console check under reduced-motion is the fallback acceptance.
  </done>
</task>

<task type="auto">
  <name>Task 4: Remove duplicate forgot-password back-link (Gap: Test 6, minor)</name>
  <files>src/components/auth/ForgotPasswordForm.tsx</files>
  <action>
Fix the duplicate "back to sign in" affordance (per UX-09). Root cause: the success state renders TWO identical back-links — the page chrome's unconditional footer link (forgot-password/page.tsx:25-32) AND the form's own success-branch link (ForgotPasswordForm.tsx:70-77). The intended pattern (login/page.tsx) has the chrome own the single link and the form render none.

In the `if (submitted)` block (lines 60-80), remove the `<p className="text-center">…<Link href="/login">{t('back')}</Link>…</p>` element (lines 70-77) so only the generic success `<p role="status">` remains. The page-chrome footer link stays and becomes the single affordance.

If, after removing the link, the `Link` import (line 17) and the `t('back')` key become unused in this file, remove the now-unused `Link` import to keep lint clean. (Keep `t` — it's still used for `t('success')`, `t('heading')`, etc.)
  </action>
  <verify>
    <automated>(Select-String -Path src/components/auth/ForgotPasswordForm.tsx -Pattern "t\('back'\)").Count -eq 0</automated>
    <automated>npx eslint src/components/auth/ForgotPasswordForm.tsx</automated>
  </verify>
  <done>
The forgot-password success state renders exactly one back-link (the page chrome's). The form no longer references `t('back')`; ESLint reports no unused-import error for `Link`. The page-chrome link in forgot-password/page.tsx:25-32 is untouched.
  </done>
</task>

<task type="auto">
  <name>Task 5 (OPTIONAL, low-priority): Defensive transport-error guard on login submit (Gap: Test 4, non-defect)</name>
  <files>src/components/auth/LoginForm.tsx</files>
  <action>
NON-DEFECT — verify-only gap. The "Failed to fetch" symptom on invalid login was diagnosed as an ENVIRONMENTAL client-side transport abort (content-injecting browser extension or a transient Turbopack HMR recompile dropping the in-flight Server Action POST), NOT an application defect: a clean-browser Playwright repro succeeded 4/4 and the live endpoint returned HTTP 200 on 15/15 invalid POSTs (per SEC-06 root_cause in 10-UAT.md).

PRIMARY ACTION (do this FIRST, it may make the code change unnecessary): re-run UAT Test 4 in an incognito/clean browser profile with all extensions disabled (and avoid editing files mid-test so HMR doesn't recompile). The "Failed to fetch" symptom should vanish, confirming the environmental diagnosis. Record the clean-profile result in the SUMMARY.

OPTIONAL hardening (apply ONLY if you want belt-and-suspenders graceful degradation; skip if the clean-profile re-verify is green and you prefer minimal diff): in LoginForm.tsx the `onSubmit` handler calls `await signInAction(data)` (around line 57) inside `startTransition`. Wrap that call in try/catch: on a thrown transport error, call `setFormError(tErrors('unknown'))` (the existing generic-error setter / translation accessor already used in the form — match the form's actual `setFormError`/error-translation identifiers; read the file first to confirm names) so an extension/HMR abort degrades to the generic anti-enumeration message instead of bubbling to the Next dev overlay. Do NOT change the success path (`window.location.assign` / `router.push`). Do NOT differentiate the error (anti-enumeration: always the generic key).

This task is OPTIONAL and low-priority. It is acceptable to complete ONLY the clean-profile re-verification and skip the code change; note the decision in the SUMMARY.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | Select-String "LoginForm"</automated>
    <automated>npx playwright test tests/e2e -g "login" 2>&1 | Select-String "passed|failed|No tests"</automated>
  </verify>
  <done>
EITHER: clean-profile re-verification of UAT Test 4 is green (invalid login shows the generic error, no "Failed to fetch") and the optional code change is intentionally skipped — recorded in SUMMARY; OR: the try/catch guard is added, `setFormError(...generic key...)` fires on a thrown transport error, the success path is unchanged, `tsc --noEmit` is clean, and existing login e2e (if any) still passes. Either outcome closes the gap.
  </done>
</task>

</tasks>

<verification>
Per-gap acceptance (each fix is independently committable):

| Gap | Test | Check | Needs |
|-----|------|-------|-------|
| Password-reset round-trip | 7 | `verifyOtp` branch present; invalid-code e2e still redirects to `?error=invalid_link`; live round-trip manual | Supabase env (live round-trip); dev server (e2e) |
| Guest cart merge | 10 | `tests/integration/cart-merge-union.test.ts` Tests A–D pass | `DATABASE_URL` (live DB) |
| Hydration mismatch | 12 | grep gate: no raw `useReducedMotion` in the two nav files; `hydration-reduced-motion.spec.ts` green | dev server + Playwright browsers (e2e); grep gate is env-free |
| Duplicate back-link | 6 | `t('back')` absent in ForgotPasswordForm; ESLint clean | none |
| Invalid-login "Failed to fetch" | 4 | clean-profile re-verify green (PRIMARY); optional guard `tsc` clean | clean browser profile |

Whole-plan regression guard: `npx tsc --noEmit` clean and `npx eslint src/components/nav/CartBadge.tsx src/components/nav/MobileNavDrawer.tsx src/components/auth/ForgotPasswordForm.tsx src/app/api/auth/callback/route.ts src/lib/cart/merge-on-signin.ts` clean.
</verification>

<success_criteria>
- Recovery callback verifies `token_hash`+`type=recovery` via `verifyOtp`, with `?code`/`exchangeCodeForSession` retained as fallback (Test 7 round-trip can complete).
- Guest cart items are unioned into the user cart (quantity summed on the unique index) before the empty guest cart is deleted — no item destruction (Test 10 retained).
- CartBadge + MobileNavDrawer use `useReducedMotionSafe`; no raw `useReducedMotion` remains in either; PDPGallery correctly left unchanged with audit note (Test 12 no mismatch).
- Forgot-password success state shows a single back-link (Test 6).
- Test 4 re-verified in a clean profile (environmental); optional transport guard applied or intentionally skipped with a recorded decision.
- `tsc --noEmit` and ESLint clean across all touched files.
- Each fix committed independently (5 logical commits, one per task; Task 5 may be a no-op verify with no commit).
</success_criteria>

<output>
After completion, create `.planning/phases/10-authentication-and-security-core/10-08-SUMMARY.md` recording: each gap closed, the commit per fix, the PDPGallery audit decision (left unchanged + why), the Test 4 clean-profile re-verification result and whether the optional guard was applied, and which acceptance checks require live env (Supabase / DATABASE_URL / Playwright browsers) vs. ran clean.
</output>
