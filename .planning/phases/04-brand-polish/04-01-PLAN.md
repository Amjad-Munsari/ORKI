# Plan: 04-01-PLAN — Brand & Polish

Implement the final editorial layer of the ORKI platform, including the home page, brand pages, and site-wide animation system.

## Wave 1: Home Page & Transitions

### 1. Page Transitions (`src/app/[locale]/layout.tsx`)
- Wrap `{children}` in a `motion.div` with `AnimatePresence` and `mode="wait"`.
- Use a premium "lift" preset: `opacity: 0, y: 20` -> `opacity: 1, y: 0`.

### 2. Home Page implementation (`src/app/[locale]/page.tsx`)
- **Hero:** Implement the editorial hero with large-scale typography.
- **Featured Section:** Integration of `ProductCard` into a featured grid.
- **Category Teasers:** High-contrast CTA blocks for Tops and Bottoms.

## Wave 2: Brand Pages

### 3. About Page (`src/app/[locale]/about/page.tsx`)
- Editorial layout with staggered text/image blocks.
- Focus on the "Underground Streetwear" brand story.

### 4. Contact Page (`src/app/[locale]/contact/page.tsx`)
- Minimalist contact form with underline-only inputs.
- Branded WhatsApp CTA.

## Wave 3: Animation Polish

### 5. Scroll Reveals (`src/components/ui/ScrollReveal.tsx`)
- Create a reusable wrapper using `motion` and `whileInView`.
- Apply to headings and product grids.

### 6. Reduced Motion Support
- Implement global check for `prefers-reduced-motion`.
- Ensure page transitions fall back to simple opacity-only fades.

## Wave 4: Final Verification

### 7. Global Audit
- Verify all pages in both EN and AR.
- Check animation performance on mobile.
- Verify cart persistence and checkout flow one last time.
- Generate final project walkthrough.

---
**Status:** Ready to execute Wave 1.
