---
phase: 01-foundation
plan: 07
type: execute
wave: 3
depends_on:
  - 01-05
  - 01-06
files_modified:
  - eslint.config.mjs
  - src/app/[locale]/layout.tsx
  - src/app/[locale]/page.tsx
  - src/components/nav/Navbar.tsx
  - src/components/nav/MobileNavDrawer.tsx
  - src/components/nav/LanguageSwitcher.tsx
  - src/components/footer/Footer.tsx
  - src/components/PlaceholderImage.tsx
autonomous: false
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - NAV-01
  - NAV-02

must_haves:
  truths:
    - "npm run lint exits 0 — no physical directional class violations anywhere in the codebase"
    - "npm run build exits 0 — TypeScript errors and missing translation key errors surface here"
    - "Visiting /en/ shows LTR layout with Space Grotesk font, nav links left-to-right, footer"
    - "Visiting /ar/ shows RTL layout with IBM Plex Arabic font, nav links right-to-left, footer"
    - "PlaceholderImage renders dark editorial treatment at both 3:4 and 4:5 — not a grey box"
    - "Mobile nav drawer slides from right in EN and from left in AR"
    - "Language switcher navigates from /en/ to /ar/ and back without page reload flash"
    - "Layout holds at 375px, 768px, and 1280px+ without horizontal overflow"
    - "No FOUT on page load in either locale"
  artifacts:
    - path: "src/app/[locale]/page.tsx"
      provides: "Verification surface for all 5 Phase 1 success criteria"
      contains: "PlaceholderImage"
    - path: "src/app/[locale]/layout.tsx"
      provides: "lang+dir set correctly for both locales"
      contains: "hasLocale"
  key_links:
    - from: "/en/ URL"
      to: "html[lang=en][dir=ltr]"
      via: "next-intl middleware → layout.tsx → html attributes"
      pattern: "lang.*en.*dir.*ltr"
    - from: "/ar/ URL"
      to: "html[lang=ar][dir=rtl]"
      via: "next-intl middleware → layout.tsx → html attributes"
      pattern: "lang.*ar.*dir.*rtl"
---

<objective>
Run the full lint + build gate and conduct browser-based spot checks across both locales and all three breakpoints to confirm all Phase 1 success criteria are met.

Purpose: Phase 1 requirements FOUND-01 through FOUND-05, NAV-01, and NAV-02 cannot be verified by automated unit tests — they require visual browser inspection. This plan is the formal verification gate before Phase 1 is marked complete and Phase 2 begins.

Output: All 5 Phase 1 success criteria confirmed in both /en/ and /ar/ at 375px, 768px, and 1280px+. Ready for /gsd-verify-work.
</objective>

<execution_context>
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-VALIDATION.md

<interfaces>
<!-- Phase 1 success criteria from ROADMAP.md -->
SC-1: /en/ → /ar/ URL switch rotates entire layout to RTL using CSS logical properties
SC-2: EN and AR fonts render without FOUT on first load
SC-3: PlaceholderImage renders dark editorial at 3:4 and 4:5 (never grey box)
SC-4: Nav shows Tops, Bottoms, About, language switcher; Footer shows Shipping, Returns, Contact
SC-5: All pages intact at 375px, 768px, 1280px+

<!-- Manual test procedure from VALIDATION.md -->
FOUND-01/02: Open /ar/ in browser; inspect <html dir="rtl" lang="ar">; confirm nav right-to-left
FOUND-03: DevTools Network → Slow 3G → reload; confirm no flash of unstyled text
FOUND-04: Inspect 3:4 and 4:5 slots; confirm dark editorial background visible
FOUND-05: DevTools Device Mode → 375px, 768px, 1280px — no horizontal overflow
NAV-01: Test language switcher in both locales; test mobile drawer direction
NAV-02: Footer visible and correct in both locales
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full lint + build gate and fix any remaining issues</name>
  <files>
    eslint.config.mjs
    src/app/[locale]/layout.tsx
    src/app/[locale]/page.tsx
    src/components/nav/Navbar.tsx
    src/components/nav/MobileNavDrawer.tsx
    src/components/nav/LanguageSwitcher.tsx
    src/components/footer/Footer.tsx
    src/components/PlaceholderImage.tsx
  </files>

  <read_first>
    - C:\dev\Antigravity\ORKI\.planning\phases\01-foundation\01-VALIDATION.md (Wave 0 Requirements checklist — verify all items are checked off)
  </read_first>

  <action>
Run the full automated gate:

```bash
cd C:/dev/Antigravity/ORKI
npm run lint
npm run build
```

If lint fails: identify the exact error. Common issues at this stage:
- Physical directional class in a component (`ml-`, `mr-`, `pl-`, `pr-`) — replace with logical equivalent (`ms-`, `me-`, `ps-`, `pe-`)
- `<img>` tag somewhere — replace with `next/image`
- Import from `framer-motion` — replace with `motion/react`
- Missing translation key — add to both `en.json` and `ar.json`

If build fails: identify the TypeScript error. Common issues:
- `params` not awaited in layout.tsx (`await params` required in Next.js 15)
- Wrong import path (case-sensitive on Linux/Vercel even if Windows is lenient)
- `getRequestConfig` called with wrong argument shape (must use `requestLocale` not `locale`)
- Missing `'use client'` on a component that uses hooks

Fix all errors until both `npm run lint` and `npm run build` exit 0. The files listed above are the expected fix targets — modify only the specific file(s) where the issue is found.

After green gate, start the dev server:
```bash
npm run dev
```

Confirm the server starts without errors and is reachable at http://localhost:3000.
  </action>

  <verify>
    <automated>cd C:/dev/Antigravity/ORKI && npm run lint && npm run build 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - `npm run lint` exits 0 with zero errors and zero warnings
    - `npm run build` exits 0 (no TypeScript errors, no missing translation keys, no module resolution errors)
    - `npm run dev` starts without crashing (server running message appears)
  </acceptance_criteria>

  <done>npm run lint exits 0. npm run build exits 0. Dev server starts cleanly. Ready for browser verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser verification — RTL, fonts, placeholders, nav, footer, responsive</name>

  <what-built>
    Plans 01-06 collectively built:
    - Next.js 15 scaffold with Tailwind v4, PostCSS, ESLint (physical class ban + no-img-element)
    - next-intl v4 routing (middleware.ts, i18n/routing.ts, messages/en.json + ar.json)
    - TypeScript domain types (Product, CartItem, Locale, Direction)
    - shadcn initialized with Sheet, NavigationMenu, Separator components
    - Font config (Space Grotesk + IBM Plex Arabic via next/font, variable option)
    - Root layout with lang+dir atomic update, dark class, NextIntlClientProvider
    - globals.css with Tailwind v4, @theme inline, :lang() font switching, Arabic size adjustment
    - PlaceholderImage component (dark-field editorial, 3:4 and 4:5 aspect ratios, next/image wired)
    - animation-presets.ts and useDirection hook
    - Navbar (server), LanguageSwitcher (client), MobileNavDrawer (Sheet, direction-aware)
    - Footer (server, policy links + copyright via t('copyright'))
    - Placeholder home page with 4:5 hero + 3:4 catalog grid
  </what-built>

  <how-to-verify>
Run `npm run dev` in the project directory. Then verify each criterion:

**Criterion 1 — RTL layout switching (FOUND-01, FOUND-02)**
1. Open http://localhost:3000 — it should redirect to http://localhost:3000/en/
2. Inspect `<html>` element in DevTools → confirm: `lang="en"` and `dir="ltr"`
3. Click the EN/AR language switcher button in the nav
4. URL changes to http://localhost:3000/ar/
5. Inspect `<html>` → confirm: `lang="ar"` and `dir="rtl"`
6. Nav links now flow from RIGHT to LEFT
7. Footer links now flow from RIGHT to LEFT
8. Mobile drawer: open it in /ar/ — should slide in from the LEFT (inline-end in RTL)

PASS if: Both lang and dir update on locale switch. Nav RTL renders correctly.

**Criterion 2 — Font rendering without FOUT (FOUND-03)**
1. Open DevTools → Network tab
2. Set throttling to "Slow 3G"
3. Hard reload the /en/ page
4. Watch the page render — text should appear styled immediately (no flash to system font)
5. Repeat for /ar/ — IBM Plex Arabic should be in use (look for Arabic character shapes)

PASS if: No visible flash of unstyled text in either locale.

**Criterion 3 — PlaceholderImage editorial treatment (FOUND-04)**
1. On the home page (/en/ or /ar/)
2. Scroll to the image slots
3. The above-fold slot should show: near-black (#0A0A0A) background, centered "ORKI" ghost mark at low opacity
4. The 3:4 grid cards below should show the same treatment
5. NO grey boxes. NO empty containers. NO spinners. NO alt text showing as text.

PASS if: All image slots show the dark editorial treatment with ORKI ghost mark.

**Criterion 4 — Navigation and Footer (NAV-01, NAV-02)**
1. Desktop (1280px+): Nav should show: ORKI wordmark | [Tops] [Bottoms] [About] | [EN/AR]
2. Mobile (375px): Nav should show: ORKI wordmark | [EN/AR] [hamburger icon]
3. Click hamburger: drawer slides in from right (EN) — shows Tops, Bottoms, About links
4. Footer: should show Shipping, Returns, Contact links + copyright
5. Switch to /ar/: footer copyright shows "جميع الحقوق محفوظة" and ORKI in Latin

PASS if: All nav links present, language switcher works, footer complete in both locales.

**Criterion 5 — Responsive breakpoints (FOUND-05)**
Use DevTools Device Mode to test these three widths:

1. 375px (iPhone SE):
   - No horizontal scroll bar
   - Nav: hamburger visible, no nav links visible
   - Image grid: 2 columns, cards properly proportioned

2. 768px (tablet):
   - No horizontal scroll bar
   - Nav: should show desktop OR hamburger (acceptable either way at 768px)

3. 1280px+ (desktop):
   - No horizontal scroll bar
   - Nav: full horizontal nav visible (Tops, Bottoms, About + language switcher)
   - Image grid: 4 columns

PASS if: No layout breakage or horizontal scroll at any breakpoint.
  </how-to-verify>

  <resume-signal>
Type "approved" if all 5 criteria pass.
If any criterion fails, describe which one and what you observed — include the specific issue (e.g., "drawer slides from wrong edge in AR" or "grey box instead of dark placeholder"). The plan will be revised to fix the issue before proceeding.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js dev server | Local development — no external network exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | Dev server on localhost | accept | Dev server runs locally; no network exposure; development-only risk |
</threat_model>

<verification>
Phase 1 verification gate:

```bash
cd C:/dev/Antigravity/ORKI
npm run lint    # Must exit 0
npm run build   # Must exit 0
```

Plus human browser verification of all 5 Phase 1 success criteria from ROADMAP.md.
</verification>

<success_criteria>
- `npm run lint` exits 0
- `npm run build` exits 0
- SC-1: /en/ → /ar/ switches entire layout direction (lang + dir on html update atomically)
- SC-2: No FOUT on first load in EN or AR locale
- SC-3: PlaceholderImage shows dark editorial treatment (not grey box) at 3:4 and 4:5
- SC-4: Nav has Tops, Bottoms, About, language switcher; Footer has Shipping, Returns, Contact
- SC-5: No layout breakage at 375px, 768px, or 1280px+
- Mobile drawer: slides from right in EN, from left in AR
- Human approves: types "approved" in resume signal
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-07-SUMMARY.md` following the summary template.

Update `.planning/STATE.md`: Phase 1 status from "Not Started" to "Complete".
Update `.planning/ROADMAP.md`: Phase 1 checkboxes from `[ ]` to `[x]`.
</output>
