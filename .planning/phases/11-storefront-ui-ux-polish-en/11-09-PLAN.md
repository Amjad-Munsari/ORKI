---
phase: 11-storefront-ui-ux-polish-en
plan: 09
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/app/[locale]/about/page.tsx
autonomous: true
requirements: [SC-1, SC-6]
tags: [about, typography, body-rhythm]

must_haves:
  truths:
    - "About body sections use ONE body style (`font-normal text-base leading-relaxed`) and ONE pull-quote style (`font-light text-2xl tracking-tight`); no other body weight/size variants on the page."
    - "About uses `display-2` on the main `ORKI / THE STORY` header (already in Plan 11-01)."
    - "About EN page keeps the `01 — Vision / 02 — Heritage / 03 — Quality` numbering (per D-09 / audit F-Copy-2 EN-side decision)."
    - "About placeholder fields use `bg-[var(--color-placeholder-bg)]` and `bg-[var(--color-secondary-surface)]` (already in Plan 11-01)."
  artifacts:
    - path: "src/app/[locale]/about/page.tsx"
      provides: "About page with locked body/pull-quote rhythm"
      contains: "font-normal text-base leading-relaxed"
  key_links:
    - from: "src/app/[locale]/about/page.tsx"
      to: "Plan 11-01 globals.css display-2"
      via: "shared display-2 utility on the header"
      pattern: "display-2"
---

<objective>
Lock About body-text rhythm to ONE body style + ONE pull-quote style per D-09. Closes F-Typo-2 (body-copy weight inconsistent, audit lines 121-123) and F-Copy-2 (EN keeps the 01/02/03 numbering, audit line 64-66).

Purpose: Closes F-Typo-2, F-Copy-2 (EN side).
Output: About page reads with consistent editorial rhythm.
</objective>

<execution_context>
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/dev/Antigravity/ORKI/.planning/STOREFRONT-UI-REVIEW-EN.md
@C:/dev/Antigravity/ORKI/.planning/phases/11-storefront-ui-ux-polish-en/11-CONTEXT.md (D-09)
@C:/dev/Antigravity/ORKI/.planning/phases/11-storefront-ui-ux-polish-en/11-01-SUMMARY.md

<interfaces>
**Body style (D-09):** `font-normal text-base leading-relaxed text-white/80` — applied to body copy in every section.
**Pull-quote style (D-09):** `font-light text-2xl tracking-tight text-white leading-tight` — used 1-2x per section MAX, never more.

**Current state after Plan 11-01:**
- Line 29 h1: `display-2 font-bold uppercase text-white` ✓
- Line 40 first body `<p>`: `text-2xl md:text-4xl font-bold uppercase tracking-tight text-white leading-tight` (Vision section — this is the dominant pull-quote per D-09, KEEP one of these per section).
- Line 60 second body `<p>`: `text-lg text-white/60 leading-relaxed` (Heritage body — needs to flip to canonical body style).
- Line 82 third body `<p>`: `text-lg text-white/60 leading-relaxed` (Quality body — needs to flip to canonical body style).

**Audit's "body inconsistent" finding:** lines 40 (giant uppercase) vs 60/82 (small `text-lg text-white/60`). Per D-09 these have different ROLES — line 40 is a pull-quote (allowed, intentional), but 60 and 82 are body and must use a single locked style. So: KEEP line 40 as pull-quote (audit said "one body + one pull-quote", not "kill the pull-quote"). Convert 60 and 82 to the canonical body style.

**01/02/03 numbering (D-09 + F-Copy-2):** Already present in EN at lines 38 (`01 — Vision`), 58 (`02 — Heritage`), 80 (`03 — Quality`). Keep on EN. AR side defers to Phase 999.11 (no change here).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock About body + pull-quote rhythm per D-09; keep EN 01/02/03 numbering</name>
  <read_first>
    - src/app/[locale]/about/page.tsx (entire file)
    - .planning/phases/11-storefront-ui-ux-polish-en/11-CONTEXT.md D-09
    - .planning/STOREFRONT-UI-REVIEW-EN.md lines 64-66, 121-123
  </read_first>
  <files>src/app/[locale]/about/page.tsx</files>
  <action>
**Step 1 — Vision section pull-quote (lines 40-44):**
Audit-aligned: this is the legitimate pull-quote (one per section is the D-09 cap). Keep the existing className but slightly normalise it to the canonical pull-quote style. Replace the className:

From:
```
className="text-2xl md:text-4xl font-bold uppercase tracking-tight text-white leading-tight"
```

To:
```
className="font-light text-2xl md:text-4xl tracking-tight text-white leading-tight"
```

Rationale: D-09 specifies `font-light` for pull-quote (not `font-bold`). The size escalation at md breakpoint stays — pull-quotes can scale. Uppercase removed because D-09 pull-quote spec is "tracking-tight", which combined with display-light reads better mixed-case for body emphasis. If executor disagrees, KEEP uppercase but still flip to `font-light` — uppercase is a stylistic call, font-light is the contract.

**Step 2 — Heritage section body (lines 60-64):**
Replace:
```
className="text-lg text-white/60 leading-relaxed"
```
With:
```
className="font-normal text-base leading-relaxed text-white/80"
```

**Step 3 — Quality section body (lines 82-86):**
Replace:
```
className="text-lg text-white/60 leading-relaxed"
```
With:
```
className="font-normal text-base leading-relaxed text-white/80"
```

**Step 4 — Verify EN numbering retained:**
Lines 38, 58, 80 each contain the EN string with `01 — `, `02 — `, `03 — `. Confirm grep finds all three.

DO NOT touch the placeholder `<div>` blocks at lines 47-54 and 69-76 — Plan 11-01 already swapped their hex literals to CSS vars.
DO NOT touch the eyebrow `<span>` styling.
DO NOT touch any AR copy strings.
DO NOT modify the grid layout, gap, or container.
  </action>
  <verify>
    <automated>grep -c "font-normal text-base leading-relaxed text-white/80" src/app/[locale]/about/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "font-normal text-base leading-relaxed text-white/80" src/app/[locale]/about/page.tsx` returns 2 (Heritage + Quality bodies).
    - `grep -c "font-light text-2xl" src/app/[locale]/about/page.tsx` returns 1 (Vision pull-quote).
    - `grep -c "text-lg text-white/60 leading-relaxed" src/app/[locale]/about/page.tsx` returns 0 (old body style fully replaced).
    - `grep -c "01 — Vision" src/app/[locale]/about/page.tsx` returns 1.
    - `grep -c "02 — Heritage" src/app/[locale]/about/page.tsx` returns 1.
    - `grep -c "03 — Quality" src/app/[locale]/about/page.tsx` returns 1.
    - `grep -c "display-2 font-bold uppercase text-white" src/app/[locale]/about/page.tsx` returns 1 (header still uses display-2 from Plan 11-01).
    - `npx tsc --noEmit` exits 0.
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <done>About page ships with one body style + one pull-quote style + EN 01/02/03 numbering retained. AR-side numbering choice deferred to Phase 999.11.</done>
</task>

</tasks>

<verification>
- F-Typo-2 closed: ONE body style (`font-normal text-base leading-relaxed text-white/80`) + ONE pull-quote style (`font-light text-2xl`).
- F-Copy-2 closed for EN: numbering retained.
- AR-side numbering decision deferred to Phase 999.11 (documented in CONTEXT.md D-09; not in scope here).
- Plan 11-16 visually verifies rhythm at 1440px + 768px + 375px.
</verification>

<success_criteria>
SC-1 (every [OPEN] finding closed) — partial: F-Typo-2, F-Copy-2 (EN).
SC-6 (no new TS/lint errors) — full.
</success_criteria>

<output>
Create `.planning/phases/11-storefront-ui-ux-polish-en/11-09-SUMMARY.md` recording: exact body/pull-quote classNames, the uppercase-vs-not call made on the Vision pull-quote, and confirmation that EN numbering survived.
</output>
