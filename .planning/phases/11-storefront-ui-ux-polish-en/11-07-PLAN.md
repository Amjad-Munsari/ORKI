---
phase: 11-storefront-ui-ux-polish-en
plan: 07
type: execute
wave: 2
depends_on: [11-01, 11-03, 11-06]
files_modified:
  - src/app/[locale]/page.tsx
  - src/components/shop/ProductGrid.tsx
autonomous: true
requirements: [SC-1, SC-5, SC-6]
tags: [home, category-splat, scroll-reveal, grid-scale, rtl-flip]

must_haves:
  truths:
    - "Home category splats (Tops, Bottoms) render a typographic-only treatment: massive kerned wordmark of the category name on a near-black field — no `bg-white/10` wash, no real image dependency."
    - "Home Featured-Drops grid uses the SAME column system as `ProductGrid`: `grid-cols-2 md:grid-cols-3 xl:grid-cols-4` with `gap-x-8 gap-y-16`."
    - "Featured-Drops ScrollReveal stagger is capped: each card uses `delay={(idx % 4) * 0.08}` and ScrollReveal duration shortens to ~500ms."
    - "Brand-ethos `Read Our Story —` decorative em-dash replaced by an `<ArrowRight>` icon with `.rtl-flip` utility (no em-dash literal)."
    - "Brand-ethos `Read Our Story` link has the canonical focus-visible ring."
  artifacts:
    - path: "src/app/[locale]/page.tsx"
      provides: "Home page with typographic category splat, tamed reveals, RTL-aware brand-ethos arrow"
      contains: "rtl-flip"
  key_links:
    - from: "src/app/[locale]/page.tsx Featured-Drops grid"
      to: "src/components/shop/ProductGrid.tsx grid columns"
      via: "shared `grid-cols-2 md:grid-cols-3 xl:grid-cols-4 gap-x-8 gap-y-16` token"
      pattern: "grid-cols-2 md:grid-cols-3 xl:grid-cols-4"
---

<objective>
Rewrite the Home page to close F-Vis-2 (category splat — D-03 typographic-only treatment), F-Space-1 (unify Featured-Drops + ProductGrid column scale), F-Exp-4 (ScrollReveal waterfall), and F-Exp-7 (em-dash trail).

Purpose: Closes F-Vis-2, F-Space-1, F-Exp-4, F-Exp-7.
Output: Home page reads as intentional editorial design with cohesive rhythm.
</objective>

<execution_context>
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/dev/Antigravity/ORKI/CLAUDE.md
@C:/dev/Antigravity/ORKI/.planning/STOREFRONT-UI-REVIEW-EN.md
@C:/dev/Antigravity/ORKI/.planning/phases/11-storefront-ui-ux-polish-en/11-CONTEXT.md (D-03)

<interfaces>
**Current state (post-Plans-11-01/03/06):**
- `page.tsx:38` outer container uses `max-w-[var(--container-max)]`.
- `page.tsx:43` hero h1 uses `display-4 font-medium text-white`.
- `page.tsx:69` Featured-Drops grid: `grid grid-cols-2 lg:grid-cols-4 gap-x-8 gap-y-20` (audit-flagged — doesn't match ProductGrid).
- `page.tsx:71` per-card ScrollReveal: `delay={idx * 0.1}` (uncapped).
- `page.tsx:95` ethos h3 uses `display-4 font-medium text-white max-w-5xl mx-auto`.
- `page.tsx:101-106` "Read Our Story —" link: ends with literal ` —` text. Trailing decorative em-dash.
- `page.tsx:114-138` CategoryCard component: currently overlays `bg-white/10 group-hover:bg-transparent` wash on top of a faint ORKI wordmark.

**Reference (ProductGrid.tsx line 27 — the shared scale):**
```
grid grid-cols-2 md:grid-cols-3 xl:grid-cols-4 gap-4 md:gap-6
```

Audit recommends bigger gap on home + shop: `gap-x-8 gap-y-16`. Apply that to BOTH files in this plan (Featured-Drops + ProductGrid).

**`.rtl-flip` utility** already exists in `globals.css:196-198`:
```
[dir="rtl"] .rtl-flip {
  transform: scaleX(-1);
}
```

**`ScrollReveal` component** — read its props before modifying call sites. It accepts `delay` (number, seconds) and `duration` (number, optional). Current usage in `page.tsx:71` only passes `delay`. To tighten duration to ~500ms, pass `duration={0.5}` if the prop exists.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inspect ScrollReveal props, unify Featured-Drops grid + tame stagger</name>
  <read_first>
    - src/components/ui/ScrollReveal.tsx (entire file — confirm props)
    - src/components/shop/ProductGrid.tsx (line 27 — current shared scale)
    - src/app/[locale]/page.tsx (lines 53-76 — Featured-Drops block)
  </read_first>
  <files>
    src/app/[locale]/page.tsx,
    src/components/shop/ProductGrid.tsx
  </files>
  <action>
**Step 1 — Read ScrollReveal.tsx:** confirm what props it accepts. If it accepts `duration` (number), use it in step 3. If not, document in the summary that duration is fixed in the ScrollReveal primitive and we'll tame only stagger.

**Step 2 — `page.tsx` Featured-Drops grid (line 69):**
Replace:
```
<div className="grid grid-cols-2 lg:grid-cols-4 gap-x-8 gap-y-20">
```
With:
```
<div className="grid grid-cols-2 md:grid-cols-3 xl:grid-cols-4 gap-x-8 gap-y-16">
```

**Step 3 — `page.tsx` per-card ScrollReveal (line 71):**
Replace:
```
<ScrollReveal key={product.id} delay={idx * 0.1}>
```
With (if ScrollReveal accepts `duration`):
```
<ScrollReveal key={product.id} delay={(idx % 4) * 0.08} duration={0.5}>
```
Or (if ScrollReveal does NOT accept `duration`):
```
<ScrollReveal key={product.id} delay={(idx % 4) * 0.08}>
```
Whichever is type-safe per the props you read in Step 1.

**Step 4 — `ProductGrid.tsx` line 27:**
Replace:
```
<div className="grid grid-cols-2 md:grid-cols-3 xl:grid-cols-4 gap-4 md:gap-6">
```
With:
```
<div className="grid grid-cols-2 md:grid-cols-3 xl:grid-cols-4 gap-x-8 gap-y-16">
```

This unifies the column scale across Home Featured-Drops + Shop product grid (F-Space-1).

DO NOT touch the ProductGrid empty-state copy (Plan 11-14 owns that).
DO NOT touch the ProductGrid stagger delay (already capped at `(i % 4) * 0.1`).
  </action>
  <verify>
    <automated>grep -c "grid-cols-2 md:grid-cols-3 xl:grid-cols-4 gap-x-8 gap-y-16" src/app/[locale]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "grid-cols-2 md:grid-cols-3 xl:grid-cols-4 gap-x-8 gap-y-16" src/app/[locale]/page.tsx` returns 1.
    - `grep -c "grid-cols-2 md:grid-cols-3 xl:grid-cols-4 gap-x-8 gap-y-16" src/components/shop/ProductGrid.tsx` returns 1.
    - `grep -c "delay={(idx % 4) \* 0.08}" src/app/[locale]/page.tsx` returns 1.
    - `grep -c "delay={idx \* 0.1}" src/app/[locale]/page.tsx` returns 0 (uncapped form gone).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Featured-Drops + ProductGrid share the same column system; home stagger capped at 4-card cycle of 0.08s.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite home category splat to typographic-only treatment (D-03)</name>
  <read_first>
    - src/app/[locale]/page.tsx (lines 78-138 — Category Splat section + CategoryCard)
    - .planning/phases/11-storefront-ui-ux-polish-en/11-CONTEXT.md D-03
  </read_first>
  <files>src/app/[locale]/page.tsx</files>
  <action>
The current `CategoryCard` (lines 114-138) renders a faint ORKI wordmark, then overlays a `bg-white/10` wash that fades on hover. Per D-03, replace the entire visual treatment with a typographic-only block: the CATEGORY NAME (Tops/Bottoms/Arabic equivalent) renders as a massive kerned wordmark filling the splat. No bg-white wash. No ORKI wordmark behind. Subtle hover scale.

Replace the `function CategoryCard` body (lines 114-138) with:

```tsx
function CategoryCard({ title, href, locale }: { title: string; href: string; locale: string }) {
  const isRtl = locale === 'ar'
  return (
    <Link
      href={href}
      className="group relative h-full w-full overflow-hidden flex items-end p-12 md:p-16 bg-[var(--color-placeholder-bg)] focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-white focus-visible:ring-offset-2 focus-visible:ring-offset-black"
    >
      {/* Massive kerned wordmark of the category title — D-03 typographic-only treatment.
          Sits behind the foreground Shop-Now block; subtle scale on group-hover. */}
      <div
        className="absolute inset-0 flex items-center justify-center pointer-events-none select-none overflow-hidden transition-transform duration-1000 group-hover:scale-[1.03]"
        aria-hidden="true"
      >
        <span
          className="font-bold uppercase leading-none tracking-[-0.04em]"
          style={{
            color: 'rgba(255, 255, 255, 0.08)',
            fontSize: '22vw',
          }}
        >
          {title}
        </span>
      </div>

      {/* Foreground action row — title (smaller, readable) + Shop Now button. */}
      <div className="relative z-10 w-full flex flex-col md:flex-row md:items-end justify-between gap-8">
        <h3 className="text-5xl md:text-8xl font-medium text-white leading-tight">
          {title}
        </h3>
        <div className="bg-black text-white px-8 py-4 text-sm font-semibold flex items-center gap-3 transition-transform duration-300 group-hover:translate-x-2">
          {isRtl ? 'تسوق الآن' : 'Shop Now'}
          {isRtl ? <ArrowLeft className="size-4" /> : <ArrowRight className="size-4" />}
        </div>
      </div>
    </Link>
  )
}
```

**Notes on the change:**
- The `bg-white/10` overlay div is removed.
- The huge background wordmark is now the category title (Tops / Bottoms / Arabic), not "ORKI".
- The near-black field uses `bg-[var(--color-placeholder-bg)]` (no more bespoke `#0d0d0d`).
- Hover gets a 3% scale on the wordmark layer (audit said "subtle hover scale").
- The visible H3 keeps the category title at readable size for screen readers (the giant wordmark is `aria-hidden`).
- Focus-visible ring added to the Link (was missing — F-Exp-1 follow-through).
  </action>
  <verify>
    <automated>grep -c "bg-white/10" src/app/[locale]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bg-white/10" src/app/[locale]/page.tsx` returns 0 (overlay wash gone).
    - `grep -c "fontSize: '22vw'" src/app/[locale]/page.tsx` returns 1 (typographic-only treatment).
    - `grep -c "bg-\[var(--color-placeholder-bg)\]" src/app/[locale]/page.tsx` returns >= 2 (hero bg + category card bg).
    - `grep -c "ORKI" src/app/[locale]/page.tsx` returns 1 (only the hero background wordmark, not in CategoryCard anymore).
    - `grep -c "focus-visible:ring-offset-black" src/app/[locale]/page.tsx` returns >= 1.
    - `npx tsc --noEmit` exits 0.
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <done>CategoryCard renders typographic-only — category title as both the giant wordmark and the readable H3 + Shop Now button. Hover: subtle scale on the wordmark layer.</done>
</task>

<task type="auto">
  <name>Task 3: Replace brand-ethos em-dash with rtl-flip ArrowRight icon + focus ring</name>
  <read_first>
    - src/app/[locale]/page.tsx (lines 92-109 — brand-ethos section)
    - src/app/globals.css (lines 193-198 — `.rtl-flip` utility confirmation)
  </read_first>
  <files>src/app/[locale]/page.tsx</files>
  <action>
Locate the brand-ethos block (lines 100-107). Replace the existing Link:

```tsx
<Link 
  href="/about"
  className="text-sm font-medium text-white/40 hover:text-white transition-colors"
>
  {isRtl ? 'اقرأ قصتنا' : 'Read Our Story'} —
</Link>
```

With:

```tsx
<Link
  href="/about"
  className="inline-flex items-center gap-2 text-sm font-medium text-white/40 hover:text-white transition-colors focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-white focus-visible:ring-offset-2 focus-visible:ring-offset-black"
>
  {isRtl ? 'اقرأ قصتنا' : 'Read Our Story'}
  <ArrowRight className="size-4 rtl-flip" />
</Link>
```

Key changes:
- Trailing `—` literal removed.
- `<ArrowRight class="rtl-flip">` icon added; the `.rtl-flip` utility (already in globals.css) scaleX(-1) flips it in RTL contexts via the `[dir="rtl"]` attribute on `<html>`.
- `inline-flex items-center gap-2` added so the icon sits cleanly next to the text.
- Canonical focus-visible ring added.

`ArrowRight` is already imported at the top of `page.tsx` (line 7). No new imports needed.

DO NOT touch the `<h3>` brand-ethos heading copy — Plan 11-15 surfaces 3 candidates as commented alternatives for UAT selection.
  </action>
  <verify>
    <automated>grep -c "rtl-flip" src/app/[locale]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rtl-flip" src/app/[locale]/page.tsx` returns 1.
    - `grep -c "Read Our Story.*—" src/app/[locale]/page.tsx` returns 0 (em-dash trail gone).
    - `grep -c "ArrowRight className=\"size-4 rtl-flip\"" src/app/[locale]/page.tsx` returns 1.
    - `grep -c "focus-visible:ring-offset-black" src/app/[locale]/page.tsx` returns >= 2 (CategoryCard + Read Our Story).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Brand-ethos link shows a direction-aware arrow icon; em-dash gone; link has focus ring.</done>
</task>

</tasks>

<verification>
- F-Vis-2 closed: typographic-only category splat (D-03).
- F-Space-1 closed: shared grid scale (cols-2/3/4, gap-x-8, gap-y-16) on Home + Shop.
- F-Exp-4 closed: stagger capped at `(idx % 4) * 0.08`, duration tightened if ScrollReveal supports it.
- F-Exp-7 closed: em-dash → rtl-flip arrow.
- Plan 11-16 will verify visually at 1440px, 1280px, 768px, 375px viewports + RTL.
</verification>

<success_criteria>
SC-1 (every [OPEN] finding closed) — partial: F-Vis-2, F-Space-1, F-Exp-4, F-Exp-7.
SC-5 (Featured-Drops degrades gracefully — reduced-motion respected) — advanced; ScrollReveal already uses motion which respects prefers-reduced-motion via Motion library default.
SC-6 (no new TS/lint errors) — full.
</success_criteria>

<output>
Create `.planning/phases/11-storefront-ui-ux-polish-en/11-07-SUMMARY.md` capturing: ScrollReveal props snapshot, exact grid class strings, CategoryCard before/after diff, em-dash removal proof, and grep verification results.
</output>
