---
phase: 11-storefront-ui-ux-polish-en
plan: 13
type: execute
wave: 3
depends_on: []
files_modified:
  - messages/en.json
autonomous: true
requirements: [SC-1, SC-6]
tags: [copy, i18n, 404, uat-candidates]

must_haves:
  truths:
    - "`messages/en.json` `Errors.notFound.*` contains the LIVE selected EN copy (one of the three candidates)."
    - "Three EN copy candidates are visible in the file as a commented block above the `Errors.notFound` key so UAT (Plan 11-16) can swap the live values to whichever candidate the user picks."
    - "AR keys at `Errors.notFound.*` are left as-is (Phase 999.11 owns AR review)."
  artifacts:
    - path: "messages/en.json"
      provides: "EN 404 voice — 3 candidates + selected live value"
      contains: "404"
  key_links:
    - from: "src/app/[locale]/not-found.tsx"
      to: "messages/en.json Errors.notFound.*"
      via: "getTranslations('Errors.notFound')"
      pattern: "Errors.notFound"
---

<objective>
Draft 3 EN 404-voice candidates per D-08. Wire one as the live value. Plan 11-16 surfaces the candidates to the user during live-dev; the user picks; this plan's commented block becomes the swap reference. Closes F-Exp-8 / cosmetic 404 finding.

Purpose: Closes F-Exp-8 (404 voice).
Output: 404 page reads in ORKI's editorial-confident voice in EN.
</objective>

<execution_context>
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/dev/Antigravity/ORKI/CLAUDE.md (voice anchor)
@C:/dev/Antigravity/ORKI/.planning/STOREFRONT-UI-REVIEW-EN.md
@C:/dev/Antigravity/ORKI/.planning/phases/11-storefront-ui-ux-polish-en/11-CONTEXT.md (D-08 + voice anchor)

<interfaces>
**Voice anchor (CONTEXT D-05 + specifics):**
Hero copy is "Made in the noise of Riyadh. / Wear what the city sounds like." Short, declarative, sensory, Riyadh-specific. Editorial-confident.

**Current `Errors.notFound` in messages/en.json (lines 415-420):**
```json
"notFound": {
  "heading": "404 — Lost in the noise.",
  "body": "This page doesn't exist or was moved. Try the shop instead.",
  "browseShop": "Browse shop",
  "returnHome": "Return home"
}
```

The audit (line 184) flagged the EN copy as cosmetic-only, "unaudited tone" — a brand-voice rewrite is in order with 3 candidates.

**Three candidates to draft (the existing copy is candidate A, lightly tightened):**

**Candidate A — current-voice baseline (declarative + sensory):**
- heading: `404 / NO DROP HERE`
- body: `That page isn't on the rack. The shop is.`

**Candidate B — Riyadh-locative:**
- heading: `404 / OFF THE MAP`
- body: `This street doesn't run through ORKI. Take the shop instead.`

**Candidate C — stripped-minimal:**
- heading: `404`
- body: `Page not here. Shop is.`

CTA labels stay constant across all three (`Browse shop` / `Return home`) — only heading + body vary.

**Selected live value:** Candidate A. Plan 11-16 swaps to user's choice if different.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update messages/en.json Errors.notFound with Candidate A + commented candidates above</name>
  <read_first>
    - messages/en.json (Errors.notFound section — lines 410-425)
    - .planning/phases/11-storefront-ui-ux-polish-en/11-CONTEXT.md D-08 + voice anchor
  </read_first>
  <files>messages/en.json</files>
  <action>
JSON does NOT support inline comments. To preserve the candidate options in the codebase without breaking parsing, embed them as sibling string keys under `Errors.notFound._candidates`. Plan 11-16 reads these and the executor renames the active one + deletes the rest.

Locate the `"notFound"` object in `messages/en.json` (currently lines 415-420). Replace the entire object with:

```json
"notFound": {
  "heading": "404 / NO DROP HERE",
  "body": "That page isn't on the rack. The shop is.",
  "browseShop": "Browse shop",
  "returnHome": "Return home",
  "_candidates": {
    "_comment": "Phase 11 D-08 — UAT picks one in Plan 11-16. Replace live heading+body above with the chosen candidate's values, then delete this _candidates key.",
    "A_voice": { "heading": "404 / NO DROP HERE", "body": "That page isn't on the rack. The shop is." },
    "B_locative": { "heading": "404 / OFF THE MAP", "body": "This street doesn't run through ORKI. Take the shop instead." },
    "C_minimal": { "heading": "404", "body": "Page not here. Shop is." }
  }
}
```

**Critical:** Keep the live `heading` + `body` keys at the top so `getTranslations('Errors.notFound').heading` still resolves — the `_candidates` sub-object is dead weight for runtime but visible to humans. After UAT in Plan 11-16, the executor renames the chosen candidate's heading/body to the top-level live values and removes `_candidates`.

DO NOT modify the AR `Errors.notFound` section in `messages/ar.json` — Phase 999.11 owns AR voice review.

If JSON has a trailing-comma rule or strict parser, validate after edit. Tools: `npx jsonlint messages/en.json` if available, OR check with `node -e "require('./messages/en.json')"` (which throws if malformed).
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('messages/en.json','utf8'))"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('messages/en.json','utf8'))"` exits 0 (valid JSON).
    - `node -e "const m = require('./messages/en.json'); if (!m.Errors.notFound.heading || !m.Errors.notFound.body) process.exit(1)"` exits 0.
    - `node -e "const m = require('./messages/en.json'); if (!m.Errors.notFound._candidates.A_voice) process.exit(1)"` exits 0.
    - `grep -c "_candidates" messages/en.json` returns >= 1.
    - `npx tsc --noEmit` exits 0.
    - `npm run build` exits 0 (build doesn't choke on the extra _candidates key).
    - Visit /en/non-existent path during Plan 11-16 — 404 page shows Candidate A copy.
  </acceptance_criteria>
  <done>Live EN 404 copy uses Candidate A voice; B and C live as sibling keys for UAT selection. AR untouched.</done>
</task>

</tasks>

<verification>
- F-Exp-8 closed (provisional): EN 404 page reads in brand voice. Plan 11-16 finalises the candidate choice.
- AR 404 voice deferred to Phase 999.11 (CONTEXT D-08).
</verification>

<success_criteria>
SC-1 (every [OPEN] finding closed) — partial: F-Exp-8.
SC-6 (no new TS/lint errors) — full.
</success_criteria>

<output>
Create `.planning/phases/11-storefront-ui-ux-polish-en/11-13-SUMMARY.md` listing the 3 candidate strings, the live default (A), and the UAT instruction for Plan 11-16 (rename the chosen candidate to the top-level live values and delete `_candidates`).
</output>
