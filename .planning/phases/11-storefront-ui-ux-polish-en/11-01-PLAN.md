---
phase: 11-storefront-ui-ux-polish-en
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/[locale]/page.tsx
  - src/app/[locale]/about/page.tsx
  - src/app/[locale]/contact/ContactClient.tsx
  - src/components/footer/Footer.tsx
  - src/components/PlaceholderImage.tsx
  - src/components/shop/ProductGrid.tsx
  - src/app/[locale]/shop/page.tsx
autonomous: true
requirements: [SC-1, SC-6]
tags: [design-tokens, typography, css-variables]

must_haves:
  truths:
    - "All bespoke display font sizes (text-[90px], text-[100px], text-[120px], text-[160px]) replaced by a shared display-1/2/3/4 scale."
    - "All near-black hex literals (#050505, #0a0a0a, #0d0d0d, #111111) on storefront surfaces resolve to two CSS variables: --color-placeholder-bg and --color-secondary-surface."
    - "All storefront page containers use a single --container-max width token (max-w-[1440px])."
  artifacts:
    - path: "src/app/globals.css"
      provides: "display-1..display-4 utility classes + --container-max var + reaffirmed --color-placeholder-bg/--color-secondary-surface"
      contains: ".display-1"
  key_links:
    - from: "src/app/[locale]/page.tsx"
      to: "src/app/globals.css"
      via: "display-N utility classes + max-w-[1440px]"
      pattern: "display-[1-4]|max-w-\\[1440px\\]"
---

<objective>
Establish Phase 11 design-token foundation: define the display-size scale, collapse near-black hex literals to existing CSS vars, and unify storefront container width to `max-w-[1440px]`. Every later plan consumes these tokens, so this is Wave 0.

Purpose: Closes F-Typo-1 (display scale), F-Color-3 (hex collapse), F-Space-2 (container width).
Output: A single source of truth for display sizes, near-black, and container width across the storefront.
</objective>

<execution_context>
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/dev/Antigravity/ORKI/CLAUDE.md
@C:/dev/Antigravity/ORKI/.planning/STOREFRONT-UI-REVIEW-EN.md
@C:/dev/Antigravity/ORKI/.planning/phases/11-storefront-ui-ux-polish-en/11-CONTEXT.md

<interfaces>
Existing tokens in `src/app/globals.css` (lines 53-60):
```
--color-dominant: #000000;
--color-secondary-surface: #111111;
--color-accent-orki: #ffffff;
--color-orki-border: rgba(255, 255, 255, 0.12);
--color-placeholder-bg: #0A0A0A;
--color-placeholder-mark: rgba(255, 255, 255, 0.15);
```

Current bespoke literals to remove (audit-cited):
- `text-[90px]` — page.tsx:43 (hero h1)
- `text-[120px]` — about/page.tsx:29
- `text-[100px]` — contact/ContactClient.tsx:29
- `text-[160px]` — footer/Footer.tsx:41
- `text-7xl` (~72px) — page.tsx:95 (ethos heading, kept as display-4)

Current container widths:
- `max-w-[1440px]` — page.tsx:38, page.tsx:54, page.tsx:94, footer/Footer.tsx:36
- `max-w-[1280px]` — shop/page.tsx:77, about/page.tsx:27, contact/ContactClient.tsx:26, ProductGrid.tsx (none — wrapped by parent)

Near-black hex literals scattered:
- `#0a0a0a` — page.tsx:27, PlaceholderImage.tsx:48, about/page.tsx:47
- `#0d0d0d` — page.tsx:118, about/page.tsx:69
- `#050505` — page.tsx:93
- `#111111` — already a CSS var (`--color-secondary-surface`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define display-scale utilities + container-max token in globals.css</name>
  <read_first>
    - src/app/globals.css (entire file — to position new utilities correctly)
    - .planning/STOREFRONT-UI-REVIEW-EN.md lines 117-119 (Typography F-Typo-1 evidence)
  </read_first>
  <files>src/app/globals.css</files>
  <action>
Open `src/app/globals.css`. Inside the existing `@layer utilities` block (currently at line 200, contains `.animate-scroll-indicator`), append the following four display-size utilities (paste verbatim):

```css
  /* Display-size scale — replaces bespoke text-[90px]/[100px]/[120px]/[160px] literals.
     Used on hero H1, About header, Contact header, Footer wordmark. */
  .display-1 {
    font-size: clamp(4rem, 12vw, 10rem); /* 64..160px — Footer wordmark */
    line-height: 0.95;
    letter-spacing: -0.04em;
  }
  .display-2 {
    font-size: clamp(3.5rem, 9vw, 7.5rem); /* 56..120px — About header */
    line-height: 1.0;
    letter-spacing: -0.03em;
  }
  .display-3 {
    font-size: clamp(3rem, 7.5vw, 6.25rem); /* 48..100px — Contact header */
    line-height: 1.05;
    letter-spacing: -0.03em;
  }
  .display-4 {
    font-size: clamp(2.75rem, 7vw, 5.625rem); /* 44..90px — Home hero H1 */
    line-height: 1.1;
    letter-spacing: -0.02em;
  }
```

Then in the same `@theme inline` block (lines 6-60), append BEFORE the closing `}` at line 60:

```css
  /* Storefront container width — single source of truth. All storefront page
     wrappers use max-w-[var(--container-max)] mx-auto. */
  --container-max: 1440px;
```

Do NOT modify any other tokens. Do NOT touch the destructive token. Do NOT remove existing tokens.
  </action>
  <verify>
    <automated>grep -c "display-1\|display-2\|display-3\|display-4" src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^\s*\.display-[1-4]\s*\{" src/app/globals.css` returns exactly 4 lines (one per utility).
    - `grep "container-max:" src/app/globals.css` returns exactly 1 match with value `1440px`.
    - `grep -E "clamp\(" src/app/globals.css` returns at least 4 matches (display utilities use clamp()).
    - File still parses: `npx tsc --noEmit` exits 0 (globals.css is not type-checked, but build must not break).
  </acceptance_criteria>
  <done>globals.css contains 4 display-N utilities and 1 --container-max token; no existing tokens removed.</done>
</task>

<task type="auto">
  <name>Task 2: Apply display utilities + container token to home/about/contact/footer</name>
  <read_first>
    - src/app/[locale]/page.tsx (entire file — multiple replacements)
    - src/app/[locale]/about/page.tsx (line 29 header)
    - src/app/[locale]/contact/ContactClient.tsx (line 29 header)
    - src/components/footer/Footer.tsx (lines 36, 41-43)
    - src/app/[locale]/shop/page.tsx (line 77 container)
  </read_first>
  <files>
    src/app/[locale]/page.tsx,
    src/app/[locale]/about/page.tsx,
    src/app/[locale]/contact/ContactClient.tsx,
    src/components/footer/Footer.tsx,
    src/app/[locale]/shop/page.tsx
  </files>
  <action>
**Replacement 1 — `src/app/[locale]/page.tsx`:**
- Line 43 h1: replace `text-6xl md:text-[90px] font-medium text-white leading-[1.1] tracking-tight` with `display-4 font-medium text-white`.
- Line 95 h3: replace `text-4xl md:text-7xl font-medium text-white leading-tight max-w-5xl mx-auto` with `display-4 font-medium text-white max-w-5xl mx-auto`.
- Line 38 div: replace `max-w-[1440px]` with `max-w-[var(--container-max)]`.
- Line 54 section: replace `max-w-[1440px]` with `max-w-[var(--container-max)]`.
- Line 94 div: replace `max-w-[1440px]` with `max-w-[var(--container-max)]`.
- Line 27 hero bg div: replace `bg-[#0a0a0a]` with `bg-[var(--color-placeholder-bg)]`.
- Line 93 brand-ethos section: replace `bg-[#050505]` with `bg-[var(--color-placeholder-bg)]`.
- Line 118 (inside CategoryCard) div bg: replace `bg-[#0d0d0d]` with `bg-[var(--color-secondary-surface)]`.

**Replacement 2 — `src/app/[locale]/about/page.tsx`:**
- Line 27: replace `max-w-[1280px]` with `max-w-[var(--container-max)]`.
- Line 29 h1: replace `text-6xl md:text-[120px] font-bold uppercase tracking-tighter leading-none text-white` with `display-2 font-bold uppercase text-white`.
- Line 47 div: replace `bg-[#0a0a0a]` with `bg-[var(--color-placeholder-bg)]`.
- Line 69 div: replace `bg-[#0d0d0d]` with `bg-[var(--color-secondary-surface)]`.

**Replacement 3 — `src/app/[locale]/contact/ContactClient.tsx`:**
- Line 26: replace `max-w-[1280px]` with `max-w-[var(--container-max)]`.
- Line 29 h1: replace `text-6xl md:text-[100px] font-bold uppercase tracking-tighter leading-none text-white` with `display-3 font-bold uppercase text-white`.

**Replacement 4 — `src/components/footer/Footer.tsx`:**
- Line 36 div: replace `max-w-[1440px]` with `max-w-[var(--container-max)]`.
- Line 41 h2: replace `text-8xl md:text-[160px] font-bold tracking-[-0.05em] leading-none` with `display-1 font-bold` (tracking now lives in `.display-1`).

**Replacement 5 — `src/app/[locale]/shop/page.tsx`:**
- Line 77 div: replace `max-w-[1280px]` with `max-w-[var(--container-max)]`.

Do NOT modify any AR copy strings. Do NOT touch ProductGrid.tsx grid columns (Plan 11-07 owns that). Do NOT touch the destructive token in StockStateBadge.tsx (Plan 11-11 owns that).
  </action>
  <verify>
    <automated>grep -nE "text-\[(90|100|120|160)px\]" src/app/[locale]/page.tsx src/app/[locale]/about/page.tsx src/app/[locale]/contact/ContactClient.tsx src/components/footer/Footer.tsx | grep -v '^#' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Grep above returns 0 (no remaining bespoke display-px literals on those 4 files).
    - `grep -rn "max-w-\[1280px\]\|max-w-\[1440px\]" src/app src/components` returns 0 matches in production files (excluding node_modules / .next).
    - `grep -c "max-w-\[var(--container-max)\]" src/app/[locale]/page.tsx` returns at least 3.
    - `grep -c "bg-\[var(--color-placeholder-bg)\]\|bg-\[var(--color-secondary-surface)\]" src/app/[locale]/page.tsx src/app/[locale]/about/page.tsx` returns at least 5 matches combined.
    - `grep -nE "bg-\[#0[0-9a-f]{5}\]" src/app/[locale]/page.tsx src/app/[locale]/about/page.tsx | grep -v '^#'` returns 0 matches (all near-black hex literals collapsed).
    - `npx tsc --noEmit` exits 0.
    - `npm run lint` (or `pnpm lint`) exits 0.
  </acceptance_criteria>
  <done>All 5 files use display-N utilities + --container-max + CSS-var near-blacks. No bespoke text-[Npx] or bg-[#0xxxxx] literals remain in the listed files. PlaceholderImage.tsx:48 is intentionally NOT touched here (Plan 11-06 rewrites that component).</done>
</task>

</tasks>

<verification>
- F-Typo-1 closed: zero bespoke display-px literals across hero / about / contact / footer.
- F-Color-3 closed: near-black hex literals collapsed to `--color-placeholder-bg` and `--color-secondary-surface` (canonical: `#0A0A0A` for editorial fields, `#111111` for chrome).
- F-Space-2 closed: every storefront container resolves to `max-w-[var(--container-max)]` = 1440px.
- Build green: `npx tsc --noEmit` + `npm run lint` exit 0.
</verification>

<success_criteria>
SC-1 (every [OPEN] finding closed) — partial: F-Typo-1, F-Color-3, F-Space-2.
SC-6 (no new TS/lint errors) — full.
</success_criteria>

<output>
Create `.planning/phases/11-storefront-ui-ux-polish-en/11-01-SUMMARY.md` recording exact tokens added, container width chosen (1440px), canonical near-black choice (`#0A0A0A` for editorial fields, `#111111` for chrome), and verification grep results.
</output>
