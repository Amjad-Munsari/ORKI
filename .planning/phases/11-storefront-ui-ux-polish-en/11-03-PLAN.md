---
phase: 11-storefront-ui-ux-polish-en
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/nav/Navbar.tsx
  - src/components/nav/LanguageSwitcher.tsx
  - src/components/nav/MobileNavDrawer.tsx
  - src/components/nav/ShopDropdown.tsx
  - src/components/nav/CartTrigger.tsx
  - src/components/shop/ShopHeader.tsx
  - src/app/[locale]/contact/ContactClient.tsx
autonomous: true
requirements: [SC-4, SC-6]
tags: [a11y, focus-visible, wcag]

must_haves:
  truths:
    - "Every storefront interactive element exposes a visible focus ring when focused via keyboard."
    - "The focus ring uses exactly `focus-visible:ring-1 focus-visible:ring-white focus-visible:ring-offset-2 focus-visible:ring-offset-black`."
  artifacts:
    - path: "src/components/nav/Navbar.tsx"
      provides: "Keyboard-focusable nav links + logo"
      contains: "focus-visible:ring-1"
  key_links:
    - from: "Tab key navigation"
      to: "visible white ring"
      via: "Tailwind focus-visible utilities"
      pattern: "focus-visible:ring-1"
---

<objective>
Add the canonical focus-visible ring to every storefront interactive. Closes F-Exp-1 (audit lines 155-157); foundation for SC-4.

Purpose: Closes F-Exp-1.
Output: Keyboard-only users see where focus is on the storefront.
</objective>

<execution_context>
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/dev/Antigravity/ORKI/CLAUDE.md
@C:/dev/Antigravity/ORKI/.planning/STOREFRONT-UI-REVIEW-EN.md

<interfaces>
**Canonical focus-visible string (paste verbatim into existing className, separated by a space):**

    focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-white focus-visible:ring-offset-2 focus-visible:ring-offset-black

`focus-visible:outline-none` is required because globals.css:133 sets `* { outline-ring/50 }`; without it the default half-opacity outline competes with our ring.

**Coverage matrix:**

| Interactive | File | Approx Line | Note |
|---|---|---|---|
| Nav text Links (.map) | Navbar.tsx | 39-46 | |
| Logo Link | Navbar.tsx | 51-56 | |
| Search button | Navbar.tsx | 61-66 | SKIP (Plan 11-04 deletes) |
| LanguageSwitcher button | LanguageSwitcher.tsx | 20-25 | |
| MobileNavDrawer trigger | MobileNavDrawer.tsx | 58-65 | |
| Drawer inner 5 Links | MobileNavDrawer.tsx | 101-137 | shop/tops/bottoms/about/contact |
| ShopDropdown trigger Link | ShopDropdown.tsx | 36-41 | |
| ShopDropdown inner Links | ShopDropdown.tsx | 52-61 | |
| CartTrigger | CartTrigger.tsx | outermost button | Skip if already focus-visible |
| ShopHeader category tabs (3 buttons) | ShopHeader.tsx | 63-77 | |
| ShopHeader native sort select | ShopHeader.tsx | 88-99 | Plan 11-12 replaces; add here for plan-standalone safety |
| Contact WhatsApp anchor | ContactClient.tsx | 39-47 | Form fields skipped (Plan 11-10 deletes form) |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add focus-visible ring to Nav + Drawer + ShopDropdown + LanguageSwitcher + CartTrigger</name>
  <read_first>
    - src/components/nav/Navbar.tsx
    - src/components/nav/LanguageSwitcher.tsx
    - src/components/nav/MobileNavDrawer.tsx
    - src/components/nav/ShopDropdown.tsx
    - src/components/nav/CartTrigger.tsx
  </read_first>
  <files>
    src/components/nav/Navbar.tsx,
    src/components/nav/LanguageSwitcher.tsx,
    src/components/nav/MobileNavDrawer.tsx,
    src/components/nav/ShopDropdown.tsx,
    src/components/nav/CartTrigger.tsx
  </files>
  <action>
For every interactive in the coverage matrix that lives in these 5 files, append the canonical focus string (see interfaces) to its existing className.

**Navbar.tsx:** append to the .map Link className (line ~42) and the logo Link className (line ~53). SKIP the search button (Plan 11-04 deletes it).

**LanguageSwitcher.tsx:** append to the button className at line ~23.

**MobileNavDrawer.tsx:** append to the SheetTrigger render button className (~line 62) and to EACH of the five inner Link className strings (shop, tops, bottoms, about, contact — lines ~104, ~112, ~119, ~127, ~135).

**ShopDropdown.tsx:** append to the trigger Link className (~line 38) and to the inner item Link className (~line 56).

**CartTrigger.tsx:** read the file; locate the outermost focusable element (button/anchor/SheetTrigger). If its className already contains a `focus-visible:` token, leave untouched (record decision in summary). Otherwise append the canonical string.

Do NOT remove or alter hover/transition/animation utilities. Do NOT touch aria-label, onClick, or any handler. Do NOT add ring to non-interactive elements (e.g. wrapper divs).
  </action>
  <verify>
    <automated>grep -c "focus-visible:ring-offset-black" src/components/nav/Navbar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "focus-visible:ring-offset-black" src/components/nav/Navbar.tsx` returns >= 2 (logo + .map links).
    - `grep -c "focus-visible:ring-offset-black" src/components/nav/LanguageSwitcher.tsx` returns >= 1.
    - `grep -c "focus-visible:ring-offset-black" src/components/nav/MobileNavDrawer.tsx` returns >= 6 (trigger + 5 inner Links).
    - `grep -c "focus-visible:ring-offset-black" src/components/nav/ShopDropdown.tsx` returns >= 2.
    - `grep -c "focus-visible:ring-offset-black" src/components/nav/CartTrigger.tsx` returns >= 1 (or summary notes a pre-existing focus-visible declaration).
    - `npx tsc --noEmit` exits 0.
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Every Nav-chrome interactive has the canonical focus-visible ring. Search button left untouched (deleted in 11-04).</done>
</task>

<task type="auto">
  <name>Task 2: Add focus-visible ring to ShopHeader tabs + sort select + Contact WhatsApp anchor</name>
  <read_first>
    - src/components/shop/ShopHeader.tsx
    - src/app/[locale]/contact/ContactClient.tsx
  </read_first>
  <files>
    src/components/shop/ShopHeader.tsx,
    src/app/[locale]/contact/ContactClient.tsx
  </files>
  <action>
**ShopHeader.tsx:**
- Lines 63-77 (the three category-tab `<button>` elements rendered inside the `categories.map`): append the canonical focus string to each button's className (the `.join(' ')` className expression). Append the focus string as an additional string-array entry inside the join.

  Example refactor (paste as the replacement for the existing className expression at lines 68-73):
  ```
  className={[
    'text-sm font-normal pb-1 transition-colors duration-150',
    isActive
      ? 'text-white border-b-2 border-white'
      : 'text-white/60 hover:text-white border-b-2 border-transparent',
    'focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-white focus-visible:ring-offset-2 focus-visible:ring-offset-black',
  ].join(' ')}
  ```

- Lines 88-99 (native sort `<select>`): append the canonical focus string to its className. NOTE: Plan 11-12 replaces this select with shadcn `Select`; this addition is defensive so this plan is independently shippable.

**ContactClient.tsx:**
- Line 39-47 (WhatsApp `<a>` anchor): append the canonical focus string to its existing className (which currently ends with `transition-all`).
- DO NOT touch the form `<input>`/`<textarea>` elements — Plan 11-10 replaces the entire form region with the WhatsApp callout. Adding rings to soon-deleted inputs is waste.
  </action>
  <verify>
    <automated>grep -c "focus-visible:ring-offset-black" src/components/shop/ShopHeader.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "focus-visible:ring-offset-black" src/components/shop/ShopHeader.tsx` returns >= 2 (tab buttons + sort select).
    - `grep -c "focus-visible:ring-offset-black" src/app/[locale]/contact/ContactClient.tsx` returns >= 1 (WhatsApp anchor).
    - `npx tsc --noEmit` exits 0.
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Category tabs, sort select, and Contact WhatsApp anchor all show the canonical focus ring on keyboard focus.</done>
</task>

</tasks>

<verification>
- F-Exp-1 closed: every storefront interactive in the coverage matrix carries the canonical focus-visible ring.
- Live-dev verification (Plan 11-16) will Tab through every surface and capture screenshots of focused state.
- SC-4 advanced (formal completion at live-dev pass).
</verification>

<success_criteria>
SC-4 (every interactive has a visible focus ring) — full coverage in code; live verify in Plan 11-16.
SC-6 (no new TS/lint errors) — full.
</success_criteria>

<output>
Create `.planning/phases/11-storefront-ui-ux-polish-en/11-03-SUMMARY.md` with a per-file diff summary, a note on which interactives were intentionally skipped (Navbar search button, Contact form inputs), and the CartTrigger pre-existing-ring decision.
</output>
