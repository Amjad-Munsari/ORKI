---
phase: 11-storefront-ui-ux-polish-en
plan: 05
type: execute
wave: 1
depends_on: [11-01]
files_modified:
  - src/components/footer/Footer.tsx
autonomous: true
requirements: [SC-1, SC-6]
tags: [typography, footer]

must_haves:
  truths:
    - "Footer ORKI wordmark uses the `display-1` utility (which encodes `letter-spacing: -0.04em` and clamps font-size at 160px max)."
    - "No `tracking-[-0.05em]` literal remains on the Footer wordmark."
  artifacts:
    - path: "src/components/footer/Footer.tsx"
      provides: "Footer wordmark using shared display-1 utility"
      contains: "display-1"
  key_links:
    - from: "src/components/footer/Footer.tsx"
      to: "src/app/globals.css .display-1"
      via: "shared display utility"
      pattern: "display-1"
---

<objective>
Replace Footer's bespoke `text-8xl md:text-[160px] tracking-[-0.05em]` with the shared `.display-1` utility introduced in Plan 11-01. The utility already encodes `letter-spacing: -0.04em` (per audit recommendation, tighter than the original) and clamps top size at 160px. Closes F-Typo-3.

Purpose: Closes F-Typo-3 (audit lines 125-127).
Output: Footer wordmark inherits the unified display scale; tracking is in the design system, not a bespoke literal.
</objective>

<execution_context>
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/dev/Antigravity/ORKI/.planning/STOREFRONT-UI-REVIEW-EN.md
@C:/dev/Antigravity/ORKI/.planning/phases/11-storefront-ui-ux-polish-en/11-01-SUMMARY.md

<interfaces>
**Current state (line 41-43 of Footer.tsx):**
```
<h2 className="text-8xl md:text-[160px] font-bold tracking-[-0.05em] leading-none">
  ORKI
</h2>
```

**Target state (after Plan 11-01 lands display-1):**
```
<h2 className="display-1 font-bold">
  ORKI
</h2>
```

Plan 11-01 already touches this file (Replacement 4 in 11-01 Task 2). If Plan 11-01 executed first, this plan verifies the swap held. If Plan 11-01 changes drift, this plan re-applies the canonical state.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Footer wordmark uses display-1; reconcile if drifted</name>
  <read_first>
    - src/components/footer/Footer.tsx (lines 35-50)
    - .planning/phases/11-storefront-ui-ux-polish-en/11-01-SUMMARY.md (to confirm exact state left by 11-01)
    - src/app/globals.css (lines 200-230 — confirm `.display-1` exists)
  </read_first>
  <files>src/components/footer/Footer.tsx</files>
  <action>
1. Read the current Footer.tsx file around lines 41-43.
2. If line 41 already reads `<h2 className="display-1 font-bold">` (as Plan 11-01 set it), this task is a no-op — record "no-op, 11-01 already applied" in summary and exit.
3. If line 41 still contains `text-8xl`, `text-[160px]`, or `tracking-[-0.05em]`, replace the entire h2 className with `display-1 font-bold`. Keep the `ORKI` text content and the surrounding div structure untouched.
4. Confirm no other `tracking-[-0.05em]` literal exists on the wordmark.

DO NOT touch the copyright `<p>` at line 44-46, the navigation groups (lines 50-80), or the benefit bar.
  </action>
  <verify>
    <automated>grep -c "display-1" src/components/footer/Footer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "display-1" src/components/footer/Footer.tsx` returns 1.
    - `grep -c "tracking-\[-0.05em\]" src/components/footer/Footer.tsx` returns 0.
    - `grep -c "text-\[160px\]" src/components/footer/Footer.tsx` returns 0.
    - `npx tsc --noEmit` exits 0.
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Footer wordmark uses display-1 utility exclusively. No bespoke literals on the wordmark.</done>
</task>

</tasks>

<verification>
- F-Typo-3 closed: Footer wordmark uses shared display-1 scale; tracking is in CSS (-0.04em, tighter than original -0.05em per audit recommendation).
- Live verify (Plan 11-16) will check that the wordmark visually fills its lane at 1440px and clamps gracefully at narrow widths.
</verification>

<success_criteria>
SC-1 (every [OPEN] finding closed) — partial: F-Typo-3.
SC-6 (no new TS/lint errors) — full.
</success_criteria>

<output>
Create `.planning/phases/11-storefront-ui-ux-polish-en/11-05-SUMMARY.md` documenting the final h2 className and noting whether this plan was a no-op (Plan 11-01 covered it) or had to reconcile.
</output>
