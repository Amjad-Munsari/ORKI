---
phase: 11-storefront-ui-ux-polish-en
plan: 04
type: execute
wave: 1
depends_on: [11-02, 11-03]
files_modified:
  - src/components/nav/Navbar.tsx
  - src/components/nav/LanguageSwitcher.tsx
  - src/components/nav/MobileNavDrawer.tsx
  - src/components/nav/ShopDropdown.tsx
autonomous: true
requirements: [SC-3, SC-6]
tags: [dark-mode, brand-chrome, navbar]

must_haves:
  truths:
    - "The global Navbar renders dark-on-dark: `bg-black/80 backdrop-blur text-white border-b border-white/[0.08]`."
    - "The Navbar search icon button is removed from the DOM (no longer rendered)."
    - "LanguageSwitcher text is white-tinted, matching the dark Navbar contrast."
    - "MobileNavDrawer trigger icon is white-tinted, matching the dark Navbar."
    - "ShopDropdown panel chrome mirrors CategoryDropdown: `bg-[var(--color-secondary-surface)] border border-white/[0.12]`."
  artifacts:
    - path: "src/components/nav/Navbar.tsx"
      provides: "Dark-chrome Navbar header"
      contains: "bg-black/80"
    - path: "src/components/nav/ShopDropdown.tsx"
      provides: "Dark dropdown panel matching CategoryDropdown"
      contains: "var(--color-secondary-surface)"
  key_links:
    - from: "src/components/nav/Navbar.tsx"
      to: "src/components/nav/LanguageSwitcher.tsx + MobileNavDrawer.tsx"
      via: "shared dark token cascade — text-white on bg-black"
      pattern: "text-white"
---

<objective>
Flip the global Navbar from white-on-black to ORKI's dark-first chrome. Cascade the token to LanguageSwitcher + MobileNavDrawer trigger. Mirror CategoryDropdown chrome on ShopDropdown's panel. Remove the no-op search icon. Closes F-Color-1 (audit lines 96-98), F-Color-2 (100-102), F-Vis-4 (90-92).

Purpose: Closes F-Color-1, F-Color-2, F-Vis-4.
Output: Navbar reads as dark, intentional, branded chrome. ShopDropdown panel matches CategoryDropdown.
</objective>

<execution_context>
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/dev/Antigravity/ORKI/CLAUDE.md
@C:/dev/Antigravity/ORKI/.planning/STOREFRONT-UI-REVIEW-EN.md
@C:/dev/Antigravity/ORKI/.planning/phases/11-storefront-ui-ux-polish-en/11-01-SUMMARY.md
@C:/dev/Antigravity/ORKI/.planning/phases/11-storefront-ui-ux-polish-en/11-02-SUMMARY.md
@C:/dev/Antigravity/ORKI/.planning/phases/11-storefront-ui-ux-polish-en/11-03-SUMMARY.md

<interfaces>
**Reference (do NOT modify):** `src/components/nav/CategoryDropdown.tsx:29` already declares the correct dark chrome:
```
bg-[#111111] border-white/[0.12]
```

This phase consolidates that hex literal to `bg-[var(--color-secondary-surface)]` (the same value, declared in globals.css per Plan 11-01).

**Target Navbar chrome (audit-cited at line 98):**
```
bg-black/80 backdrop-blur text-white border-b border-white/[0.08]
```

**Cascade tokens:**
- LanguageSwitcher button text: `text-black/60 hover:text-black` → `text-white/60 hover:text-white`.
- MobileNavDrawer SheetTrigger render button: `text-black` → `text-white`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip Navbar chrome dark + remove search button</name>
  <read_first>
    - src/components/nav/Navbar.tsx (entire file)
    - src/components/nav/CategoryDropdown.tsx (line 29 reference)
    - .planning/STOREFRONT-UI-REVIEW-EN.md lines 96-98 (F-Color-1 evidence)
    - .planning/STOREFRONT-UI-REVIEW-EN.md lines 90-92 (F-Vis-4 evidence — search icon)
  </read_first>
  <files>src/components/nav/Navbar.tsx</files>
  <action>
**Step 1 — Flip header chrome (line 23):**
Replace:
```
<header className="fixed top-0 z-50 w-full bg-white text-black border-b border-black/5">
```
With:
```
<header className="fixed top-0 z-50 w-full bg-black/80 backdrop-blur text-white border-b border-white/[0.08]">
```

**Step 2 — Remove search button (lines 61-66):**
Delete the entire block:
```
<button
  aria-label="Search"
  className="h-full flex items-center justify-center hover:opacity-60 transition-opacity"
>
  <Search className="size-5" strokeWidth={2.5} />
</button>
```

**Step 3 — Remove the now-unused `Search` import (line 3):**
Change:
```
import { Search } from 'lucide-react'
```
to delete the entire import line (no other usage of `Search` remains in the file).

Verify by grep: `grep -n "Search" src/components/nav/Navbar.tsx` should return 0 lines.

DO NOT touch the focus-visible utilities added in Plan 11-03 (they remain on logo + .map Links).
DO NOT touch ShopDropdown / LanguageSwitcher / CartTrigger / UserMenu / MobileNavDrawer wiring inside Navbar (only the search button block is removed).
DO NOT touch the `dir="ltr"` on the ORKI logo span (line 55) — that's correct.
  </action>
  <verify>
    <automated>grep -c "bg-black/80 backdrop-blur text-white" src/components/nav/Navbar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bg-black/80 backdrop-blur" src/components/nav/Navbar.tsx` returns 1.
    - `grep -c "bg-white text-black" src/components/nav/Navbar.tsx` returns 0.
    - `grep -c "Search" src/components/nav/Navbar.tsx` returns 0 (no import, no element).
    - `grep -c "border-white/\[0.08\]" src/components/nav/Navbar.tsx` returns 1.
    - `npx tsc --noEmit` exits 0.
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Navbar header is dark, search icon is gone, Search import is removed, lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Cascade dark tokens to LanguageSwitcher + MobileNavDrawer trigger</name>
  <read_first>
    - src/components/nav/LanguageSwitcher.tsx (entire file — 28 lines)
    - src/components/nav/MobileNavDrawer.tsx (lines 58-65 — the trigger render)
  </read_first>
  <files>
    src/components/nav/LanguageSwitcher.tsx,
    src/components/nav/MobileNavDrawer.tsx
  </files>
  <action>
**LanguageSwitcher.tsx, line ~23 button className:**
Replace `text-black/60 hover:text-black` with `text-white/60 hover:text-white`. Final className keeps all other utilities (min-h, min-w, focus-visible ring from Plan 11-03, active scale).

**MobileNavDrawer.tsx, lines 58-65 SheetTrigger render button:**
Replace `text-black` with `text-white`. Keep `hover:opacity-60`, the min-h/min-w sizing, the focus-visible ring from Plan 11-03, and the active scale.

The List/X hamburger icons inside `<motion.span>` (lines 76, 86) inherit `currentColor` from the button — no changes needed there.
  </action>
  <verify>
    <automated>grep -c "text-white/60 hover:text-white" src/components/nav/LanguageSwitcher.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "text-white/60 hover:text-white" src/components/nav/LanguageSwitcher.tsx` returns 1.
    - `grep -c "text-black" src/components/nav/LanguageSwitcher.tsx` returns 0.
    - `grep -c "text-white" src/components/nav/MobileNavDrawer.tsx` returns >= 1 (on the SheetTrigger render button).
    - `grep -n "text-black" src/components/nav/MobileNavDrawer.tsx` returns 0 matches on the SheetTrigger render block (lines 58-65). Inner panel content was already white; should remain unchanged.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>LanguageSwitcher + drawer trigger render white on the dark Navbar. No black-tinted text leaks into the new chrome.</done>
</task>

<task type="auto">
  <name>Task 3: Flip ShopDropdown panel to dark, mirror CategoryDropdown</name>
  <read_first>
    - src/components/nav/ShopDropdown.tsx (entire file)
    - src/components/nav/CategoryDropdown.tsx (entire file — reference chrome at line 29)
  </read_first>
  <files>src/components/nav/ShopDropdown.tsx</files>
  <action>
**ShopDropdown.tsx line 50 (motion.div panel className):**
Current (after Plan 11-02 fix):
```
className="absolute top-full start-0 min-w-[160px] bg-white border border-black/5 shadow-xl py-2 z-50"
```

Replace with:
```
className="absolute top-full start-0 min-w-[160px] bg-[var(--color-secondary-surface)] border border-white/[0.12] shadow-xl py-2 z-50"
```

**Line 56 (inner Link className) — the hover state needs to flip:**
Current:
```
className="block px-6 py-3 text-sm font-medium hover:bg-black/5 transition-colors whitespace-nowrap"
```

Replace with (keeping Plan 11-03's focus-visible ring):
```
className="block px-6 py-3 text-sm font-medium text-white hover:bg-white/5 transition-colors whitespace-nowrap focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-white focus-visible:ring-offset-2 focus-visible:ring-offset-black"
```

The trigger Link at line 36-41 already inherits Navbar's `text-white` from Task 1. No change needed there.
  </action>
  <verify>
    <automated>grep -c "bg-\[var(--color-secondary-surface)\]" src/components/nav/ShopDropdown.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bg-\[var(--color-secondary-surface)\]" src/components/nav/ShopDropdown.tsx` returns 1.
    - `grep -c "bg-white " src/components/nav/ShopDropdown.tsx` returns 0 (the old bg-white must be gone).
    - `grep -c "hover:bg-white/5" src/components/nav/ShopDropdown.tsx` returns 1 (item hover state flipped).
    - `grep -c "border-white/\[0.12\]" src/components/nav/ShopDropdown.tsx` returns 1.
    - `grep -c "border-black/5" src/components/nav/ShopDropdown.tsx` returns 0.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>ShopDropdown panel chrome matches CategoryDropdown reference: dark surface, white/12% border, white text, white/5 hover bg.</done>
</task>

</tasks>

<verification>
- F-Color-1 closed: Navbar dark.
- F-Color-2 closed: ShopDropdown panel matches CategoryDropdown.
- F-Vis-4 closed: Search button removed entirely + Search import removed.
- Build: `npm run build` exits 0; `npm run lint` exits 0.
- Plan 11-16 will visually verify the dark Navbar at every viewport + verify the Tab traversal still picks up logo, nav links, ShopDropdown trigger, language switcher, mobile drawer trigger, cart trigger.
</verification>

<success_criteria>
SC-3 (Navbar + ShopDropdown dark-mode chrome) — full.
SC-6 (no new TS/lint errors) — full.
</success_criteria>

<output>
Create `.planning/phases/11-storefront-ui-ux-polish-en/11-04-SUMMARY.md` documenting: exact className diffs per file, Search icon + import removal, CategoryDropdown chrome reference adopted, and grep verification results.
</output>
