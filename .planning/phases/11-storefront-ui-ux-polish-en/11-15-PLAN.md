---
phase: 11-storefront-ui-ux-polish-en
plan: 15
type: execute
wave: 4
depends_on: [11-01, 11-02, 11-03, 11-04, 11-05, 11-06, 11-07, 11-08, 11-09, 11-10, 11-11, 11-12, 11-13, 11-14]
files_modified:
  - .planning/phases/11-storefront-ui-ux-polish-en/11-VERIFICATION.md
  - messages/en.json
  - messages/ar.json
  - src/app/[locale]/contact/ContactClient.tsx
autonomous: false
requirements: [SC-1, SC-2, SC-3, SC-4, SC-5, SC-6]
tags: [verification, live-dev, uat, finalization]

must_haves:
  truths:
    - "`11-VERIFICATION.md` exists and records per-finding (F-01..F-23 audit numbering OR F-Copy-1, F-Vis-1, etc. semantic-id numbering) status: closed / deferred / unresolved."
    - "`npm run dev` launched at least once during this plan; every changed storefront surface visited in browser at least once at desktop AND mobile-emulator viewport AND in both EN + AR + RTL contexts."
    - "`pnpm tsc` (or `npm run tsc`) + `pnpm lint` + `pnpm test` exit codes captured in 11-VERIFICATION.md."
    - "WhatsApp `WA_NUMBER` constant in ContactClient.tsx has been swapped from `'TBD'` to a real international-format number provided by the user during this plan."
    - "EN 404 + Shop.empty + Home.ethos UAT candidates resolved: chosen candidate moved to live keys, `_candidates` blocks removed from messages/en.json (and AR live keys reconciled in messages/ar.json if user picked a non-A candidate that requires AR parity)."
  artifacts:
    - path: ".planning/phases/11-storefront-ui-ux-polish-en/11-VERIFICATION.md"
      provides: "Per-finding status table for the entire EN-scope audit"
      contains: "F-Copy-1"
  key_links:
    - from: "11-VERIFICATION.md"
      to: "STOREFRONT-UI-REVIEW-EN.md"
      via: "per-finding closure status"
      pattern: "OPEN.*closed|OPEN.*deferred|OPEN.*unresolved"
---

<objective>
Phase 11's final-mile execution: live-dev verification + three UAT-deferred decisions + verification artifact. This plan is NOT autonomous — it has human checkpoints for (a) WhatsApp number entry, (b) Shop empty-state copy choice, (c) Home ethos copy choice, (d) EN 404 voice choice.

Purpose: Closes SC-2 (live verify) + finalises copy choices (D-06, D-07, D-08) + replaces WA_NUMBER (D-16) + captures per-finding pass/defer table.
Output: 11-VERIFICATION.md + finalised storefront ready for sign-off.
</objective>

<execution_context>
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/dev/Antigravity/ORKI/.planning/STOREFRONT-UI-REVIEW-EN.md
@C:/dev/Antigravity/ORKI/.planning/phases/11-storefront-ui-ux-polish-en/11-CONTEXT.md
@C:/dev/Antigravity/ORKI/.planning/phases/11-storefront-ui-ux-polish-en/11-01-SUMMARY.md
@C:/dev/Antigravity/ORKI/.planning/phases/11-storefront-ui-ux-polish-en/11-02-SUMMARY.md
@C:/dev/Antigravity/ORKI/.planning/phases/11-storefront-ui-ux-polish-en/11-03-SUMMARY.md
@C:/dev/Antigravity/ORKI/.planning/phases/11-storefront-ui-ux-polish-en/11-04-SUMMARY.md
@C:/dev/Antigravity/ORKI/.planning/phases/11-storefront-ui-ux-polish-en/11-05-SUMMARY.md
@C:/dev/Antigravity/ORKI/.planning/phases/11-storefront-ui-ux-polish-en/11-06-SUMMARY.md
@C:/dev/Antigravity/ORKI/.planning/phases/11-storefront-ui-ux-polish-en/11-07-SUMMARY.md
@C:/dev/Antigravity/ORKI/.planning/phases/11-storefront-ui-ux-polish-en/11-08-SUMMARY.md
@C:/dev/Antigravity/ORKI/.planning/phases/11-storefront-ui-ux-polish-en/11-09-SUMMARY.md
@C:/dev/Antigravity/ORKI/.planning/phases/11-storefront-ui-ux-polish-en/11-10-SUMMARY.md
@C:/dev/Antigravity/ORKI/.planning/phases/11-storefront-ui-ux-polish-en/11-11-SUMMARY.md
@C:/dev/Antigravity/ORKI/.planning/phases/11-storefront-ui-ux-polish-en/11-12-SUMMARY.md
@C:/dev/Antigravity/ORKI/.planning/phases/11-storefront-ui-ux-polish-en/11-13-SUMMARY.md
@C:/dev/Antigravity/ORKI/.planning/phases/11-storefront-ui-ux-polish-en/11-14-SUMMARY.md

<interfaces>
**Surface inventory to walk during live dev (every URL visited at desktop + mobile-emulator viewport + EN + AR):**

| Surface | URL pattern | Findings closed in this surface |
|---|---|---|
| Home | /en, /ar | F-Vis-2, F-Space-1, F-Exp-4, F-Exp-7, F-Copy-1 (ethos), F-Typo-1 (hero), F-Color-3, F-Space-2 |
| Shop list | /en/shop, /ar/shop | F-Vis-1 (cards), F-Exp-2 (skeleton), F-Exp-6 (sort), F-Space-1, F-Copy-1 (empty), F-Color-3, F-Space-2 |
| Shop category | /en/shop?category=tops, etc. | F-Vis-1, F-Exp-2 |
| Shop empty | /en/shop?category=tops with sort that returns 0 (force via `data/products.ts` if needed) | F-Copy-1 (empty) |
| PDP | /en/products/{any-slug}, /ar/products/{any-slug} | F-Vis-3 (gallery), F-Color-4, F-Exp-5, F-Space-4, F-Typo-1 (price area) |
| About | /en/about, /ar/about | F-Typo-2, F-Copy-2, F-Color-3, F-Typo-1 |
| Contact | /en/contact, /ar/contact | F-Copy-3, F-Exp-3, F-Vis-4 (search hidden — verifiable from Navbar everywhere) |
| 404 | /en/nope, /ar/nope | F-Exp-8 |
| Global | (every page) | F-Color-1 (Navbar), F-Color-2 (ShopDropdown), F-Exp-1 (focus rings), F-Typo-3 (footer wordmark) |

**Keyboard-only check:** Tab through Home → Shop → PDP → Contact → 404 pages. The focus ring must be visible at every stop. Capture which interactives lacked rings if any.

**Reduced-motion check:** Toggle OS-level `prefers-reduced-motion` (Windows: Settings → Accessibility → Visual effects → Animation effects OFF; macOS: System Preferences → Accessibility → Display → Reduce motion). Verify ScrollReveal still renders content (instant), PDPGallery thumb-click uses instant scroll, MobileNavDrawer slides without spring.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated suite and capture results</name>
  <read_first>
    - package.json (confirm script names: `tsc`, `lint`, `test`, `build`)
  </read_first>
  <files>.planning/phases/11-storefront-ui-ux-polish-en/11-VERIFICATION.md</files>
  <action>
1. Run each command, capturing exit code:
   - `npx tsc --noEmit` (or `pnpm tsc` / `npm run tsc`)
   - `npm run lint` (or `pnpm lint`)
   - `npm test` (or `pnpm test`)
   - `npm run build`
2. Create `11-VERIFICATION.md` at `.planning/phases/11-storefront-ui-ux-polish-en/11-VERIFICATION.md`. Initial skeleton:

```markdown
# Phase 11 — Verification (Storefront UI/UX Polish, EN)

**Verified:** {date}
**Verifier:** Claude (execute-phase)
**Source audit:** .planning/STOREFRONT-UI-REVIEW-EN.md

## Automated suite

| Command | Exit code | Notes |
|---|---|---|
| `tsc --noEmit` | {0/non-zero} | |
| `lint` | {0/non-zero} | |
| `test` | {0/non-zero} | |
| `build` | {0/non-zero} | |

## Per-finding closure

| Finding | Severity | Plan | Status | Notes |
|---|---|---|---|---|
| F-Copy-1 (Shop empty + Home ethos) | MAJOR | 11-14 | { closed / deferred / unresolved } | |
| F-Copy-2 (About 01/02/03 EN-keep) | MINOR | 11-09 | { } | |
| F-Copy-3 (Contact form lies) | MINOR | 11-10 | { } | |
| F-Vis-1 (placeholder kit) | BLOCKER | 11-06, 11-07 | { } | |
| F-Vis-2 (home category splat) | MAJOR | 11-07 | { } | |
| F-Vis-3 (PDP gallery) | MAJOR | 11-08 | { } | |
| F-Vis-4 (search icon no-op) | MINOR | 11-04 | { } | |
| F-Color-1 (Navbar dark) | MAJOR | 11-04 | { } | |
| F-Color-2 (ShopDropdown panel) | MAJOR | 11-04 | { } | |
| F-Color-3 (near-black hex collapse) | MINOR | 11-01 | { } | |
| F-Color-4 (text-destructive OOS) | MINOR | 11-11 | { } | |
| F-Typo-1 (display scale) | MAJOR | 11-01 | { } | |
| F-Typo-2 (About body rhythm) | MAJOR | 11-09 | { } | |
| F-Typo-3 (Footer wordmark) | MINOR | 11-01, 11-05 | { } | |
| F-Space-1 (grid scale unification) | MAJOR | 11-07 | { } | |
| F-Space-2 (container width unification) | MAJOR | 11-01 | { } | |
| F-Space-3 (ShopDropdown left-0 + shadcn) | MINOR | 11-02 | { } | |
| F-Space-4 (PDP nav-height comment) | MINOR | 11-11 | { } | |
| F-Exp-1 (focus-visible rings) | MAJOR | 11-03 | { } | |
| F-Exp-2 (shop loading state) | MAJOR | 11-12 | { } | |
| F-Exp-3 (Contact form aria-live) | MAJOR | 11-10 | { } | |
| F-Exp-4 (Featured-Drops ScrollReveal) | MAJOR | 11-07 | { } | |
| F-Exp-5 (PDP Limited stock passive) | MINOR | 11-11 | { } | |
| F-Exp-6 (sort select shadcn) | MINOR | 11-12 | { } | |
| F-Exp-7 (brand-ethos em-dash) | COSMETIC | 11-07 | { } | |
| F-Exp-8 (EN 404 voice) | COSMETIC | 11-13 | { } | |

## UAT decisions captured

| Decision | Owner | Value selected |
|---|---|---|
| D-16 WhatsApp number | user | { e.g. 966555123456 } |
| D-06 Shop empty copy | user | { A_dropCycle / B_locale / C_stripped } |
| D-07 Home ethos line | user | { A_voice / B_heritage / C_stripped } |
| D-08 EN 404 voice | user | { A_voice / B_locative / C_minimal } |

## Surface walk

(Filled in by Tasks 3-5)

## Deferred follow-ups

(Filled in if any finding ends in `unresolved` or `deferred`)
```

Fill the automated-suite table with the actual exit codes captured.
  </action>
  <verify>
    <automated>test -f .planning/phases/11-storefront-ui-ux-polish-en/11-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/11-storefront-ui-ux-polish-en/11-VERIFICATION.md` exists.
    - Per-finding table has 26 rows (F-Copy-1, F-Copy-2, F-Copy-3, F-Vis-1..4, F-Color-1..4, F-Typo-1..3, F-Space-1..4, F-Exp-1..8).
    - Automated-suite exit codes are captured.
    - All four automated commands exit 0 — otherwise stop and report the failure; do not proceed to live dev.
  </acceptance_criteria>
  <done>Automated suite green; verification scaffold ready for live-dev results.</done>
</task>

<task type="auto">
  <name>Task 2: Launch dev server and walk every changed surface (auto)</name>
  <read_first>
    - The surface inventory in this plan's `<interfaces>` block
  </read_first>
  <files>.planning/phases/11-storefront-ui-ux-polish-en/11-VERIFICATION.md</files>
  <action>
1. Launch the dev server: `npm run dev` (background). Wait ~5 seconds for compile + readiness.
2. For each surface in the inventory (`<interfaces>` table), visit the URL (use `curl -I` for HTTP 200 sanity check at minimum, but for visual confirmation Plan 11-15 Task 3 hands off to the human checkpoint).
3. Capture HTTP status + render success for each URL:

```bash
for url in \
  "http://localhost:3000/en" \
  "http://localhost:3000/ar" \
  "http://localhost:3000/en/shop" \
  "http://localhost:3000/ar/shop" \
  "http://localhost:3000/en/shop?category=tops" \
  "http://localhost:3000/en/about" \
  "http://localhost:3000/ar/about" \
  "http://localhost:3000/en/contact" \
  "http://localhost:3000/ar/contact" \
  "http://localhost:3000/en/nope" \
  "http://localhost:3000/ar/nope"
do
  echo "${url}: $(curl -s -o /dev/null -w '%{http_code}' "$url")"
done
```

Append the output to 11-VERIFICATION.md under a `### HTTP smoke pass` section. Expected: 200 for all except /nope which should be 404. Any non-200/404 result = compile or routing failure; halt and report in summary.

4. Take ONE PDP URL: read `data/products.ts` (or query the DB if seeded) for a real slug, then `curl` `/en/products/{slug}` and `/ar/products/{slug}`.
  </action>
  <verify>
    <automated>grep -c "HTTP smoke pass" .planning/phases/11-storefront-ui-ux-polish-en/11-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - HTTP smoke section present in 11-VERIFICATION.md.
    - Every storefront URL returns 200 (or 404 for /nope).
    - PDP route renders without server-error.
  </acceptance_criteria>
  <done>Dev server up; smoke pass captured; no compile errors on any surface.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual + keyboard walk of every changed surface</name>
  <what-built>
    Phase 11 closed 17 [OPEN] audit findings via Plans 11-01..11-14. Surfaces touched: Home, Shop, Shop empty-state, PDP gallery + info, About, Contact, 404, global Navbar, global Footer, global ShopDropdown.
  </what-built>
  <how-to-verify>
    Dev server is running at http://localhost:3000.

    **For each surface in the inventory in `<interfaces>`, perform these checks at BOTH desktop (1440px+ viewport) AND mobile emulator (375px viewport), in BOTH EN AND AR:**

    1. **Visual:** does the surface match Phase 11 intent? Specifically check:
       - Home category splats render typographic-only (massive `TOPS` / `BOTTOMS` wordmark, no `bg-white/10` overlay, subtle hover scale).
       - Navbar is dark (`bg-black/80 backdrop-blur`).
       - ShopDropdown panel matches CategoryDropdown chrome (dark with white/12 border).
       - Search icon is GONE from Navbar.
       - Footer wordmark fills its lane at 1440px and clamps at mobile.
       - Product cards show varied placeholder treatments (4 variants — ghost / color-block / texture / typographic).
       - PDP gallery shows vertical thumb strip on desktop (inline-start), horizontal scroll-snap with dots on mobile, skeleton on slow load.
       - PDP "Limited stock" passive line shows on a product with a low-stock size (manually edit `data/products.ts` to set one size stock to 3 if needed).
       - Contact page right column is the WhatsApp callout card, NOT a form.
       - About body sections use one body style + one pull-quote style.
       - 404 page reads in brand voice (Candidate A by default — "404 / NO DROP HERE").

    2. **Keyboard nav:** Tab from the top of each page. EVERY interactive (logo, nav links, ShopDropdown trigger, ShopDropdown items, language switcher, cart trigger, drawer trigger, product cards, sort select, PDP thumbs, PDP dots, WhatsApp anchors, 404 CTAs) MUST show a visible white ring on focus. List any that don't.

    3. **Reduced motion:** Toggle OS-level reduced-motion ON. Reload Home + a PDP. ScrollReveal must render content instantly (no entrance animation). PDPGallery thumb-click must scroll instantly (no smooth).

    4. **RTL:** Toggle to /ar/... for every surface. PDP thumb strip must mirror to inline-start (which is the right edge in AR). Footer benefit borders, ShopDropdown panel, Contact callout, all use logical-prop spacing.

    5. **Throttled network:** In DevTools Network tab, set throttling to Slow 3G. Reload /en/shop. The 8-card skeleton must show before products render.

    Use the `<a-checkbox>` notation when reporting. For each finding F-XX in 11-VERIFICATION.md, write a one-line note describing what you saw and mark `closed` / `deferred` / `unresolved`.
  </how-to-verify>
  <resume-signal>Type "approved" once the per-finding table is filled OR list issues per finding</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: User provides the real WhatsApp number (D-16)</name>
  <what-built>
    `ContactClient.tsx` line ~14 has `const WA_NUMBER = 'TBD'`. UAT step: replace with the real international-format number (no `+`, no spaces, no dashes).
  </what-built>
  <how-to-verify>
    Visit http://localhost:3000/en/contact. The "Open WhatsApp" button currently points to `https://wa.me/TBD`, which is intentionally broken so the swap is visible.

    User: provide the real WhatsApp number for ORKI's customer channel. Format: `966555123456` (country code + number, no leading zero, no `+`, no spaces, no dashes).

    Once provided, edit `src/app/[locale]/contact/ContactClient.tsx` and replace:
    ```
    const WA_NUMBER = 'TBD'
    ```
    With:
    ```
    const WA_NUMBER = '<NUMBER>'
    ```

    Then visit `https://wa.me/<NUMBER>` in a new tab to confirm it resolves to a WhatsApp chat target.

    Record the chosen number in 11-VERIFICATION.md under "UAT decisions captured" → "D-16 WhatsApp number".
  </how-to-verify>
  <resume-signal>Provide number in format `966XXXXXXXXX`, or type "skip" to leave as TBD (note in summary as deferred)</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 5: User picks Shop empty + Home ethos + EN 404 copy candidates (D-06, D-07, D-08)</name>
  <decision>
    Three copy decisions to finalise. Plans 11-13 and 11-14 wrote candidates into `messages/en.json` as sibling `_candidates` objects. Live default is candidate "A" for each. User reviews live, picks one.
  </decision>
  <context>
    Visit each empty-state in dev:
    - Shop empty: visit `/en/shop?category=tops&sort=price-asc` after temporarily filtering out all tops (or temporarily edit `data/products.ts` to seed zero tops; revert after).
    - Home ethos: scroll to the brand-ethos section on `/en`.
    - 404: visit `/en/nope`.

    Each section currently shows Candidate A. To preview B or C, temporarily edit `messages/en.json` and swap the live `heading`/`body`/`line` values to the candidate's values — reload — view — revert if not chosen.
  </context>
  <options>
    <option id="shop-A_dropCycle">
      <name>Shop empty: A — drop-cycle</name>
      <pros>Brand calendar cue; encourages return visit on Friday</pros>
      <cons>Assumes Friday drop cadence — confirm with user</cons>
    </option>
    <option id="shop-B_locale">
      <name>Shop empty: B — Riyadh-locale</name>
      <pros>Hyper-local voice; ties to hero copy</pros>
      <cons>Longer; reads as marketing</cons>
    </option>
    <option id="shop-C_stripped">
      <name>Shop empty: C — stripped</name>
      <pros>Minimal; brand-clean</pros>
      <cons>Loses voice — could be any store</cons>
    </option>
    <option id="ethos-A_voice">
      <name>Home ethos: A — voice-confident</name>
      <pros>"What Riyadh sounds like at night" — strongest brand signal</pros>
      <cons>Riff on hero, may feel repetitive</cons>
    </option>
    <option id="ethos-B_heritage">
      <name>Home ethos: B — Najd-heritage</name>
      <pros>Layered; nods to traditional/contemporary blend</pros>
      <cons>Heavier register</cons>
    </option>
    <option id="ethos-C_stripped">
      <name>Home ethos: C — short-stripped</name>
      <pros>Two-sentence punch</pros>
      <cons>Less editorial weight</cons>
    </option>
    <option id="404-A_voice">
      <name>404: A — NO DROP HERE</name>
      <pros>Riffs on brand lexicon ("drops")</pros>
      <cons>—</cons>
    </option>
    <option id="404-B_locative">
      <name>404: B — OFF THE MAP</name>
      <pros>Streetwear-coded; locative</pros>
      <cons>Slightly busier</cons>
    </option>
    <option id="404-C_minimal">
      <name>404: C — bare</name>
      <pros>Clinical; ORKI-minimal</pros>
      <cons>Loses voice</cons>
    </option>
  </options>
  <resume-signal>
    Select one per decision: `shop-X, ethos-X, 404-X` (e.g. `shop-A_dropCycle, ethos-A_voice, 404-A_voice`).
    Executor will then rename the chosen candidate's heading/body to the top-level live keys in messages/en.json AND remove the `_candidates` sibling block from the three locations. AR keys are left as their existing placeholders; Phase 999.11 reviews.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 6: Apply user's UAT choices to messages/en.json; finalize 11-VERIFICATION.md</name>
  <read_first>
    - messages/en.json (Shop.empty, Home.ethos, Errors.notFound — all have `_candidates` siblings)
    - .planning/phases/11-storefront-ui-ux-polish-en/11-VERIFICATION.md
  </read_first>
  <files>
    messages/en.json,
    .planning/phases/11-storefront-ui-ux-polish-en/11-VERIFICATION.md
  </files>
  <action>
1. Based on the user's choices in Task 5:
   - **If user picked Shop = A:** no change needed to live values; just delete the `Shop.empty._candidates` sub-object.
   - **If user picked Shop = B or C:** copy the chosen candidate's `heading` + `body` into `Shop.empty.heading` + `Shop.empty.body`, then delete `Shop.empty._candidates`.
   - **Repeat the same logic for `Home.ethos`** (single `line` key + `readStory` is unchanged) and `Errors.notFound` (`heading` + `body` keys).
2. Validate JSON: `node -e "JSON.parse(require('fs').readFileSync('messages/en.json','utf8'))"`.
3. Also delete the legacy `Shop.emptyHeading` and `Shop.emptyBody` keys (lines 87-88 of the pre-Phase-11 file) — Plan 11-14 noted these as orphaned. Confirm no component references them: `grep -rn "emptyHeading\|emptyBody" src/` returns 0.
4. Reload dev server (or wait for HMR) and confirm the chosen copy renders.
5. Update 11-VERIFICATION.md:
   - Fill "UAT decisions captured" with the chosen IDs.
   - Fill "Per-finding closure" table — every row gets a `closed` / `deferred` / `unresolved` value.
   - Fill the "Surface walk" section with one-line notes from Task 3.
   - If any finding is `deferred` or `unresolved`, add a row under "Deferred follow-ups" with rationale and a candidate next-phase ticket.
6. Commit a final atomic commit:
   ```
   git add .planning/phases/11-storefront-ui-ux-polish-en/11-VERIFICATION.md messages/en.json src/app/[locale]/contact/ContactClient.tsx
   git commit -m "feat(11): finalize storefront EN polish — UAT copy + WA number + verification"
   ```
   Do NOT run `git push`; user decides when to publish.
  </action>
  <verify>
    <automated>node -e "const m=require('./messages/en.json'); if(m.Shop.empty._candidates||m.Home.ethos._candidates||m.Errors.notFound._candidates) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `_candidates` sub-objects removed from all three blocks.
    - `Shop.empty.heading`, `Shop.empty.body`, `Home.ethos.line`, `Errors.notFound.heading`, `Errors.notFound.body` all reflect user's chosen candidate.
    - `messages/en.json` parses (`node -e "JSON.parse(...)"`).
    - `grep -rn "emptyHeading\|emptyBody" src/` returns 0 (legacy keys + references removed).
    - 11-VERIFICATION.md has no `{ }` placeholders left — every cell filled.
    - Final commit landed.
  </acceptance_criteria>
  <done>Phase 11 EN scope closed. All UAT decisions captured. Verification document is the single signed-off artifact.</done>
</task>

</tasks>

<verification>
- SC-1 verified per-finding in 11-VERIFICATION.md table.
- SC-2 verified: dev server walked at desktop + mobile + EN + AR + RTL + reduced-motion + throttled-network.
- SC-3 verified at every page (dark Navbar + ShopDropdown).
- SC-4 verified by keyboard Tab traversal of every surface.
- SC-5 verified: shop skeleton on throttle, PDP skeleton on slow PDP load, PageTransition reduced-motion respected.
- SC-6 verified: tsc + lint + test + build all exit 0.
</verification>

<success_criteria>
SC-1, SC-2, SC-3, SC-4, SC-5, SC-6 — full (captured in 11-VERIFICATION.md).
</success_criteria>

<output>
Create `.planning/phases/11-storefront-ui-ux-polish-en/11-15-SUMMARY.md` linking to 11-VERIFICATION.md, summarising UAT decisions (Shop / Home / 404 / WA number), automated-suite exit codes, and any deferred items.

The phase is complete only when 11-VERIFICATION.md has zero `{ }` placeholders and every finding row is filled.
</output>
