---
phase: 11-storefront-ui-ux-polish-en
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/ui/dialog.tsx
  - src/components/ui/sheet.tsx
  - src/components/ui/navigation-menu.tsx
  - src/components/nav/ShopDropdown.tsx
autonomous: true
requirements: [SC-1, SC-6]
tags: [rtl, logical-properties, shadcn-primitives]

must_haves:
  truths:
    - "No `left-/right-/ml-/mr-/pl-/pr-` directional classes leak from shadcn primitives or ShopDropdown into the storefront DOM."
    - "ShopDropdown opens flush with the inline-start edge of its trigger in both EN (LTR) and AR (RTL) without manual class overrides."
  artifacts:
    - path: "src/components/nav/ShopDropdown.tsx"
      provides: "Logical-prop dropdown panel using start-0"
      contains: "start-0"
  key_links:
    - from: "src/components/ui/dialog.tsx"
      to: "src/app/[locale]/layout.tsx (dir attribute)"
      via: "logical CSS props respect [dir]"
      pattern: "start-|end-"
---

<objective>
Sweep RTL-unsafe physical-direction classes (`left-`, `right-`, `ml-`, `mr-`, `pl-`, `pr-`) inside the shadcn primitives we ship and inside `ShopDropdown.tsx:50`. Replace with Tailwind v4 logical equivalents (`start-`, `end-`, `ms-`, `me-`, `ps-`, `pe-`). Per CLAUDE.md, logical props are non-negotiable; this closes F-Space-3.

Purpose: Closes F-Space-3 (audit lines 142-144).
Output: shadcn primitives + ShopDropdown ship logical-prop-only positioning.
</objective>

<execution_context>
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/ORKI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/dev/Antigravity/ORKI/CLAUDE.md
@C:/dev/Antigravity/ORKI/.planning/STOREFRONT-UI-REVIEW-EN.md

<interfaces>
Audit-cited leaks (STOREFRONT-UI-REVIEW-EN.md:142-144):
- `src/components/nav/ShopDropdown.tsx:50` — `top-full left-0` → `top-full start-0`
- `src/components/ui/dialog.tsx:56,68` (close button positioning + content padding)
- `src/components/ui/sheet.tsx:56,68` (close button + content positioning)
- `src/components/ui/navigation-menu.tsx:87,111` (viewport positioning)

Tailwind v4 logical-direction mapping (CLAUDE.md):
- `left-N` → `start-N`
- `right-N` → `end-N`
- `ml-N` → `ms-N`
- `mr-N` → `me-N`
- `pl-N` → `ps-N`
- `pr-N` → `pe-N`
- `inset-x-N` is already logical-safe (no change needed)
- `top-N`, `bottom-N`, `inset-y-N`, `h-N`, `w-N` are direction-agnostic (no change)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep shadcn primitives (dialog.tsx, sheet.tsx, navigation-menu.tsx) for physical-direction classes</name>
  <read_first>
    - src/components/ui/dialog.tsx (entire file)
    - src/components/ui/sheet.tsx (entire file)
    - src/components/ui/navigation-menu.tsx (entire file)
    - CLAUDE.md "RTL is non-negotiable" section
  </read_first>
  <files>
    src/components/ui/dialog.tsx,
    src/components/ui/sheet.tsx,
    src/components/ui/navigation-menu.tsx
  </files>
  <action>
For each of the three primitives, scan EVERY `className` string and replace physical-direction Tailwind utilities with their logical equivalents using the mapping table above. Specifically:

1. **`left-N` → `start-N`** (e.g. `left-0` → `start-0`, `left-4` → `start-4`, `left-1/2` → `start-1/2`).
2. **`right-N` → `end-N`** (e.g. `right-0` → `end-0`, `right-4` → `end-4`).
3. **`ml-N` → `ms-N`**.
4. **`mr-N` → `me-N`**.
5. **`pl-N` → `ps-N`**.
6. **`pr-N` → `pe-N`**.

DO NOT touch:
- `top-`, `bottom-`, `inset-y-`, `inset-x-` (already direction-agnostic).
- Animation keyframe references (`data-[state=open]:slide-in-from-left` and similar) — those are shadcn-internal animation tokens, not directional layout. Leave them as-is in this plan (downstream future audit may revisit; out of scope per audit which lists ONLY positional leaks).
- The translate utilities used for centering (`-translate-x-1/2`) — keep, they pair with `start-1/2` and remain mathematically correct in RTL because `translate-x` flips with the writing mode in Tailwind v4. If you find `-translate-x-1/2` paired with `left-1/2`, change ONLY the `left-1/2` to `start-1/2` and leave the translate untouched.
- shadcn-managed style or data-state attribute selectors — only touch raw Tailwind className tokens.

Work file-by-file. After each file: visually inspect that no physical-direction layout class remains by grepping `(left|right|ml|mr|pl|pr)-[0-9]` inside the file.
  </action>
  <verify>
    <automated>grep -nE "\b(left|right|ml|mr|pl|pr)-[0-9]" src/components/ui/dialog.tsx src/components/ui/sheet.tsx src/components/ui/navigation-menu.tsx | grep -v "slide-in-from-\(left\|right\)" | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - The grep above returns 0 matches (zero physical-direction classes outside slide-in animation tokens).
    - `grep -cE "\b(start|end|ms|me|ps|pe)-[0-9]" src/components/ui/dialog.tsx src/components/ui/sheet.tsx src/components/ui/navigation-menu.tsx` returns a positive number (logical equivalents are now present).
    - `npx tsc --noEmit` exits 0.
    - `npm run lint` exits 0.
    - Dialog/Sheet/NavigationMenu still mount: `npm run build` exits 0 (the build resolves all primitives at compile time).
  </acceptance_criteria>
  <done>Three shadcn primitives shipped with logical-only positional classes. Animation-token directional names left untouched per scope decision; documented in summary.</done>
</task>

<task type="auto">
  <name>Task 2: Fix ShopDropdown panel positioning to logical start-0</name>
  <read_first>
    - src/components/nav/ShopDropdown.tsx (entire file — 67 lines)
    - .planning/STOREFRONT-UI-REVIEW-EN.md line 143 (evidence pointer)
  </read_first>
  <files>src/components/nav/ShopDropdown.tsx</files>
  <action>
Open `src/components/nav/ShopDropdown.tsx`. On line 50 the motion.div has className:

```
className="absolute top-full left-0 min-w-[160px] bg-white border border-black/5 shadow-xl py-2 z-50"
```

Replace `left-0` with `start-0`. Final className:

```
className="absolute top-full start-0 min-w-[160px] bg-white border border-black/5 shadow-xl py-2 z-50"
```

DO NOT touch the chrome colors here (`bg-white`, `border-black/5`) — Plan 11-04 owns the dark-chrome flip; that plan rewrites the panel chrome and inherits this logical-prop fix. Only the `left-0` → `start-0` change is in scope for this task.
  </action>
  <verify>
    <automated>grep -nE "\bleft-0\b" src/components/nav/ShopDropdown.tsx | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - Grep above returns 0 matches.
    - `grep -c "start-0" src/components/nav/ShopDropdown.tsx` returns at least 1.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>ShopDropdown.tsx:50 panel uses start-0; no physical left-0 anywhere in the file.</done>
</task>

</tasks>

<verification>
- F-Space-3 closed: zero `left-N`/`right-N`/`ml-N`/`mr-N`/`pl-N`/`pr-N` positional classes in the four touched files (slide-in animation tokens kept by design — they're not layout, they're transitions; scoped decision documented).
- Build green: `npm run build` exits 0.
</verification>

<success_criteria>
SC-1 (every [OPEN] finding closed) — partial: F-Space-3.
SC-6 (no new TS/lint errors) — full.
</success_criteria>

<output>
Create `.planning/phases/11-storefront-ui-ux-polish-en/11-02-SUMMARY.md` listing every class replacement made (file:line:before → after), the slide-in animation-token scope decision, and grep verification results.
</output>
